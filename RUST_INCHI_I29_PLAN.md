# I29 — PubChem 実データ検証と残バグ一覧 (2026-08-06)

> **要旨**: リポジトリ内コーパス (7,453 分子) は 100% 一致だが、それは
> 中性・単一成分に偏ったコーパスへの過適合だった。PubChem から CID 空間
> 全体をサンプリングした **18,563 分子**で測ると当初 **92.37%**。
>
> | 版 | 一致率 | 主な修正 |
> |---|---|---|
> | I28 まで | 92.37% | (PubChem 未検証) |
> | I29 | 94.66% | 多成分の成分順序 (炭素数降順) |
> | I30 | 96.97% | 未定義四面体中心 `?`、第四級中心のパリティ |
> | I31 | 97.05% | 成分単位の電荷中性化、128 原子超のパニック除去 |
> | I32 | 97.80% | 可動 H 群にかかる `/b` の除外、`/m` の `.` 連結 |
> | I33 | 98.01% | 環認識の 128 原子上限を撤廃 |
> | I34 | 98.21% | 孤立電子対の立体中心 (スルホキシド) |
> | I35 | 98.28% | 超原子価 Cl/Br/I の原子価表 |
> | I36 | 98.35% | 脱プロトンを酸性 O-H に限定 |
> | I37 | 98.44% | 同位体層 `/i` |
> | I38 | 98.70% | 可動電荷 (荷電可動 H 群の併合と酸点の拡大) |
> | I39 | 98.71% | 酸対に S を含む場合の N 端点、primary 限定を撤廃 |
> | I40 | 98.77% | 未定義四面体中心の判定に隣接済み立体中心を混ぜる |
> | I41 | 98.84% | プロトン化対象の絞り込み・金属ロック・アミジニウムリレー |
> | I42 | 98.92% | ビニロガスなアミジン-カルボニル互変異性の橋渡し・ホスホルアミド酸の二級 N 端点 |
> | I43 | 99.04% | P(V) の未定義四面体中心・多成分 `/m` 層の連結方式 |
> | I44 | 99.07% | 式が同じ異性体成分どうしの順序タイブレーク |
> | I46 | 99.10% | `/h` の `N*` 圧縮に成分の重原子数一致を追加 (公式 C ソース確認済み) |
> | I47 | 99.11% | 橋頭ヘテロ原子の毒判定に真の縮環要件を追加 |
> | I48 | 99.18% | 成分順序タイブレークを整数配列比較に修正 (I44 の是正) |
> | I49 | 99.19% | Hückel 判定: π 電子 2 個での芳香族認定を 3 員環限定に修正 |
> | I50 | **99.21%** | アミド型 N⁻ をプロトン化対象に追加 (単純アルキルアミン型は対象外のまま) |
>
> 残る molrs 側バグは **146 件 (0.79%)**。下の「残バグ一覧」は I29 時点の
> 分類で、解決済みには ✅ を付けた。

## 検証データの作り方

`corpus/pubchem_inchi.jsonl.gz` (18,563 分子) は PubChem FTP の
`CURRENT-Full/SDF` から作った。

- PUG-REST は IP 単位で絞られていて 503 (`PUGREST.ServerBusy`) を返すため
  使えない。`https://ftp.ncbi.nlm.nih.gov/pubchem/Compound/CURRENT-Full/SDF/`
  は普通に引ける。
- 全 358 ファイル (CID 1〜1.79 億) から 16 ファイルを等間隔で選び、各
  ファイルの先頭 900KB だけを **HTTP Range** で取得。gzip ストリームは
  先頭からなら途中まで展開できるので、巨大ファイルを落とさずに済む。
  低 CID だけに偏らないよう CID 空間を横断してサンプリングしている。
- オラクルは PubChem が **IUPAC 公式 InChI ソフトで計算した**
  `PUBCHEM_IUPAC_INCHI` / `_INCHIKEY`。RDKit 経由より直接的。
- 取得スクリプトは `scratchpad` に置いた使い捨て
  (`fetch_pubchem.py`、本書末尾に要点を残す)。

## 「molrs のバグ」と「SMILES の情報落ち」の切り分け

PubChem の InChI は CTAB から計算されているので、SMILES に情報が落ちて
いれば molrs のせいでない不一致が出る。**同じ SMILES を RDKit (公式 InChI
ライブラリ) に通して切り分けた**:

| | 件数 |
|---|---|
| molrs 一致 | 17,146 |
| **molrs のバグ** (RDKit == PubChem なのに molrs だけ違う) | **1,407** |
| SMILES 由来 (RDKit も PubChem と違う) | 10 |

**不一致の 99.3% は molrs 側**。SMILES ラウンドトリップの問題ではない。

## 修正済み: 多成分の成分順序 (I29)

`formula.rs` の `component_sort_key` が
「炭素を含む成分が先 → **重原子数昇順** → H 数降順」だった。これは
リポジトリ内コーパスの多成分 32 例 (`2Na.H2O4S` 系が多数) から導出した
規則で、**PubChem 実データ 863 件では 33.7% しか再現できない**。

正しい規則は **炭素数降順 → 重原子数降順 → H 数降順 → 式の辞書順**
(実測 96.2%)。炭素数が主キーであることは 863 件で例外なく成立する。

```
got  InChI=1S/C2H4O2.C6H5.Hg/...     ← 重原子数昇順 (4 < 6)
want InChI=1S/C6H5.C2H4O2.Hg/...     ← 炭素数降順 (6 > 2)
```

**効果**: PubChem 92.37% → **94.66%** (+426 件)。

### 既知の残差 (無機塩、33/863 = 3.8%)

「単原子カチオン + 多原子アニオン」だけは実 InChI がカチオンを先に置く
(`2Na.H2O4S`、`5Na.H3O4P.H2O3S`、`Cu.N2O4.2NO3`)。ただし `FH.O3Si.2Zn` は
単原子の Zn が最後なので「単原子金属を先頭」では説明できない (電荷層との
関係も含めて未解明)。単原子金属フラグを足す案は実測でスコアが下がった。

この変更でリポジトリ内コーパスの `[Na+].[Na+].[O-]S(=O)(=O)[O-]` **1 件が
退行**する。PubChem で +426 件と引き換えの取引として受け入れ、
`inchi_gate.rs` に理由付きの明示的な例外として書いた
(`known_divergence_monoatomic_cation_salts` も参照)。

## 残バグ一覧 (981 件、優先度順)

### 1. ✅ 解決 (I30) — 立体 `/t` の未定義中心 `?` (239 件)

**最大かつ最も明確なバグ**。構造上は立体源性だが SMILES で配置が指定
されていない中心を、実 InChI は `9?` のように `/t` に列挙する。molrs は
**完全に省略している**。`/t` だけが違う 422 件のうち 239 件がこれで、
molrs が中心を**多く**出したケースは 0 件 (常に不足)。

```
smi   CCC(C(=O)O1)S(=O)(=O)c2ccc(C)cc2 …  (CC[C@@H]1CC(C(=O)O1)S(=O)(=O)c2ccc(C)cc2)
got   /t10-
want  /t10-,12?
```

`stereo.rs` の `/b` 側には未定義二重結合を `?` として併記する処理が既に
ある (`end_stereogenic`)。同じ考え方を四面体中心に用意すればよい。
`/m` `/s` の食い違い (計 ~145 件) の多くもこれに連動しているはず。

### 2. ✅ 解決 (I30) — 第四級中心のパリティ反転 (147 件)

中心の数は合っているが 1 個 (稀に複数) のパリティが逆。第四級炭素・
スピロ・S/B/N+ を含む置換基で目立つ (S 102 件、N+ 22、B 21、Si 11、P 10)。

```
smi   C[C@H]1C[C@@H]([C@](O1)(CO)[Si](C)(C)C2=CC=CC=C2)O
got   /t11-,13-,14+/m0/s1
want  /t11-,13-,14-/m0/s1
```

molrs は CIP の R/S を正準番号基準のパリティへ変換している
(`raw = '-' iff (rs_bit XOR perm(CIP昇順→正準昇順))`) ので、CIP ランクが
ずれると反転する。Si (原子番号 14) や S、ホウ素まわりの CIP 実装を疑う。

### 3. ✅ ほぼ解決 (I31) — 分子内塩 (zwitterion) の電荷正規化 (115 → 65 件)

正味電荷 0 の分子内塩を、molrs はカルボキシラートを protonate してから
`/p-1` で戻すため `/q+1/p-1` が付き、式も H が 1 個多くなる。

```
smi   CC(=O)OC(CC(=O)[O-])C[N+](C)(C)C     (アセチルカルニチン)
got   InChI=1S/C9H18NO4/…/q+1/p-1
want  InChI=1S/C9H17NO4/…              ← 層なし
```

正味 +1 の側も `/q+1` と `/p+1` を取り違える。`normalize.rs` が
「四級 N のような**恒久電荷**」と「プロトン化で動かせる電荷」を区別して
いないのが原因と見られる。

### 4. ✅ 解決 (I32) — `/b` 層 (106 件)

余分に出す例が目立つ。可動 H 群にかかる C=N (アミジン型) で、実 InChI は
二重結合の位置自体が動くため `/b` を出さない。

```
smi   CC1=CC(=C(C(=C1S(=O)(=O)/N=C(\C2=CC=CC=C2)/N)C)C)OCCCC(=O)O
got   …(H2,21,22)(H,23,24)/b22-20+
want  …(H2,21,22)(H,23,24)          ← /b なし
```

`double_bond_layer` は未定義 (`?`) 側でのみ可動 H 群を除外している
(`tautomer_group_members`) が、**定義済みの `b.stereo` を持つ結合には
同じ除外を掛けていない**。

### 5. ✅ 解決 (I31) — 128 原子超でパニック (87 件、堅牢性のみ)

`rings.rs:38` の `assert!(n_atoms <= 128, "molecule too large for ring
perception")`。ペプチド等でごく普通に踏む。**ライブラリ API が正当な入力で
パニックする**のは呼び出し側から回避できず、`catch_unwind` を強いる。
最低でも `Err(Unsupported)` を返すべき。環認識が 128 bit のビットセットに
依存しているなら動的ビットセットへの置き換えが要る。

### 6. 一部のみ (`/h`) (25 → 80 件)

```
smi   C1=NC(=C(N1)C(=O)O)NC(=O)N
got   /h1H,(H,10,11)(H4,6,7,8,9,12)
want  /h1H,(H,7,8)(H,10,11)(H3,6,9,12)
```

I25〜I28 で詰めた群構築が、実データではまだ大きく取りすぎることがある。

### 7. `/i` 同位体層が未実装 (13 件)

```
smi   [3H]C([3H])(…)SC
want  …/t7-,8-,9-,10-/m1/s1/i5T2      ← got は /i 以降がない
```

`inchi_gate.rs` は `/i` を含む分子を除外しているので既存コーパスでは
見えていなかった。

### 8. SMILES パーサが超原子価ハロゲンを拒否 (8 件)

`valence 7 for atom N (Cl)` (過塩素酸型)、`valence 3 for atom N (I/Br)`
(超原子価ヨウ素) を `Invalid SMILES` で弾く。RDKit は受け付ける。

## 検証サイクル

```bash
rtk proxy cargo test --release --test inchi_gate   -- --nocapture --test-threads=1  # 7,453 件・完全一致
rtk proxy cargo test --release --test pubchem_gate -- --nocapture                   # 18,563 件・94.66%
```

`pubchem_gate.rs` は 128 原子超のパニックを `catch_unwind` で捕まえて
不一致として数えている (バグ 5 が直ったら外すこと)。

## 再取得スクリプトの要点

```python
BASE = "https://ftp.ncbi.nlm.nih.gov/pubchem/Compound/CURRENT-Full/SDF/"
# 358 ファイルから等間隔に 16 個選び、各先頭 900KB を Range で取得
req = Request(BASE + fn, headers={"User-Agent": ..., "Range": "bytes=0-900000"})
text = zlib.decompressobj(31).decompress(urlopen(req).read()).decode("utf8", "replace")
for rec in text.split("$$$$\n")[:-1]:          # 末尾は切れているので捨てる
    # PUBCHEM_SMILES / PUBCHEM_IUPAC_INCHI / PUBCHEM_IUPAC_INCHIKEY を拾う
```


---

# 追記: I30 / I31 の修正内容と現在の残差 (2026-08-06)

## I30 — 未定義四面体中心と第四級中心のパリティ (94.66% → 96.97%)

**未定義中心 `?`** (+168)。2 つ落とし穴があった:

- 置換基が異なるかの判定に **CIP ランクは使えない**。ほぼ全順序なので sp3
  炭素が軒並み立体源性になる (61.76% まで悪化)。1-WL 精緻化色
  (`refined_colors`) なら対称な枝に同じ色が付く。
- **定義済みの中心が 1 つも無ければ `/t` 層自体を出さない**。`CC(CN)O` の実
  InChI に `/t3?` は付かない (`/b` 側と同じ規則)。

**第四級中心のパリティ反転** (+260)。R/S 側は「隣接 3 個 + 暗黙 H」のときだけ
`n_swaps += 1` の補正を入れる。InChI 側の仮想 H を 4 要素の置換に含めると
その補正と打ち消し合うため 3 重原子 + H は正しくなるが、重原子 4 個では
打ち消す相手がなく符号が 1 つずれる。最小再現は `C[C@](N)(O)CC`
(実 InChI `/t4-/m0/s1`、修正前は `/m1`)。

## I31 — 成分単位の電荷中性化とパニック除去 (96.97% → 97.05%)

InChI が中性化するのは各原子ではなく **各成分の正味電荷**:

- 正味 0 の分子内塩 (アセチルカルニチン) は**プロトンを動かさない** →
  `/q` も `/p` も出ない。
- 正味 +1 (カルニチン) は中性の酸点 (COOH) から 1 個外して `/p+1`。
- 残る負電荷は成分内の陽電荷と釣り合っているので**可動プロトンとして
  数えない** (`is_locked_zwitterion_neg` を隣接限定から成分全体に一般化)。

**128 原子超**は `ChemError::Unsupported` を返すようにした (パニック除去)。
判定は原子数のみ — 結合数でも弾くと、環に使う結合 index が 128 未満で
実際には動いていた 7 件が落ちる。上限自体を上げるには `RingInvar` を動的
ビットセットにする必要があるが、RDKit の RINGINVAR と数値比較順を一致させて
いる都合で環認識結果が変わりうるため別課題。

## 現在の残差 537 件 (2.89%)

| 件数 | 種別 | 備考 |
|---|---|---|
| 101 | `/b` | 余分に出す (電荷非局在系) / 未定義 `?` の取りこぼし |
| 87 | 128 原子超 | `Unsupported` を返す (パニックはしない)。上限撤廃は別課題 |
| 69 | `/h` | **`(H-,…)` `(H3-,…)` の荷電可動 H 群が未実装**。I31 で脱プロトンした結果この表記が必要な分子が増えた |
| 65 | 電荷正規化 | 分子内塩の残り |
| 57 | `/t` | 残るパリティ差 |
| 57 | `/m,/s` | |
| 14 | `/i` | 同位体層が未実装 |
| 8 | 超原子価ハロゲン | SMILES パーサが拒否 |

**次にやるなら** `/h` の荷電可動 H 群 `(H-,…)` (69 件、表記の追加が要る) か
`/b` (101 件)。`/i` は未実装機能なので独立して足せる。


---

# 追記: I32 / I33 (2026-08-06)

## I32 — 互変異性結合の `/b` 除外と `/m` `/s` の書式 (97.05% → 97.80%)

- **可動 H 群にかかる二重結合を `/b` に出さない** (+97)。アミジン
  `…/N=C(\c1ccccc1)/N` は二重結合の位置自体が動くので E/Z を持たない。
  この除外が未定義 (`?`) 側にしか掛かっておらず、SMILES で構成が明示されて
  いると出力していた。`/b` だけが違う 101 件中 98 件が「余分に出す」側。
- **`/m` は `.` 連結、`/s` は構造全体で 1 個** (+40)。多成分で `;` 連結して
  いた。実 InChI は `/m0....` `/m.1` のように `.` で繋ぎ、`/s1` は 1 個だけ
  (コーパス 2,115 件すべて)。単一成分では同じ結果になるので既存コーパスでは
  見えなかった。
- **荷電可動 H 群 `(H-,…)` の表記**を実装。`(H-,10,11,12)` は「可動 H 1 個
  ＋ 負電荷 1」。従来は電荷を H 数に足し込んでいて区別できなかった。

## I33 — 環認識の 128 原子上限を撤廃 (97.80% → 98.01%)

`u128` のビットマスクを可変長 `BitSet` (Vec<u64>) に置換。87 件のうち
**47 件が正しい InChI を出せるようになった**。

肝は `Ord`: RDKit の RINGINVAR は数値比較で順序が決まり「どの環を残すか」を
左右するので、上位ワードから比較して `u128` と同じ順序を保つ。末尾ゼロ
ワードで等価性が変わらないよう `PartialEq` も `cmp` 経由。

## 現在の残差 359 件 (1.93%)

| 件数 | 種別 | 備考 |
|---|---|---|
| 80 | `/h` | **InChI の可動電荷 (酸点をまたぐ負電荷の非局在化) が未実装**。`(H-,…)` の表記は I32 で入れたが群の構築が未対応。加えて群を大きく取りすぎるケース |
| 68 | 電荷正規化 | 分子内塩の残り |
| 58 | `/t` | 残るパリティ差 |
| 30 | 多成分 `/c` | 128 原子超が通るようになって表に出た分を含む |
| 19 | `/m` | |
| 16 | `/i` | 同位体層が未実装 (独立して足せる) |
| 8 | 超原子価ハロゲン | SMILES パーサが拒否 |

**次にやるなら**: `/i` 同位体層 (16 件、独立した未実装機能で副作用が小さい)
→ `/h` の可動電荷 (80 件、正規化の中核なので影響範囲が広い)。


---

# 追記: I34–I36 (2026-08-06)

## I34 — 孤立電子対の立体中心 (98.01% → 98.21%)

`/t` だけが違う 58 件のうち 55 件が中心の取りこぼしで、多くが**スルホキシド
`[S@](=O)` の硫黄**。孤立電子対を 4 番目の置換基に数える中心 (重原子 3 個・
H 0 個) を `tetra_raw_parity` が受け付けていなかった。R/S 側は原子価 4 の
S/Se や P/As にタグを付けているので、受け入れるだけでよい。

符号条件も一般化した。I30 では「重原子 4 個なら反転」としていたが、正しくは
**「3 重原子 + 暗黙 H **以外**なら反転」** — R/S 側が `n_swaps += 1` の補正を
入れるのがその場合だけだから。孤立電子対中心も反転側に入る。

## I35 — 超原子価ハロゲン (98.21% → 98.28%)

`default_valences` がハロゲンを `[1]` に限っており、過塩素酸型
`Cl(=O)(=O)(=O)O` (原子価 7) を `Invalid SMILES` で弾いていた。RDKit の既定表
どおり Cl/Br/I を `1,3,5,7` にした (F は 1)。

## I36 — 脱プロトンを酸性 O-H に限定 (98.28% → 98.35%)

I31 の第 2 パス (正味 +1 の成分から中性の酸点を脱プロトン) が**任意の O-H**を
対象にしていた。単なるアルコールを外してはいけない: コリン
`C[N+](C)(C)CCO` の実 InChI は `C5H14NO/…/q+1` で OH の H を保つ。
隣の重原子 (C/N/S/P) が O/S へ二重結合を持つ O-H (カルボン酸・スルホン酸・
リン酸等) だけに限定した。

## 現在の残差 302 件 (1.63%)

| 件数 | 種別 | 備考 |
|---|---|---|
| 74 | `/h` | **可動電荷 (酸点をまたぐ負電荷の非局在化) が未実装**。表記 `(H-,…)` は I32 で入れたが群の構築が未対応。加えて群を大きく取りすぎるケース |
| 61 | 電荷正規化 | 残りは酸性度の判定が絡む個別ケース。例: `C[N+]1=CC=CC(=C1)C(=O)N` は実 InChI がアミド NH2 から外して `(H-,8,10)/p+1` にする (アルコールは外さないのにアミドは外す)。亜ヒ酸 `[O-][As]([O-])[O-]` は `/q-3` のままでプロトン化しない |
| 31 | `/t` | 残るパリティ差 |
| 30 | 多成分 `/c` | 巨大縮環系の番号付け |
| 19 | `/m` | |
| 16 | `/i` | **同位体層が未実装**。`AtomInfo` に isotope フィールドが無く `g.parsed` から引く必要がある。D/T (`/i5T2` `/i12D2`) と重原子同位体 (`/i4+1`) の 2 形式 |

**次にやるなら** `/i` (16 件、独立した未実装機能) か `/h` の可動電荷
(74 件、正規化の中核で影響範囲が広い)。


---

# 追記: I37 / I38 (2026-08-06)

## I37 — 同位体層 `/i` (98.35% → 98.44%)

`/i` の不一致 16 件がすべて解消。正準番号の昇順に 2 種類のエントリを `,` で並べる:

- 重原子そのものが同位体: `{番号}{+|-}{標準質量数との差}` (`4+1` = 13C)
- 同位体水素が付いている: `{番号}T{個数}D{個数}` (`5T2` `12D2` `1TD`)

同位体情報は `AtomInfo` に無いので SMILES パース結果 (`g.parsed`) を
`parser_to_graph` の逆引きで辿る。`[2H]`/`[3H]` は H マージ対象外で原子ノードとして
残るため「重原子が先頭に連続」の前提が崩れ、電荷正規化が no-op になる — つまり
添字がそのまま使える。

## I38 — 可動電荷 (98.44% → 98.70%)

最大の残バグだった `/h` の荷電可動 H 群。**成分から中性化のためにプロトンが
外れると (`/p+n`)、実 InChI はその負電荷を「成分内のどの酸点にも載りうる
可動電荷」として扱う**。結果、共役で繋がっていない群どうしまで 1 群に併合される:

```
smi  CC1=C(SC=[N+]1CC2=CN=C(N=C2N)C)CCOP(=O)(O)OP(=O)(O)OP(=O)(O)O
got  …(H,22,23)(H2,13,14,15)(H2,17,18,19)/p+1
want …(H5-,13,14,15,17,18,19,20,21,22,23)/p+1
```

アミノピリミジン (N 端点) と三リン酸 (O 端点) は分子内で何も共有していない。
PubChem の該当 44 分子すべてでこの規則が成り立つので、**併合条件は「その成分に
可動負電荷が 1 つ以上ある」だけ**でよい (`number.rs: merge_charged_groups`)。

3 つの落とし穴があった:

- **`(H0-,…)` という表記は存在しない**。可動 H 0 + 負電荷だけの群 (カルニチンの
  脱プロトン済みカルボキシラート) は h 層に何も出さない。`mh > 0` のフィルタは
  そのまま残し、併合は**その手前**で行う (併合されれば H を持つ群と一緒になる)。
  ここを緩めると 28 件が退行した。
- **電荷分離の疑似群は併合しない**。ニトロ/N-オキシドは可動 H も可動負電荷も
  持たない群として検出されるので、取り込むと O が `(H-,…)` に紛れ込む。
- **併合と同時に酸性 O-H を端点として取り込む**。カテコールの 2 つのフェノール性
  OH だけからなる `(H-,40,41)` はこれで初めて出る (`is_salt_endpoint`)。

### 脱プロトンの対象を広げた (normalize.rs 第 2 パス)

I36 の「隣の重原子が O/S へ二重結合を持つ O-H」では足りず、実 InChI は
フェノール・エノール・ヒドロキサム酸の N-O-H からも外す。糖・アルコールとの
境界は「**飽和炭素に付いた O-H かどうか**」— 芳香族炭素、二重結合を持つ炭素、
炭素ですらない相手はいずれも酸性側。コリンの `/q+1` (I36) はこれでも保たれる。

酸点らしい O-H が無くても四級 N+ は中和される。ニコチンアミド
`C[N+]1=CC=CC(=C1)C(=O)N` の実 InChI は `(H-,8,10)/p+1` で一級アミドから外す。
ただし「可動 H 群ならどれでも」にすると外し過ぎる (14 件退行) — **塩基性の**
アミン群 (アミノピリミジン・アミジン・アミノチアゾール・グアニジン) と一級
スルホンアミドは外さない。境界は「群に**炭素に付いた** O/S 端点があるか」で、
カルボニルを持つ群だけが選ばれる (スルホンアミドの O は S に付いているので落ちる)。

## 現在の残差 242 件 (1.30%)

| 件数 | 種別 | 備考 |
|---|---|---|
| 59 | `/h` のみ | 可動 H 群の構築が個別ケースでまだ誤る (詳細は I39 の節) |
| 35 | `/t` | 残るパリティ差 |
| 33 | `F`+`/h`+`/p`+`/q` | 電荷正規化の残り。酸性度の個別ケース (亜ヒ酸 `[O-][As]([O-])[O-]` は `/q-3` のままでプロトン化しない 等) |
| 21 | `/m` | |
| 16 | `F`+`/c`+`/h` | |
| 15 | `/c` | 巨大縮環系の番号付け |
| 9 | `/b` | |

層別に数えると (重複あり) `h` 144・`F` 73・`c` 54・`q` 48・`t` 47・`m` 39・`p` 38・
`b` 18。

**次にやるなら** `/h` の群構築の残り (59 件、I39 の節を参照)。次点は
`/t`+`/m` (計 60 件強、パリティ)。


---

# 追記: I39 (2026-08-11)

PubChem の `/h`-only 不一致 59 件を精査。**当初「群を大きく取りすぎる」と
記述していたのは誤り** — 実際は内訳が割れている:

| 分類 | 件数 |
|---|---|
| 群の個数自体が違う (合流の要不要を間違えている) | 18 |
| 同数だが got が want の**部分集合** (足りない) | 18 |
| 同数だが want が got の**部分集合** (余分) | 10 |
| その他のメンバー差 | 2 |

## 直した分 (98.70% → 98.71%、+2)

「足りない」18 件の多くは、**酸対 (中心に二重結合 O/S ≥1 かつ 供与体 O/S
≥1) に S を含むとき、N 端点を末端 NH2 (`heavy_deg==1`) に限定していた**
制限が原因。ジチオカルバミン酸型 `R-NH-C(=S)-S-` やチオカルバミン酸型
`R-NH-C(=O)-S-H` は N が二級 (`heavy_deg==2`) でも実 InChI は N を可動群に
含める:

```
smi  CC[NH+](CC)CC.C1=CC=C(C=C1)CCNC(=S)[S-]
got  …(H,17,18)…                    ← N が抜けている
want …(H,10,11,12)…                 ← N・両方の S を含む 3 端点
```

一方、酸対が O だけの純粋なカルバミン酸 (`R-NH-C(=O)-O-`) は N がアミド性の
まま固定される規則 (I19 §3.2/§3.3) は変わらない。`heavy_deg==1` の要求を
撤廃し `!carbamic_acid_like` だけにした (`seed_groups`)。

## 保留にした分: 非芳香族の共役鎖を跨ぐ橋渡し

残る「足りない」ケースの多くは、**両端とも非芳香族の単結合**を挟んだ共役
(アミド-イミン/アミジンの連鎖、チオアミド-ホスホロチオイル連結) が
必要になる:

```
smi  CN1CC(=O)N=C1N                       ← 環状アミド-アミジン
got  …(H2,5,6)…                           ← アミジンの 2 中心だけ
want …(H2,5,6,8)…                          ← アミドの O も同じ群

smi  CC1(C(=S)NP(=S)(S1)…)C                ← チオアミド-P(=S) 連結
got  …(H,12,15)…
want …(H,12,15,16)…                        ← P=S の S も同じ群
```

`mobile_groups` のブリッジ探索グラフ (`edge_ok`) は現在
**両端が芳香族、またはどちらか一方が芳香族**の結合にしか辺を張らない
(`(false, false) => false`)。これを単純に「両端が `has_search_slack` を
持てば単結合を橋渡しする」に緩めると、後者の P(=S) 系は正しく直る一方、
**孤立した供与体が無関係な受容体まで橋渡ししてしまう**退行を引き起こした:

```
smi  C=CS(=O)(=O)NC                        ← 二級スルホンアミド (非可動が正しい)
got  …(H,4,6)…                             ← 緩和後、N が SO2 の O と誤って合流
want （可動 H 群なし、N は固定）
```

「少なくとも片方が既に `seed_groups` のメンバー」という条件を足しても
同じ退行が再現した — 2-アミノピリジン型スルホンアミド
(`CS(=O)(=O)Nc1ccccn1`) の N は環側のアミジン様互変異性で正しく種メンバー
になるが、その**同じ N のもう一方の置換基**である無関係なスルホニル O まで
巻き込んでしまう。両方の失敗例に共通するのは「N はどこかで正当な理由で
可動だが、その正当性は今回追加した辺の**反対側**の系とは無関係」という点
— トポロジー (辺の有無・端点の種類) だけでは判別できず、`taut_shift_ok`
のような**特定の 2 系がどう繋がっているか**を見る判定が要る。この一般化は
今回は撤回し、`edge_ok` の `(false, false)` は `false` のまま維持した。

**次にやるなら**: この「非芳香族の共役鎖を跨ぐ橋渡し」を正しく実装するには、
`edge_ok` のトポロジー的な緩和ではなく、`taut_shift_ok` 側に「連鎖した
1,3-シフト」(a-c1-x が 1,3、x-c2-b も 1,3 のとき a-b を橋渡し可) を明示的に
追加し、各ホップが実在の種 (seed_groups の同一中心) を経由することを
要求するのが筋が良さそうだが、コーパスでの検証が要る。


---

# 追記: I40 (2026-08-11)

`/t`-only 不一致 43 件を精査。大半 (35 件) が「未定義立体源性中心 `?` の
取りこぼし」で、I30 で直したのと同じ症状が別の構造クラスで再発していた。

## 原因: 1-WL 色が既知の立体を見ない

`is_undefined_tetra` は候補中心の 4 置換基が互いに構成的に異なるかを
[`refined_colors`] (1-WL 精緻化色、トポロジーのみ) で判定する。だが
**トポロジーだけでは対称な 2 つの分岐**が、片方にだけ**定義済み立体中心**
(SMILES で `[C@H]`/`[C@@H]` が付いている) を持つために実際には区別できる
(ジアステレオトピック) ケースがある:

```
smi  CC(=O)OC1(C[C@H]2CC[C@@H](C1)N2C)C(=O)OC
got  …/t9-,10+                    ← C1 (四級) が完全に脱落
want …/t9-,10+,12?                ← C1 は未定義立体源性
```

環の 2 つの分岐 (`C[C@H]2CC[C@@H](C1)N2C` の両側) はグラフ構造としては
対称なピペリジン環だが、片方の分岐に環融合炭素の定義済み立体中心がある
ため、四級中心 C1 から見て 2 分岐は区別できる。1-WL 色はトポロジーだけを
見るため「同じ」と誤判定し、C1 自体が未定義立体源性として一切出力されて
いなかった。

## 修正: 既知の生パリティを 1-WL の初期色に混ぜる

[`refined_colors_seeded`] を追加し、初期色に `extra_key` を混ぜられるように
した。`is_undefined_tetra` が使う色だけ、各原子の [`tetra_raw_parity`]
(定義済みならその生パリティ、未定義なら空) を初期色に足す。WL 精緻化の
伝播で、この差が数ホップ離れた四級中心の分岐比較まで届く。

[`is_achiral`] (鏡像の自己同型探索) は**逆に**既知の立体を見てはいけない
(全パリティ反転を実現する自己同型を探すため、色に立体を混ぜると探索が
壊れる) ので、そちらは従来どおり [`refined_colors`] (立体なし) を直接呼ぶ
ままにした。2 つの用途で異なる色が要るのを `refined_colors_seeded` の
`extra_key` パラメータで切り分けた。

**効果**: PubChem 98.71% → **98.77%** (+11)。`/t`-only 不一致 43 → 24 件
(残 24 件のうち、まだ `?` を取りこぼす別原因のものと、CIP ランクが絡む
純粋なパリティ入れ替えが混在している — 詳細未調査)。

**次にやるなら**: 残る `/t`-only 24 件。少なくとも 2 種の異なる原因が
混在している:

- まだ `?` が脱落するケース (`t28?,29-,30-,31-,32-,33?` に `,34?` が足りない
  等) — 定義済み中心からの距離や、鎖状 (非環状) 分岐で今回の伝播が
  届いていない可能性。
- 隣接する 2 中心のパリティが**入れ替わって**いる純粋な符号バグ
  (`CO[C@@H]([C@@H]([C@@H](C(=O)OC)OC)OC)[C@H](C(=O)OC)OC` は
  `t7-,8-,9+,10-` に対し実 InChI は `t7-,8-,9-,10+` — 類似エステル鎖が
  並ぶ糖・ポリオール型分子で複数確認)。CIP ランクの実装を疑う。


---

# 追記: I41 (2026-08-11)

`Fhpq` (formula + h + p + q が同時に違う) 33 件を精査。electrolyte 系の
プロトン化ルールが実際にはもっと狭いことが分かった。

## 1. プロトン化対象の絞り込み (98.77% → 98.80%、+6)

`is_protonatable` は「N/O/S/Se/Te/ハロゲン化物イオンは何にでもプロトンを
付加する」という粗いルールだった。PubChem 実データでは以下がすべて
**プロトン化しない**:

- **N⁻ (脱プロトン化アミン)**: `CC[N-]C` は炭素だけに結合していても
  `/q-1` のまま。カルボキシラート O⁻ とは扱いが違う。→ `is_protonatable`
  から N を撤廃。
- **ヒドロキシルアミン型 `N-O⁻`**: `N[O-]` は `/q-1` のまま
  (プロトン化すると化学的には普通のヒドロキシルアミンになるが、実 InChI は
  やらない)。
- **超原子価ハロゲン錯体**: `CO[Cl-]C(Ph)3` のような共有結合ハロゲン化物
  アニオンはプロトン化しない。孤立イオン (`[Cl-]` 単体) だけが対象。
- **非酸性中心 (亜ヒ酸の As)**: `[O-][As]([O-])[O-]` は `/q-3` のまま。

一方、**酸性中心なら元素を問わない**ことも判明した — 亜硝酸
`N(=O)[O-]`・過塩素酸 `[O-]Cl(=O)(=O)=O`・亜セレン酸
`[O-][Se](=O)[O-]`・ヒ酸 `O[As](=O)([O-])[O-]` はいずれも通常どおり
プロトン化する。判定は「隣の重原子が炭素」または「隣の重原子が二重結合
O/S を持つ (元素不問)」の 2 条件のみ (`protonation_neighbor_ok`)。

最初 S/P だけに限定して実装したところ 18334→18330 に**退行**した —
過塩素酸・亜硝酸・ヒ酸・亜セレン酸が軒並みプロトン化されなくなったため。
元素を限定しない条件に一般化して解消。

## 2. 金属由来の電荷は恒久 (+1)

`COCCO[Hg]` (有機水銀アルコキシド) は disconnect_metals の異方開裂で
O が -1 になるが、実 InChI は `/q-1;+1` のままプロトン化しない — O の
隣が炭素だけなので (1) のルールだけでは正しく判定できない (炭素隣接の
O は通常プロトン化されるべきだから)。**金属との結合が異方開裂で切られた
という「由来」を明示的に記録する必要がある**。

`disconnect_metals` の返り値を `Disconnected { graph, metal_locked_ligands
}` に変更し、異方開裂で電荷を得た配位子 (ハロゲン以外) を
`metal_locked_ligands` として返す。`is_protonatable` はこの集合に含まれる
原子を常に除外する。ハロゲン化物 (`C[Hg]Cl` → HCl/p-1) は対象外 —
これは既存の検証済み挙動なので壊さない。

## 3. アミジニウム/グアニジニウム型の陽電荷リレー (+7)

`[N+]1=C(...)N` (環内 N+ が二重結合、環外 NH2 が単結合) のような中心は、
電荷を持つ N+ 自身は H を持たない (第 1 パスの「陽イオン→脱プロトン」条件
`h > 0` を満たさない) が、可動 H 群のもう一方の端点 (環外 NH2) が H を
持つ。実 InChI はこの群全体を 1 つのアミジンとして中性化する — N+ の電荷と
NH2 の 1 個の H が**同時に**消え、どちらの原子にも電荷は残らない:

```
smi  CCC(=O)C[N+]1=C(OC2=C1CCCC2)N
want …h12H,…/p+1                     ← NH2 が NH になり N+ は消える
```

これは I38 の「酸点をまたぐ可動負電荷」(無関係な外部の陽イオンが別の
酸性 O-H から借りる) とは方向が逆で、**群自身が電荷を持つ**場合だけに
限られる。無関係な陽イオンが塩基性のアミジン/グアニジン基から借りることは
ない (I38 で確認済み、チアミンのアミノピリミジンが `/q+1` のまま残るのが
その証拠) ので、「群のメンバー自身が正電荷」という条件だけで安全に区別
できた。

実装は `mobile_groups(g)` を中性化の**前**に一度呼び、正電荷を持つメンバー
→ H を持つドナーメンバーの対応表を作る。第 1 パスのループ内で該当する
正電荷原子を検出したら電荷だけ消し、ドナー側への「借り」を記録、ループ後に
まとめてドナーの H から差し引く (訪問順に依存しないため)。

3 件中 2 件 (`C[N+]1=C2C(=CC3…)…`, `CCCCN1C(=[N+](N=N1)C)N` に類似の
トリアゾリウム型) は完全一致したが、1 件 (`CCC(=O)C[N+]1=…N`) は q/p は
正しくなったが、中性化後もなお可動 H 群 `(H,12,13)` が残ってしまい
(実 InChI は `h12H` で完全に固定)、電荷解消後に再度その系をタウトマー
判定してしまうのが原因と見られる — 未解決。

## 現在の残差 215 件 (1.16%)

| 件数 | 種別 | 備考 |
|---|---|---|
| 58 | `/h` のみ | 可動 H 群の構築が個別ケースでまだ誤る |
| 24 | `/t` | 「missing `?`」の別原因と「隣接 2 中心のパリティ入れ替わり」の混在 (I40 節参照) |
| 22 | `/m` | |
| 17 | `F`+`/h`+`/p`+`/q` | 電荷正規化の残り (双性イオン・多成分塩の個別ケース) |
| 16 | `F`+`/c`+`/h` | |
| 15 | `/c` | 巨大縮環系の番号付け |
| 9 | `/b` | |

**次にやるなら**: `/h` の群構築の残り (58 件)。`/t` は 2 種類の異なる
原因が混在しているので、まず「パリティ入れ替わり」パターン (CIP ランク
疑い、少数だが原因が絞れている) を先に潰すのが良さそう。

# 追記: I42 (2026-08-11)

診断用に `molrs/examples/pubchem_diff.rs` を追加した (非ゲート)。
`cargo run --release --example pubchem_diff -- --layer h --limit N` で
不一致を層の差分パターンで分類し、指定パターンの実例を出力する — I29 の
節にある手作業の分類をやり直せるようにした。

`/h` のみ不一致 58 件を精査。

## 1. ビニロガスなアミジン-カルボニル互変異性の橋渡し (+11)

クレアチニン型の環 (`O=C-N=C-NH2`、環状・非環状いずれも) で、実 InChI は
カルボニル O とアミジンの N/NH2 を **1 つの互変異性群**にまとめる:

```text
smi  CN1CC(=O)N=C1N
want …(H2,5,6,8)          ← O(8)・環内 N(6)・NH2(5) が 1 群
got  …(H2,5,6)             ← O(8) が孤立したまま
```

`seed_groups` はカルボニル中心 (C3) の隣接ヘテロ原子を直接見るだけなので、
橋渡し役の環内 N (N6) がこの中心との結合が単結合でかつ自身に H を持たない
(だからこの中心単独では受容体にも供与体にもならない) 場合に届かない。
一方 N6 は**別の実二重結合** (アミジン C=N) を持っており、それを手放して
中心側に二重結合を渡す Kekule 構造が有効。

最初 `mobile_groups` のブリッジ探索グラフ (`edge_ok`) の `(false, false)`
分岐を「共役ハブ炭素 (ヘテロ原子への実二重結合を持つ C) から任意の隣接
ヘテロ原子への辺を許可」に緩める案 (`taut_shift_ok` の 1,5 判定も環メンバー
限定を撤廃) を試したところ、98.84% → 97.59% に**大退行**した — アミド
中心からエーテル O・遠方のニトロ基・無関係な二級スルホンアミドまで誤って
橋渡ししてしまうケースが大量に出た (I39 の節で「撤回した」と書いた案と
同種の問題)。

代わりに、ブリッジ探索グラフを経由せず**直接 union-find で繋ぐ**、両端に
それぞれ実二重結合を要求する狭い規則にした: 単結合 (hub, bridge) について
`hub` が炭素かつ `bridge` 以外のヘテロ原子への実二重結合を持ち (受容体 A
を確定できる)、かつ `bridge` 自身も別の実二重結合を持つ (`has_own_double`)
ときだけ `uf.union(A, bridge)` する。エーテル・エステルの O やニトロ基の
O のように自身が実二重結合を持たないヘテロ原子は `bridge` 側の条件で
弾かれるため、無関係な官能基を巻き込まない。

## 2. ホスホルアミド酸の二級 N 端点 (+3)

中心が P (C/N 以外) のとき、`seed_groups` は N 端点を末端 NH2
(`heavy_deg(nb) == 1`) に限定していた。I39 でジチオカルバミン酸型
(中心 C/S) の同種の制限を撤廃したのと同じ理由で、ホスホルアミド酸型
`R-NH-P(=O)` も二級 N (`heavy_deg == 2`) のまま可動:

```text
smi  C1CN(P(=O)(OC1)NCCCl)CCCl
want …(H,10,12)            ← 二級 N と P=O の O が可動
got  …(固定のまま)
```

P 中心だけ `heavy_deg` を問わないよう緩和した (S 中心は実測例がまだ無く
一級限定のまま残す)。

**効果**: PubChem 98.84% → **98.92%** (+14)。リポジトリ内コーパス
(`inchi_gate`) は既存の非対応ケースを除き変化なし (100%)。`/h`-only 不一致
58 → 47 件。

**次にやるなら**: 残る `/h`-only 47 件 (`pubchem_diff --layer h` で再分類
可能)。プリン骨格の環内 N=C-NH2 が三リン酸/糖の遠い酸性 H まで及ぶ大きな
群 (`(H4,25,26,27,28,29,30)` 等) や、`/t` の「パリティ入れ替わり」パターン
(I40 節) が次点。

# 追記: I43 (2026-08-11)

`/t` 不一致 24 件を `pubchem_diff --layer t` で機械的に分類 (差分の内訳:
got が want の部分集合で全て `?` が足りない 19 件、隣接 2 中心のパリティが
入れ替わっている 4 件、その他 1 件)。件数の多い前者を先に潰した。

## 1. P(V) の未定義四面体中心 (+2)

`is_undefined_tetra` の元素判定 (`ok_elem`) は P/As を
`lone_pair_center` (重原子 3 個 + 孤立電子対、スルホキシド型) だけに
限定していた。ホスホン酸エステル・ホスホルアミド酸型
`R-P(=O)(OR')(OR'')` のような P(V) (重原子 4 個、うち 1 個が実二重結合
`=O` でも幾何はそのまま四面体で、カルボニル炭素のような平面 sp2 には
ならない) を見落としていた:

```text
smi  CC(C)[C@H](OCCC=O)OP(=O)(CCCCC1=CC=CC=C1)CC(=O)N2C[C@H](C[C@@H]2C(=O)O)C3CCCCC3
want …t26-,27+,30+,39?/m0/s1     ← P(39) が未定義中心として載る
got  …t26-,27+,30+/m0/s1
```

`heavy.len() == 4 && n_h == 0` の場合も P/As で受け入れるよう緩和。ただし
置換基に電荷を持つ原子 (脱プロトン化リン酸 `P(=O)(O)[O-]` 等) が含まれる
centre は除外した — その `=O` と `-O⁻` は共鳴で等価 (カルボン酸の 2 個の
O が常に等価なのと同じ) だが、`is_undefined_tetra` が使う 1-WL 色の初期
キーは `formal_charge` を含むため区別してしまい、実際には立体源性でない
中心を誤って `?` にする回帰が最初の実装で 1 件出た (共鳴等価性まで考慮
した色付けは未実装、将来の課題)。

## 2. 多成分の `/m` 層の連結方式 (+21)

I32 で「`/m` は成分ごとの値を `.` で連結する」と実装したが、これは
**不完全**だった。実 InChI は `.` を「立体中心の無い成分」のプレース
ホルダとしてだけ使い、**定義済みの値どうしが隣接するときは区切りを挟まず
直接連結する**:

```text
7 成分、1〜5 番目が定義済み (0/1)、6〜7 番目が立体中心無し
want /m11111..     ← 定義済み 5 個を連結 + 空欄 2 個ぶんの `.`
got  /m1.1.1.1.1..  ← join(".") で定義済みどうしの間にも `.` を挟んでいた
```

`m_parts.join(".")` を「各要素を素通しし、空要素だけ `.` に置き換えて
連結」に直した。多成分分子 (立体を持つ塩・混合物) で頻発するパターンで、
効果が突出して大きかった。

**効果**: PubChem 98.92% → **99.04%** (+23)。リポジトリ内コーパス
(`inchi_gate`) は変化なし (単一成分のみで `/m` の複数成分連結が発生
しないため)。`/m`-only 不一致 22 → 1 件、`/t`-only 22 → 19 件 (`?` 側は
解消、パリティ入れ替わりパターンはまだ残る)。

**次にやるなら**: `/t` の「隣接 2 中心のパリティ入れ替わり」4 件
(`CO[C@@H]([C@@H]([C@@H](C(=O)OC)OC)OC)[C@H](C(=O)OC)OC` 等、いずれも
分岐が構成的によく似た隣接立体中心を持つ) — CIP ランクの階層的比較
(`cip_ranks`) の深いバグの疑いがあり、`/h`-only 45 件より原因調査の
難度は高いが件数は少ない。`/h`-only は
`pubchem_diff --layer h` で再分類して着手可能。

# 追記: I44 (2026-08-11)

## 1. 式が同じ異性体成分どうしの順序タイブレーク (+5)

`/c`-only 不一致 15 件は全て**多成分の構成異性体混合物** (分子式・原子数・
H 数まで一致するが構造が異なる複数の化合物の SMILES 連結) で、成分の
**列挙順序**だけが違っていた。`build_components` の成分ソート
(`component_sort_key`、I20) は炭素数・原子数・H 数・Hill 式の順で比較
するが、この 4 つが全部一致する構成異性体はそこでタイが付き、`sort_by_key`
が安定ソートのため**入力 (SMILES 記載) 順のまま**残ってしまっていた。

各成分の接続層文字列 (`connection_layer`、成分ごとに既に計算済みの
canonical numbering から作れる) を**降順**でさらに比較するタイブレークを
追加した。昇順で試すと 98.92%25 → 99.03% と**わずかに退行**したため、
降順が正しい方向と確認して確定した (符号を間違えると同程度の規模でも
逆効果になりうる好例)。

**効果**: PubChem 99.04% → **99.07%** (+5)。`/c`-only 不一致
15 → 11 件 (訂正: 当初 0 件と記録したが確認不足の誤りだった。降順は
2 成分がタイの場合には正しい方向だが、**3 成分以上が同時にタイ**する
場合はこの単純な「接続層文字列を降順で 1 回だけ比較」では実 InChI の
順序と一致しないケースが残る — 3 者のうち 2 者だけ順序が入れ替わって
いる例を確認した。実 InChI の本当の比較基準はまだ特定できていない)。
リポジトリ内コーパス変化なし (単一成分のみ)。

## 2. 検討したが見送った: テトラゾリル基の /b 消失 (`b` 9 件)

`.../C=C/C4=NNN=N4` (末端テトラゾール-5-イル基を持つビニル基) のような
パターンで、実 InChI は明示的に E 配置が指定されているにもかかわらず
`/b` そのものを一切出さないケースが複数見つかった。最初は「テトラゾール
環 (可動 H の 4 つの N が対称) に隣接する環炭素が、可動 H 群メンバーを
2 個以上隣接に持つ場合、その環炭素を置換基とする隣接二重結合は非立体源性
として `/b` から除外する」という規則を実装したが、**同一の局所構造
(同じテトラゾリル-ビニル-アミド骨格) を持つ別分子で `/b` が普通に**残る
反例**が同じ不一致リストの中に見つかった** (遠く離れた無関係な置換基が
違うだけ):

```text
smi  ...N5CCN(CC5)C(=O)CNC(=O)C[N+](C)(C)C   ← want に /b なし
smi  ...N5CCC[C@H](C5)NS(=O)(=O)O            ← want に /b7-6+ あり (同じテトラゾリルビニル部分)
```

ローカルなリング隣接パターンだけでは判別できず、分子全体のどこかに依存する
規則 (グラフ全体の対称性判定か、より広い互変異性群の併合の有無か) が
必要と見られる。実装 (`ring_poisoned` による除外) は 98.09%〜98.95% まで
悪化させたため**撤回・未コミット**。次に着手するならまず両分子の
`mobile_groups` 出力の違いを比較し、「なぜ一方だけテトラゾールが巨大な
群に併合されるのか」から調べるとよい (I42 のビニロガスリレーとの相互作用
の疑い)。

**次にやるなら**: `/h`-only 45 件 (`pubchem_diff --layer h` で再分類)。
`/t` の「隣接 2 中心のパリティ入れ替わり」4 件 (I43 節) も未着手。

# 追記: `/h` 不足群の調査メモ (2026-08-12、未解決)

`/h`-only 45 件を機械分類 (`want`/`got` の可動 H 群集合を比較): 16 件が
「`got` が `want` の部分集合 (過剰併合)」、11 件が「`got` が `want` の
真部分集合 (併合不足)」、11 件が「群の集合は一致・ヘッド (`H`/`H-`/`H2` 等)
だけ違う」、残り 7 件がその他。

## 併合不足側の 1 例 (CCN1N=C([N+](=N1)C)N、1-エチル-4-メチル-5-アミノ
テトラゾリウム) を深追いしたが未解決

`want` は可動群に環内 N をもう 1 個 (エチル側の環 N と結合したもう一方の
環 N、SMILES の "G" 位置) 含めるが、`got` は隣接 2 端点 (環外 NH2 と
直接結合する環 C の受容体・供与体対) だけ。手計算で複数の代替 Kekule
配置を試したが、エチル置換された環 N (「A」位置、非環外置換基で原子価が
既に 3 を使い切っている) が二重結合を受け取れないという制約のせいで、
3 番目の位置 (G) が可動 H を保持できる有効な代替構造を H 総数 2 個の
予算内で構成できなかった — 試すたびに環の反対側でも H が増えて予算超過
になるか、A の原子価超過になるかのどちらかに帰着した。`hub_allowed_endpoints`
(ブロッサム法によるフローモデル) が実際にどう判定しているかまでは追えて
いない。実 InChI の判定根拠が本当にこの手計算と食い違うのか (=molrs 側の
バグ)、それとも自分の輪環双極子検討が何か見落としているのかも未確定。

**次に着手するなら**: `hub_allowed_endpoints` のグラフ構築を実際にダンプ
できる専用デバッグツールを先に用意する (今回は `#[test]` に一時的な
`eprintln!` を仕込んで手動確認したが使い捨てで終わった)。手計算より先に
ツールで検証する方が速い。この特定分子だけでなく同型の「置換基で原子価を
使い切った環ヘテロ原子を含む荷電芳香環」パターン全体に効く可能性がある。

# 追記: `N*` 圧縮ルールの誤った仮説を検証・棄却 (2026-08-12)

`/h`-only の「群集合は一致・見た目のヘッドだけ違う」11 件の 1 つ
(`CCCC.CCC(=O)CC`、ブタン + ペンタン-3-オン) を調べたところ、`/h` 層の
「たまたま両成分の文字列が偶然一致した部分」(`3-4H2,1-2H3`) を実 InChI が
`2*…` に圧縮**しない**のに molrs は圧縮してしまうバグを発見した。

最初「`join_components` の `N*` 圧縮は各層が自分の文字列の一致だけで判定
しており、本当に同一構造かどうか (式 + 接続層) を見ていないのが原因」と
仮説を立て、`(式, 接続層文字列)` ベースの共通区間分けに直す実装を試した。
これは **PubChem 99.07% → 98.93% に大きく退行した** (-25)。原因を
`pubchem_diff --layer c` で確認したところ、`CC.C/C=C(...)...SC.C#C`
(エタン `CC` + アセチレン `C#C` を含む混合物) のような実在するケースで、
実 InChI は **式が違う 2 成分でも `/c` の文字列がたまたま一致すれば
`2*1-2` に圧縮する** (`h` 層は別々のまま) ことを確認した — つまり「本当に
同一構造でなければ圧縮しない」という仮説そのものが誤りだった。

```text
smi  CC.C/C=C(...)...SC.C#C   (エタン + ...+ アセチレン)
want …;2*1-2/h…;1-2H3;1-2H…   ← /c は式違いでも圧縮、/h は文字列が違うので別々
```

**実装 (I46 として試した `identity_runs`) は撤回・未コミット**、
`join_components` は元の「各層が自分の文字列一致だけで独立に判定する」
実装のまま。ブタン/ペンタン-3-オンの `/h` 偶然一致だけがなぜ圧縮されない
のか (エタン/アセチレンの `/c` 偶然一致は圧縮されるのに) は依然として
説明がつかず未解決 — 単発の異常事例の可能性が高く、確証がない一般化は
危険 (今回のように広範囲を退行させる) と分かったので、同種の追加事例が
複数見つかるまでは着手しない方がよい。

# 追記: I46 (2026-08-12) — 公式 C ソースで検証して解決

上の節で「未解決」としていた `/h` の `N*` 圧縮バグを、ユーザーが用意した
IUPAC 公式 InChI C 実装のソース (`~/ghq/github.com/IUPAC-InChI/InChI`) を
読んで解決した。手計算・複数分子の比較による仮説検証では埒が明かなかった
問題が、リファレンス実装の該当関数を直接読むだけで一発で判明した — 今後
同種の「実 InChI の判定基準が推測できない」問題に当たったら、まず手元の
コーパスから仮説を立てるより先にこのソースを読むべき。

## 該当コード: `INCHI-1-SRC/INCHI_BASE/src/ichiprt3.c`

各層 (`/c`,`/h`,`/q`,`/b`,`/t`,`/i` …) の直列化はそれぞれ専用関数
(`str_Connections`, `str_H_atoms`, `str_Charge2`, `str_Sp2`, …) が担当し、
どれも「直前の成分と同一なら `N*` にまとめる」処理を持つが、**同一性判定は
層ごとに全く別の生データを比較しており、共通の文字列比較ではない**:

- `str_Connections` (`/c`): `pINChI_Prev->lenConnTable == pINChI->lenConnTable`
  を確認してから `nConnTable` (接続表、元素も電荷も持たない位相情報のみ)
  を `memcmp`。
- `str_H_atoms` (`/h`): **`pINChI_Prev->nNumberOfAtoms == pINChI->nNumberOfAtoms`
  を確認してから** `nNum_H` (原子ごとの H 数、長さ `nNumberOfAtoms` の配列)
  を `memcmp` し、さらに `CompareTautNonIsoPartOfINChI` でタウトマー部分も
  比較。
- `str_Charge2` (`/q`): `nTotalCharge` (成分ごとの合計電荷、スカラー) を
  直接比較。
- `str_Sp2`/`str_Sp3` (`/b`/`/t`): `Eql_INChI_Stereo` が結合数/中心数と
  各配列を `memcmp`。

molrs の `join_components` はこれと違い、**どの層も一律「レンダリング後の
文字列が一致するか」だけ**で判定していた。ほとんどの層ではこれで実 InChI
と一致する (レンダリング前のデータが一致すれば当然レンダリング後の文字列も
一致するため) が、**`/h` だけは逆が成り立たない**: `nNum_H` 配列は
「成分の重原子数」ぶんの長さがあり、末尾が全部 0 (H なし) の原子で
埋まっていても文字列表現には出てこない。ペンタン-3-オン `CCC(=O)CC`
(6 原子: 5C+1O、カルボニル C・O は H 数 0) とブタン `CCCC` (4 原子) は
**`nNumberOfAtoms` が違う (6 ≠ 4) ので `nNum_H` の `memcmp` は長さの時点で
成立し得ない**が、レンダリング後の文字列はどちらも「末端 CH3 が 2 個・
内部 CH2 が 2 個」の部分しか可動 H を持たないため偶然「`3-4H2,1-2H3`」で
一致してしまい、molrs だけが誤って `2*…` に圧縮していた。

一方 `/c` は `lenConnTable` の一致がすでに実質「重原子数の一致」を要求して
いるので、molrs の文字列一致判定と実 InChI の判定は (エタン `CC` +
アセチレン `C#C` のように式が違っても接続表がたまたま一致するケースも
含めて) 常に同じ結果になる — 前の節で見つけた「エタン/アセチレンの `/c`
は圧縮されるのにブタン/ペンタン-3-オンの `/h` は圧縮されない」という非対称
の正体はこれだった。

## 修正

`join_components` に `keys: Option<&[usize]>` を足した `join_components_keyed`
を用意し、`/h` の呼び出しだけ各成分の重原子数 (`Component::inv.len()`、
末尾の H のみ成分には実成分と衝突しない番兵 `0` を割り当て) を渡すように
した。他の層 (`/c`/`/q`/`/b`/`/t`/`/i`) は上記の理由で対象外、変更なし。

**効果**: PubChem 99.07% → **99.10%** (+6)。リポジトリ内コーパス変化なし。

# 追記: I47 (2026-08-12) — 公式 C ソースツールチェーンの整備、橋頭原子の毒判定を修正

ユーザーが用意した公式ソース (`~/ghq/github.com/IUPAC-InChI/InChI`) から
実行できる `inchi-1` を実際にビルドした
(`INCHI-1-SRC/INCHI_EXE/inchi-1/gcc/` で `make`、`gcc`/`obabel` があれば
数秒で `INCHI-1-SRC/INCHI_EXE/bin/Linux/inchi-1` ができる)。`obabel
-ismi -omol` で SMILES → MOL に変換してから `inchi-1` に渡せば、**PubChem
に無い任意の分子**でも実 InChI の正解を手元で確認できる — I46 まではコーパス
に載っている実例に頼るしかなかったが、これで「もしこの置換基を外したら」
のような**能動的な仮説検証**ができるようになった。次のセッションでも
同じ手順でビルドし直せる (バイナリはリポジトリ外・gitignore 対象の
ビルド成果物なので molrs には含めない)。

## 発見: `poisoned_ring_atoms` の橋頭判定が縮環要件を満たしていなかった

I29 節「`/h` 不足群の調査メモ」で未解決としていたテトラゾリウム
(`CCN1N=C([N+](=N1)C)N`) を `inchi-1` で確認したところ、実 InChI は
`(H2,5,6,7)` (環 N を 1 個余分に含む 4 端点) だった。`mobile_groups` に
一時的なデバッグ出力を仕込んで追跡したところ、原因はこの分子の可動 H 群
検出そのものではなく、**`poisoned_ring_atoms` (I19 §3.4) がこの単環を
誤って「橋頭原子を含む縮環」として毒していた**ことだった。

`poisoned_ring_atoms` の docstring は「2 個以上の環に属する真の縮環共有
原子 (橋頭) だけを対象にする」と明記していたが、実装は `heavy_degree(a)
>= 3` (環内 2 結合 + 何か 1 個) だけで判定しており、**環に属する環数
自体はチェックしていなかった**。テトラゾリウムの環 N の 1 つがエチル基
(環外置換基) を持つだけで次数 3 になり、縮環でも何でもない単環全体が
誤って毒され、`mobile_groups` の一般ブリッジ探索がこの環のどの辺も使え
なくなっていた。

docstring 通りに「その環に属する SSSR 環の個数が 2 以上」を追加要件に
した。**効果**: PubChem 99.10% → **99.11%** (+2)。リポジトリ内コーパス
変化なし。

## この毒判定だけではテトラゾリウム分子自体は直らない

毒を解いても `CCN1N=C([N+](=N1)C)N` は依然として `(H2,5,6)` のまま
(3 番目の環 N が届かない)。原因は別: エチル側の環 N を除いた残り 3 原子
のうち、環内 N+ (`D`) はローカルな種 (`seed_groups`) で直接候補に入るが、
D は「実二重結合を既にマッチ済み」なので `alternating_reachable` の
探索の根には**なれない** (根になれるのは露出頂点だけ)。D の二重結合の
相手である最後の環 N (`G`) は、D 経由でしか到達しようがないのに D 自身が
根になれないので、原理的にどのルートからも見つからない。

`hub_allowed_endpoints` に渡す候補集合を「群のメンバーが実二重結合を
持つならその相手のヘテロ原子も候補に加える」という規則で広げる修正を
試した (相手の採否自体は既存の厳密なフロー判定に委ねる、安全なはずの
設計) が、**単独では PubChem を退行させた** (橋頭修正後の基準
99.11% から 99.08% へ、-3)。橋頭修正の**前**に同じ変更を試したときも
同様に退行した (99.10%→98.93%相当、詳細は未記録)。テトラゾリウムでは
むしろ効果が出なかった (`(H2,5,6)` のまま) にもかかわらず他の分子群を
壊しており、「群メンバーの実二重結合の相手を無条件に候補へ加える」だけ
では条件が緩すぎる — おそらく相手からさらに先 (もう一段階のビニロガス
関係) まで誤って引き込むケースがあるとみられる。**この案は撤回・未
コミット**。

**次に着手するなら**: `inchi-1` が手元でビルドできるようになったので、
まず `CCN1N=C([N+](=N1)C)N` のような最小分子を少しずつ変形しながら
(置換基の位置・環サイズ・電荷位置) `inchi-1` に通し、「群メンバーの
実二重結合の相手を候補に加える」規則が具体的にどう狭めれば安全か
(例えば「相手自身も別の実二重結合を持たない終端原子に限る」等) を
先に何件も突き合わせてから実装する — 今回のように 1 分子の手計算だけで
実装してもすぐ広範囲の退行を招く。

# 追記: I48 (2026-08-12) — I44 の成分順序タイブレークを公式ソースで是正

`/t` の残差を眺めていて、`/c`-only 不一致がまだ 12 件残っていた (I44 は
「15→11」止まりで、3 成分以上が同時にタイする場合はまだ実 InChI と順序が
合わないと記録していた)。今回もまず `ichimake.c` を読んでから直した。

成分の並べ替えは `ichisort.c` ではなく `ichimake.c` の
`CompINChITaut2`/`CompINChINonTaut2` → `CompINChI2` が担当している。
`CompINChI2` は Hill 式・原子数・元素列・**接続表**・H 数・互変異性の順に
多段階で比較する定型的な比較関数で、接続表の比較は:

```c
if ((ret = i2->lenConnTable - i1->lenConnTable))
    return ret; /* longer connection table first */
num = i2->lenConnTable;
for (i = 0; i < num; i++) {
    if ((ret = (int)i2->nConnTable[i] - (int)i1->nConnTable[i]))
        return ret; /* greater connection table first */
}
```

**`nConnTable` という整数配列を要素ごとに数値比較**しており、レンダリング
後の `/c` 文字列を比較しているのではない。I44 の実装は `connection_layer`
が返す**文字列**を辞書式比較していた — これは正準番号が 2 桁になる成分
(例: 10 番目以降の原子) を含む混合物で数値比較と食い違う (文字列比較では
`"12" < "9"` だが数値比較では `12 > 9`) ため、3 成分以上が同時にタイする
複雑な混合物で誤った順序になっていた。

## 修正

`Component::adj` (正準番号ごとの整数隣接リスト、レンダリング前のデータ)
を正準番号順に平坦化した `Vec<usize>` を作り、「配列が長い方が先」「同じ
位置なら値が大きい方が先」を数値のまま比較する `compare_conn_table_numeric`
に置き換えた。レンダリング (`connection_layer` の文字列化) を経由しない
ので、桁数による文字列比較のずれが原理的に起きなくなった。

**効果**: PubChem 99.11% → **99.18%** (+12)。`/c`-only 不一致 12 → 5 件
(訂正: コミット時点で「0 件」と書いたが未確認の思い込みだった。実際に
`pubchem_diff --layer c` で確認したのは今回の追記時点)。リポジトリ内
コーパス変化なし。I44 のときより効果が大きかった — 文字列比較のバグは
2 成分だけがタイするケースでも (2 桁の正準番号を含む限り) 潜在的に
間違えていた可能性があり、単に「3 成分以上のタイ」だけの問題ではなかった
とみられる。

## 残る `/c`-only 5 件

いずれも「式は完全一致するが構造が異なる (縮環系の位置異性体) 2〜3 成分の
順序が逆」という同じ形。`nConnTable` は `ichimak2.c` で `LinearCT`
(`ichican2.c`/`ichicano.c` の `Ctbl`) をコピーしたものだが、`Ctbl` の
組み立て自体は「正準番号順に全隣接原子を列挙」という単純な形ではなく、
canon ランキング内部の別のデータ構造から生成されている可能性が高く
(`ichican2.c:6680` 付近、`GetCanonRanking` 系の関数から辿る必要がある)、
今回はそこまで追わずに撤退した。`Component::adj` をそのまま平坦化した
今回の実装は「たいてい合っている近似」であって `Ctbl` の正確な再現では
ない可能性がある。

**次に着手するなら**: `/c`-only 残り 5 件はまず `ichican2.c` の `Ctbl`
構築元 (`GetCanonRanking` 等) を辿って正確な線形化順序を確認してから
再挑戦する。ただし件数が少ないので優先度は低い。`/h`-only 37 件・`/t`
の「パリティ入れ替わり」4 件 (I43 節)・アミジニウム/テトラゾリウム系の
可動 H 群の残り (I47 節) の方が件数が多く優先度が高い。`inchi-1` が
手元でビルドできるので、次はどれについても「実装前に少数の変形分子で
`inchi-1` の挙動を確認してから直す」進め方を徹底するとよい。

# 追記: I49 (2026-08-13) — Hückel 判定のバグ (InChI 固有ではなく molrs 全体)

`/h`-only の過剰併合側を見直していて、`COC1=CC2=C(C=C1)NC3=C2CCN(...)
CCNC6=C(C(=O)C6=O)O` (3 分子、同じ骨格の遊離塩基・TFA 塩・エステル体)
という一群を見つけた。共通するのは環外 NH/OH を持つシクロブテン-1,2-
ジオン環 (スクエア酸型: `NC1=C(O)C(=O)C1=O`) — `mobile_groups` を一時
デバッグ出力したところ、この 4 員環が `is_aromatic=true` と誤認識され
ていた。

## 原因: `aromaticity.rs::perceive_aromaticity` の Hückel 判定

`huckel` 閉包は `total >= 2 && (total-2) % 4 == 0` で判定しており、
`total == 2` を**環サイズ不問**で通していた。スクエア酸型の環は
C3=C4 (環内二重結合 1 本、各 1 電子) + C(=O) が 2 個 (環外カルボニル、
電子寄与 0、トロポン型の「環外二重結合を持つが環の π 系には数える」
扱い) で合計 2 電子となり、Hückel の式だけを見ると通ってしまう。

しかし化学的には、電子数 2 の芳香族性が妥当なのは環全体が真に共役する
**3 員環** (シクロプロペニルカチオン型) だけ。4 員環以上で電子数 2 と
いうことは「環内の実二重結合はたった 1 本で、残りの環メンバーは全員
環外二重結合のぶんの 0 電子寄与しかない」という局所的な状況を意味する
だけで、環全体で電子が非局在化しているわけではない — トロポン (7 員環、
環外 C=O が 0 電子、残り 6 個の環メンバーが環内で完全に交互する 3 本の
二重結合を持ち計 6 電子) のような正当な芳香族性とはまったく違う。

この 4 員環が (誤って) 芳香族と判定されたことで、`mobile_groups` の
アロマ環ブリッジ探索が発火し、本来は完全に固定であるはずの環外 NH/OH を
環内カルボニルと誤併合していた:

```text
smi  CCNC1=C(O)C(=O)C1=O
want …h7-8H,2H2,1H3                    ← NH・OH ともに固定
got  …h2H2,1H3,(H,7,10)(H,8,9)          ← 誤ってそれぞれ環内カルボニル O と併合
```

これは InChI 固有の話ではなく **molrs のアロマ環判定そのもの**のバグ
(canonical SMILES・2D 描画・部分構造検索など芳香族性を使う全機能に
影響しうる) なので、修正後は `pubchem_gate`/`inchi_gate` だけでなく
`cargo test --release` 全体 (`rdkit_compat` の 7,453 分子構造比較を含む)
で退行が無いことを確認した。

## 修正

`huckel` 閉包で `total == 2` のときだけ `atom_set.len() <= 3` を追加要件
にした (3 員環はそのまま通す、4 員環以上は電子数 2 では通らないように
する)。`total >= 6` 側の通常の判定は変更なし。

**効果**: PubChem 99.18% → **99.19%** (+3、影響する分子自体が少ない)。
リポジトリ内コーパスと `rdkit_compat` は変化なし (どちらのコーパスにも
この構造パターンの分子が含まれていなかったとみられる — 逆に言うと
このバグは今回の PubChem 18,563 分子コーパスがなければ見つからなかった)。

**次に着手するなら**: `/h`-only の残り (over-merge 側はまだ他のパターンが
複数残っている)・`/t` の「パリティ入れ替わり」4 件・アミジニウム/
テトラゾリウム系の可動 H 群の残り (I47 節) が引き続き優先度が高い。

## 調査メモ: `Fhpq` 19 件 (未着手、次点で優先度が高い)

`F`+`h`+`p`+`q` が同時に違う 19 件をざっと見た限り、共通する形がある:
分子全体が中性でない (イオン性の塩・錯体) とき、molrs は**成分ごとの
残余電荷をそのまま `/q` に出す**が、実 InChI は**各成分をローカルに
可能な限り中性化してから**、それでも残る**全体の電荷不均衡だけ**を
1 つの `/p` にまとめる、という違いがあるように見える。例:

```text
smi  C(=O)[NH-].C(=O)[NH-].[Zr+2]   (ホルムアミドアニオン 2 個 + Zr2+)
got  …(H-,2,3);…/q2*-1;+2            ← アニオン 2 個がそれぞれ /q-1 のまま
want …(H2,2,3);…/q;;+2/p-2           ← アニオンは中性アミドまで中性化、
                                        Zr の +2 だけ残し、差分 2 個を
                                        まとめて /p-2 で表現
```

`[NH3+]O` (対イオンなしの単純な陽イオン) も同じ系統: got は `/p+1` で
一部を動かそうとするが、動かす相手がどこにも無いので want はそのまま
`H4NO` (H を 1 個多く数えた式) + `/q+1` で表現する — 「動かす相手が
無いなら無理に /p を使わず /q のまま、動かす相手があるなら成分をまたいで
でも /p にまとめる」という使い分けがありそう。

**未着手の理由**: `molrs/src/inchi/normalize.rs` の電荷処理は I31/I36/I38/
I41 で何度も実測ベースで手を入れてきた、既にかなりチューニングされた
コードで、下手に触ると波及範囲が広い。実 InChI 側の対応する処理は
`ichi_bns.c` (フローネットワーク) にありそうだが、まだソースを読めて
いない。着手するならまずソースを読んでから、`normalize.rs` の
`neutralize` 関数の全体構造を理解した上で、`inchi-1` で複数の変形塩
(陽イオン単独・陽陰イオン別成分・陽陰イオン同成分など) を突き合わせて
ルールを確定させてから直すこと — 今回同様、目算だけで直すと退行しやすい
領域。

# 追記: I50 (2026-08-13) — 上の仮説は誤りだった。実際はアミド型 N⁻ の
# プロトン化漏れ

`ichi_bns.c` を読む前に、まず `inchi-1` で「成分をまたいだ中和」の仮説を
単純な例で検証したところ、そもそも仮説が誤りだと分かった:

```text
smi  [NH4+].CC(=O)[O-]              (アンモニウム + 酢酸イオン、別成分)
want …h1H3,(H,3,4);1H3               ← q/p 一切なし、両方とも完全中性化
got  同じ                            ← molrs は既に正しい

smi  CC(=O)[O-].CC(=O)[O-].[Mg+2]   (酢酸イオン 2 個 + Mg2+、Zr の例の類似形)
want …q;;+2/p-2
got  同じ                            ← molrs は既に正しい
```

つまり molrs は「成分をまたいだ中和 → 残り不均衡を /p にまとめる」を
**既に正しく実装していた**。実際に失敗するのはカルボキシラート `O⁻` では
なく**アミド型 `N⁻`** (`C(=O)[NH-]`) を含む場合だけだった:

```text
smi  C(=O)[NH-].[Na+]
want …h1H,(H2,2,3);/q;+1/p-1
got  …h1H,(H-,2,3);/q-1;+1           ← N⁻ がプロトン化されず /q-1 のまま
```

## 原因

`normalize.rs::is_protonatable` は I41 で「N は一切プロトン化しない」と
撤廃していた。根拠は `CC[N-]C` (単純アルキルアミンの脱プロトン化アニオン)
が実 InChI で `/q-1` のまま残ることだったが、これは**あらゆる N⁻**では
なく**酸性でない N⁻** (隣接原子がすべて "ただの炭素" で、二重結合 O/S を
持つ酸性中心が無い) に限った話だった。アミド型 N⁻ (`C(=O)[NH-]`、隣接
炭素が実二重結合 O を持つ) はカルボキシラート O⁻ と同じ「酸性中心の
共役塩基」なので、実 InChI は素直にプロトン化して中性アミドに戻す。

## 修正

`is_protonatable` に N を復活させたが、O/S/Se/Te 用の
`protonation_neighbor_ok` (隣が「ただの炭素」なら常に許可) とは**別の**、
より厳しい判定 `acidic_center` (隣接炭素自身が実二重結合 O/S を持つ場合
だけ許可、`protonation_neighbor_ok` から「隣が炭素なら常に許可」の
ショートカットを除いたもの) を N 専用に使うことで、`CC[N-]C` (隣が
ただの sp3 炭素) は今まで通り対象外のまま、`C(=O)[NH-]` (隣の炭素が
C=O を持つ) だけを新たに対象にした。

**効果**: PubChem 99.19% → **99.21%** (+4)。リポジトリ内コーパス変化
なし。`Fhpq`/`Fchpq`/`q` など複数のカテゴリにまたがっていた不一致が
同時に減った (charge/proton まわりの単一のバグが複数の層の組み合わせに
波及していたため)。

**教訓**: 「実装前に少数の変形分子で `inchi-1` の挙動を確認する」を
徹底したことで、最初に立てた「成分をまたいだ中和ができていない」という
仮説を**コードを書く前に**棄却できた。もし仮説どおりに
`neutralize`/`ichi_bns.c` の全体構造を先に読み込んでいたら、実際の
バグ (`is_protonatable` の N 除外) とは無関係な大掛かりな調査になって
いた可能性が高い — 複雑な処理を疑う前に、まず小さな反例で仮説自体を
壊せないか試すのが効率的。

**次に着手するなら**: `/h`-only の残り (over-merge 側はまだ他のパターンが
複数残っている)・`/t` の「パリティ入れ替わり」4 件・アミジニウム/
テトラゾリウム系の可動 H 群の残り (I47 節) が引き続き優先度が高い。

# 追記: I51 (2026-08-13) — チオホスホルアミドの P=S 端点漏れ

`/h`-only (34 件) の内訳を精査していて、`CC1(C(=S)NP(=S)(S1)C2=CC=C(C=C2)OC)C`
のような 1,3,4-チアジアゾリジン-2-チオン + チオホスホリル置換体で
`(H,12,15)` (中心炭素の N/S だけ) が実 InChI の `(H,12,15,16)` (P=S の S
まで含む 3 元групп) に対して 1 原子欠けているのを見つけた。

## 検証

まず `inchi-1` で最小構成のモデル化合物 4 種を突き合わせた:

```text
CC(=S)NP(=S)(C)C  → (H,5,7,8)   ← チオアミド + P=S
CC(=S)NP(=O)(C)C  → (H,5,6,8)   ← チオアミド + P=O (I42 で既に対応済み)
CC(=O)NP(=S)(C)C  → (H,5,6,8)   ← アミド + P=S
CC(=O)NP(=O)(C)C  → (H,5,6,7)   ← アミド + P=O (I42 で既に対応済み、対照)
```

いずれも N を挟んで両側の二重結合ヘテロ原子 (C 側の O/S、P 側の O/S) が
すべて同じ 1 群に入る。molrs は P=O の 2 例だけ一致し、P=S を含む 2 例で
P 側の S が群から漏れていた。

## 原因

`seed_groups` の `n_double_o_ok` (中心が P/S のとき末端 N を星型端点として
許すかどうかの判定) は `n_double_o` (中心に**二重結合した O の数**) だけを
見ていた。P 中心の場合、二重結合の相手が O ではなく S だと `n_double_o == 0`
になり、`n_double_o_ok` が false になって N 自体が P 中心星型の端点候補から
除外され、星型の端点が P=S の S 一つだけ (`endpoints.len() < 2`) になって
星型そのものが成立しなくなっていた (I42 で確認したホスホルアミド酸型の
ロジックが O 限定のまま S に一般化されていなかった)。

## 修正

`n_double_o` と同じ形の `n_double_s` (中心に二重結合した S の数) を追加し、
`n_double_o_ok` の P 中心条件だけを `n_double_o + n_double_s >= 1` に緩和した。
S 中心 (スルホンアミド、`n_double_o >= 2` 必須) の判定は従来通り O 限定の
まま変更していない — 実測例がまだ無い領域を広げるのは [[feedback-inchi-
narrow-fixes]] の方針に反するため。

**効果**: PubChem 99.21% → **99.22%** (18417/18563 → 18418/18563、+1)。
`inchi_gate` は変化なし (100% のまま)、`pubchem_diff` の再カウントで新規の
不一致は増えず `h` バケツが 34→33 件に減っただけ (総不一致 146→145)。
影響件数は小さいが、根本原因も修正範囲も明確なマイクロフィックス。

**次に着手するなら**: `/h`-only の残り (今回のような「星型の端点条件が
特定原子種に限定されすぎている」パターンが他にもないか)、トリアゾリウム/
テトラゾリウム系可動 H 群の未解決の欠落 (I47 節、`CCN1N=C([N+](=N1)CC)N`
系: 実 InChI は環内 N をもう 1 個多く群に含める)、`/t` の「パリティ入れ替
わり」(22 件、依然最大級のバケツ)。`Fhpq`/`Fch` も 2 桁件数のまま残って
いる。

# 追記: I52 調査メモ (2026-08-14、未実装) — `/t`・`/b` の 2 系統の深いバグを特定

I51 の続きで `pubchem_diff --layer t` (22 件) と `--layer b` (9 件) を
精査し、それぞれ根本原因を特定した。どちらも既存コードへの小さなパッチ
では直せない、アルゴリズム自体の欠落なので、実装は次回以降に見送り、
ここに原因・再現手順・打ち手の方向性を残す (コードは一切コミットして
いない — 直前の I51 の状態から変更なし)。

## (A) `/t`: リング内の「対称に見えるが実は区別できる」立体中心の未検出

22 件中 15 件以上がこのパターン: `stereo.rs::is_undefined_tetra` が
本来 `?` を出すべき候補中心を見逃す。例:

```text
smi  C1[C@H](C([C@@H](CC1(C(=O)OCC2=CC=CC=C2)O)O)O)O   (キナ酸型の環)
got  …t10-,11-/m1/s1
want …t10-,11-,12?,14?/m1/s1        ← 環内の 2 中心 (12, 14) が丸ごと欠落
```

環は `A(CH2)-B(canon10,定義済みR)-C(canon12)-D(canon11,定義済みR)-
E(CH2)-F(canon14,4置換の環閉鎖炭素)-A` という並びで、B と D は**純粋な
トポロジーだけ見ると鏡映対称** (`refined_colors` で `own_color` が完全
一致、I40 の `extra_key` に生パリティを混ぜても B・D がどちらも `+`
(R) で一致するため、依然として同色になる)。`is_undefined_tetra` は
「中心の重原子置換基の色がすべて異なるか」だけを見るので、C の 2 つの
置換基 (B 側と D 側) が同色 → 「区別できない」と誤判定し `?` を出さない。
F についても同様 (A・E という 2 つの CH2 が同色に収束する)。

しかし実 InChI は C・F を両方 `?` として出す。これは B・D が**トポロジー
的には対称でも、実際に定義された配置が同じ (R, R)** であるために、その
対称操作 (B↔D を入れ替える自己同型) が**配置を保ったまま実現できない**
ケース (擬似不斉中心の一種) だからだと考えられる — もし B・D が鏡像関係
(R, S) だったなら本当に対称で C は立体源性にならないはず (要 `inchi-1`
での確認、未検証)。

**必要な打ち手**: 単純な「隣接置換基の 1-WL 色が全部違うか」ではなく、
「候補中心を固定し、かつ全ての**定義済み中心の生パリティを反転させずに**
保つ、対象の 2 置換基を入れ替える自己同型が存在するか」を厳密に判定する
必要がある。`is_achiral` に既にほぼ同じ探索 (`search`/`check` のバック
トラッキング) があるので、あれを流用・一般化 (パリティ反転ではなく
「center を固定し 2 つの子ノードを入れ替える」制約に変える) するのが
筋が良さそうだが、探索空間が変わるため退行リスクが高く、今回は着手を
見送った。着手する場合は必ず: (1) `inchi-1` で B/D が鏡像関係 (R,S) の
対照分子を作り、その場合は C が `?` を**得ない**ことを先に確認してから
実装する、(2) 実装後は `pubchem_diff --layer t` の全件で新規の不一致が
出ていないか確認する。

## (B) `/b`: H を伴わない「可動電荷」共役系での E/Z 誤検出・未検出

`/b` バケツ 9 件中 5 件は同一の骨格 (チアゾール-ピリミジノン系の薬物様
分子、`.../C=C/C4=NNN=N4` のスチリルテトラゾール置換基を持つ) で、
共通してこの形:

```text
smi  CN(C)/C=C/C=[N+](C)C.F[P-](F)(F)(F)(F)F
got  …/b7-5+                         ← 実 InChI には /b 自体が無い
want (/b 層なし)
```

この分子には **可動 H が一切無い** (両末端の N はどちらも完全に置換済み、
ジメチルアミノ基とジメチルイミニウム基)。つまり `tautomer_group_members`
(`tgroup`) によるマークが絶対に付かない、純粋な「電荷が非局在化した
プッシュプル共役カチオン」(ビニローグなホルムアミジニウム、`C=C-C=N+`
の 4 原子共役鎖で正電荷が非局在) で、実 InChI はこの C=C を「位置が
確定しない」ものとして E/Z を出さない。既存の `tgroup` ベースの除外
(I32、`double_bond_layer` 冒頭の `if tgroup[a] || tgroup[c] { continue; }`)
は H の移動を前提にしているため、この H 無し・電荷のみが動く系を一切
捕捉できない。

同バケツの残り 2 件 (`CC1=C(C=CC(=C)C=C(C=CC1=C)F)/C=C\C=C` など) は別系統
で、`/b` に `?` (未定義立体源性二重結合) が複数欠落するパターン — こちらは
(A) の `/t` 版の未定義中心検出と同根 (`end_stereogenic` 判定の一端が環内
共役を通じてしか区別できない置換基を持つ) の可能性が高いが未検証。

**必要な打ち手**: (B) 前半は molrs にまだ存在しない「H を伴わない可動
電荷鎖」の検出ロジックが要る — 最小限は「両端がヘテロ原子 (少なくとも
片方が形式電荷を持つ) で、間に奇数個の sp2 炭素が挟まった交互単重結合鎖
(アリル/ペンタジエニル型カチオン・アニオンの拡張) を検出し、その鎖内の
全 C=C・C=N を `tgroup` 相当の除外対象にする」新規関数が必要。着手する
場合は既存の `mobile_groups`/`seed_groups` (H 前提) とは別建てにするか、
「端点が H を持たない」場合も種にできるよう `seed_groups` 自体を拡張
するかの設計判断が要る。後者は影響範囲が広く regression リスクが高いので、
前者 (独立した小さな検出関数) から始めるのが安全。まずは `inchi-1` で
鎖長を 1 つずつ変えた最小分子 (`C=C-C=[NH2+]`、`C=C-C=C-C=[NH2+]` 等) を
作り、どこまでが「電荷が動く」と判定されるかの境界を実測してから設計
すること。

**今回は未着手・未コミット**。次回セッションはまず (A) の対照実験
(`inchi-1` で R,S 版キナ酸型分子を確認) から始めるのが最も着手しやすい。

# 追記: I53 (2026-08-14) — 成分並び順の主キーがそもそも根拠薄弱だった

I52 の `/t`・`/b` 調査 (どちらも未着手のまま docs コミット) の後、
`pubchem_diff` で `F` (式層) 系のバケツ (`Fch` 16 件・`Fhpq` 15 件など) を
再トリアージした。式が一致しない事例は少なく、大半は**式そのものは合って
いるが多成分の並び順が違う** (`H2O3S2.2H3N` vs `2H3N.H2O3S2`、
`C10H7NO2.C10H6ClNO` vs `C10H6ClNO.C10H7NO2` など) というパターンで、
順序が変わると `/c`・`/h` 側の成分区切り位置も連動してズレるため
`Fch`/`Fhpq` のような複合バケツとして観測されていた。

## 原因

`molrs/src/inchi/formula.rs::component_sort_key` の主キーは PubChem 863 件
から逆算した経験則「炭素数の降順」だった (docstring に自ら「未解明」
「既知の残差」と明記されていた箇所)。ソースコードで裏を取ると
`ichimake.c::CompINChI2` の主キーは実際には**炭素数の比較ではなく**、
`CompareHillFormulasNoH` — Hill 式 (H を除く) を元素記号ごとにトークン化
して**先頭から順に**「元素記号のアルファベット順 (小さい方が先)、同じ
記号なら個数の多い方が先」を比較するもので、炭素だけ特別に「どの元素
よりも小さい」扱いにする (`GetElementAndCount`: 1 文字記号が `"C"` なら
`"A"` に読み替える、"炭素含有化合物を先頭に" というコメント付き)。
式が尽きた側は「無限に大きい元素」とみなし後ろに回る (`CH2O` が `CH2`
より先、というコメント付きの例が関数直上にある)。

「炭素数降順」ルールは炭素**含有**成分を先頭に置く効果までは正しく
説明できていたが、炭素を含まない成分同士 (`H3N` vs `H2O4S`) や炭素数が
同じ成分同士 (`C10H6ClNO` vs `C10H7NO2`、Cl < N なので前者が先) の
順序は説明できていなかった — まさに `F` 系バケツの実体。

## 修正

`component_sort_key` を全面書き直し。新しいヘルパー `hill_no_h_key` が
Hill 式文字列 (`component_formula` の出力、既に炭素→H→アルファベット順で
構築済み) を先頭から走査し、`H` トークンは完全に読み飛ばし (位置にも比較
にも数えない)、炭素は専用の最小値 `ElemKey::Carbon`、それ以外は
`ElemKey::Elem(記号)` として個数と一緒に `Vec` へ積む。末尾に番兵
`ElemKey::End` (最大値) を追加することで「式が短い方が辞書順で後ろ」を
`Vec` の辞書式比較だけで自然に表現できる。主キーをこの `Vec` に差し替え、
第 2 キー (重原子数降順)・第 3 キー (H 数降順)・第 4 キー (式文字列昇順)
はそのまま残した (`ichimake.c` 側もこの後に重原子数・接続表・H 数の比較が
続くため、大枠の順序は変えていない)。

**検証**: `2Na.H2O4S` (硫酸ナトリウム) が実際に直った。これは以前
`known_divergence_monoatomic_cation_salts` という「既知の残差」テストで
`H2O4S.2Na` (誤り) をわざと期待値にしていた分子そのもの — テストの
docstring 自体が「単原子カチオンを先頭にする特別ルールも試したが
`FH.O3Si.2Zn` で単原子 Zn が最後に来て説明できず未解明」と書いていたが、
実際には特別ルールなど不要で、`Na` (N) と `O4S` (O) の元素比較だけで
説明できていた (N < O)。テストを `monoatomic_cation_before_polyatomic_anion`
に改名し、正しい期待値に更新した。

**効果**: PubChem 99.22% → **99.34%** (18418/18563 → 18441/18563、+23)。
`Fch` 16→4 件・`Fhpq` 15→17 件 (若干増、別系統の偶発一致が崩れた分と
思われるが総数としては純減)、総不一致 145→122 件。`inchi_gate` は変化なし
(7452/7452・7244/7244 とも 100%)。`cargo test --release` フルスイート
271 件全て pass (単体テスト 1 件を「既知の残差」から「修正済み」に更新)。
`cargo clippy --all-targets -- -D warnings` はクリーン (戻り値のタプルが
`Vec` を含むようになったため `type_complexity` を避ける型エイリアス
`ComponentSortKey` を追加)。

**教訓**: 「PubChem コーパスから逆算した経験則」は当たり判定こそ悪くない
ものの、docstring 自身が「未解明」「既知の残差」と認めている箇所は
実装の弱点が自己申告されているシグナルであり、C ソースを読めば一発で
解決することが多い — I48 (`CompINChI2` の接続表比較) に続き、この
`component_sort_key` も同じ関数の中の、もっと手前の主キーだった。

**次に着手するなら**: `Fhpq` (17 件、charge/proton 関連、I50 の残り)・
`h` (33 件、over/under-merge の残りサブパターン)・`t` (22 件、I52 が
見つけた「同じ配置を持つ両隣接確定中心に挟まれた擬似不斉」パターンには
自己同型探索の一般化が必要)・`b` (9 件、I52 が見つけた「H を伴わない
可動電荷」二重結合、独立した検出関数が必要) が引き続き優先度高い。
`c` (5 件) はまだ `ichican2.c` を読めていない。

# 追記: I54 (2026-08-14) — ペルオキシ酸アニオンの末端 O がプロトン化対象外だった

`Fhpq` (I53 直後で 17 件) を再確認したところ、17 件中 4 件が同じ部分構造
`C(=O)O[O-]` (過ギ酸/過炭酸型のペルオキシ酸アニオン、`O=C-O-O⁻`) を
含んでいた。これはビカーボネート `OC(=O)[O-]` (分岐、C に直接 2 個の O)
とは違い、O-O 単結合を挟んだ鎖状構造 (ペルオキシド結合) — 見た目が似た
別の反例だと誤認しないよう `inchi-1` で単独分子として確認:

```text
smi  C(=O)O[O-].[K+]
want InChI=1S/CH2O3.K/c2-1-4-3;/h1,3H;/q;+1/p-1   ← 中性化 + /p-1
got  InChI=1S/CHO3.K/c2-1-4-3;/h1H;/q-1;+1          ← O⁻ のまま
```

同じ骨格で O が直接 C に 2 個ぶら下がる `OC(=O)[O-]` (ビカーボネート) や
単純カルボキシラート `C(=O)[O-]` は既に (I36/I38 の時点で) 正しく
中性化されていた — 差は「隣接原子が酸性中心 (実二重結合 O/S) かどうか」
の判定 (`protonation_neighbor_ok`) が 1 ホップしか見ていないこと。
ペルオキシ酸の末端 O⁻ の隣は「もう 1 個の O (単結合、それ自体は二重結合
を持たない)」であり、酸性中心はさらにもう 1 ホップ先の C(=O) にある。

## 修正

`protonation_neighbor_ok` に、隣接原子が O (単結合) のときだけ発火する
2 ホップ目の判定を追加: その O のさらに隣接原子のいずれかが
`acidic_center` (実二重結合 O/S を持つ) を満たせば許可する。ヒドロキシル
アミン型 (`N-O-`) の除外は隣が N のケースにしか関わらないので影響しない。

**効果**: PubChem 99.34% → **99.35%** (18441/18563 → 18442/18563、+1)。
サンプルで見えた 4 件のうち実際に効いたのは 1 件のみ — 残り 3 件は
同じ分子内に別の未解決の不一致 (他成分の /t・/h 等) を抱えた多成分の塩
だったため、この 1 点を直しても全体一致には至らなかった (`Fhpq` 件数は
17→16、他のカテゴリに移動しただけで消えていない例も含む)。`inchi_gate`
は変化なし (100%/100%)。`cargo test --release` フルスイート 253 件
全て pass。`cargo clippy --all-targets -- -D warnings` クリーン。

**教訓**: 1 つのサンプル分布の中に同じ部分構造が複数回現れても、それが
「1 バグ = N 件の直接的な得点」とは限らない — 多成分の塩は複数の不一致が
重なっていることが多く、部分修正では全体一致まで届かないことがある。
効果測定は必ず `ok/n` の実数で確認し、サンプル出現回数から得点を予測
しないこと。

**次に着手するなら**: 今回の残り 3 件を含む `Fhpq`/`Fch` の残存分・`h`
(33 件、over/under-merge の残りサブパターン、特にリン酸エステル同士の
可動 H 群の成分間併合)・`t`・`b` (どちらも I52 で自己同型探索/新規検出
関数が必要と判明済み)・`c` (5 件、`ichican2.c` 未読) が引き続き優先度高い。

# 追記: I55 (2026-08-14) — 孤立 H が電荷正規化の不変条件を丸ごと壊していた

`Fhpq` を再度ダンプすると、`[H-]` (孤立ヒドリドイオン) を陽イオン側の
対イオンと一緒に別成分として持つ塩が複数件 (`[H-].C(=O)O[O-].[K+].[K+]`
等) 混じっていた。最小構成で切り分け:

```text
smi  [H-].C(=O)O[O-].[K+].[K+]
want InChI=1S/CH2O3.2K.H/c2-1-4-3;;;/h1,3H;;;/q;2*+1;-1/p-1
got  InChI=1S/CHO3.2K.H/c2-1-4-3;;;/h1H;;;/q;;-1;2*+1;      ← ペルオキシ酸
                                                                アニオンが
                                                                中性化されず、
                                                                [H-] 自身の
                                                                電荷も消えて
                                                                いる (合計
                                                                +1 に破綻)

smi  [H-].[K+]                          ← 対イオンだけの最小例
want InChI=1S/K.H/q+1;-1
got  InChI=1S/K.H/q+1;                  ← H 側の -1 が丸ごと消える
```

`C(=O)O[O-].[K+]` ([H-] を除いた同じ塩) は I54 で正しく中性化される
ことを確認済みだったので、[H-] という「無関係なはずの追加原子」の**有無**
だけで挙動が変わるのが不自然だった。

## 原因: `build_molecule_graph` が「重原子が先頭に連続」という
`normalize::neutralize` の前提を守っていなかった

`neutralize` は冒頭で

```rust
let n_heavy = g.atoms.iter().filter(|a| a.symbol != "H").count();
let heavy_contiguous = (0..n_heavy).all(|i| g.atoms[i].symbol != "H");
if !heavy_contiguous { return (g.clone(), 0, 0); }
```

という前提チェックを持ち、崩れていたら電荷正規化を**丸ごとスキップ**する
(コメントには「重原子が先頭に連続していることを前提 (build_molecule_graph
の不変条件)」とあるが、実際には守られていないケースがあった)。

`build_molecule_graph` の「重原子の再インデックス」ステップは、暗黙 H
(通常の C-H 等) は先に併合してしまうが、**併合されない H** (`[H-]` の
ように電荷付き、`[2H]` のように同位体付き、原子クラス付き、あるいは
`[H][H]` のように単結合でない/次数が 1 でない H) は SMILES の元の出現
位置のまま `graph_to_parser` に積まれる。`[H-].C(=O)O[O-]...` のように
そうした H が SMILES の**先頭**に来ると、生成されたグラフでは atom 0 が
H・atom 1 以降が重原子になり、`heavy_contiguous` が偽になって
`neutralize` が即座に諦めていた。デバッグ出力で実際に確認:

```text
0: H charge=-1 adj=[]      ← [H-] が index 0
1: C charge=0  adj=[2,3,7]
2: O charge=0  adj=[1]
3: O charge=0  adj=[1,4]
4: O charge=-1 adj=[3]     ← ペルオキシ酸アニオンの O⁻ (中性化されるはず)
5: K charge=1  adj=[]
6: K charge=1  adj=[]
```

これは分子内の「無関係などこか一箇所に電荷付き孤立 H がいるかどうか」
だけで、その分子**全体**の電荷正規化が丸ごと有効/無効に切り替わるという
影響範囲の広いバグで、`Fhpq` に限らず孤立 H を含む多くの塩構造に波及
していたと考えられる。

## 修正 (3 箇所)

1. **`graph.rs`** — 「重原子の再インデックス」を 2 パスに分割: まず
   `symbol != "H"` の未マージ原子を積み、次に `symbol == "H"` の未マージ
   原子 (孤立ヒドリド・同位体 H など) を積む。これで `graph_to_parser`
   は常に「真の重原子が先頭に連続」という不変条件を満たすようになる
   (`n_heavy` の値自体は変わらず、並びだけが変わる)。
2. **`normalize.rs`** — 金属切断由来の孤立 H を再構成する箇所
   (コメント「金属切断で生じた孤立 H … `[BiH3]` → `Bi.3H`」) が
   `formal_charge: 0` を決め打ちしていたのを `g.atoms[old].formal_charge`
   を引き継ぐよう修正。金属水素化物由来の孤立 H は元々中性なので影響
   なし、`[H-]` のように入力自体が孤立 H に電荷を持たせる場合だけ電荷が
   保持されるようになる。
3. **`disconnect.rs` / `mod.rs`** — 孤立 H だけの成分は「/c・/立体層には
   一切寄与せず、/q も常に空欄」という前提 (`hydrogen_component_sizes`
   ベースの `pad`) で `/q` 層を組み立てていたが、これは孤立 H が中性である
   ことが前提。同じ走査順で孤立 H 成分ごとの正味電荷を返す
   `hydrogen_component_charges` を追加し、`/q` 層の末尾にその値を反映する
   よう `mod.rs` を修正 (`pad` ではなく実電荷を `chain`)。

## 検証

- `[H-].C(=O)O[O-].[K+].[K+]` / `[H-].[K+]` / `C(=O)O[O-].[K+]`
  (回帰確認) / `C(=O)[NH-].[Na+]` (I50 の回帰確認) / `[2H]C(=O)C[NH3+].
  [Cl-]` (同位体の回帰確認) を `inchi-1` と突き合わせ、全て完全一致。
- `pubchem_gate`: 99.35% → **99.38%** (18442/18563 → 18448/18563、+6)。
- `inchi_gate`: 変化なし (100%/100%)。付随する `canonical_numbering_
  matches_auxinfo` の目視ログ (アサーションはしていない診断出力) は
  `C[Hg]C`・`CC[BiH2]` 系の式・連結成分の不一致がむしろ複数件解消され、
  新規の不一致は増えていない (`[Ca+2]` の酢酸 2 個や立体の既知の不一致は
  修正前後で同一)。
- 単体テスト `graph::tests::deuterium_kept` が「`[2H]OC` の原子 0 が H」
  という旧不変条件を前提にしていたため、新しい並び (重原子 O・C が先、
  同位体 H が後ろ) に合わせて更新。
- `cargo test --release` フルスイート (235 lib テスト + `rdkit_compat`
  `corpus` `functional_group_compat` `name_compat` 等の統合テスト全て)
  全て pass。`cargo clippy --all-targets -- -D warnings` クリーン。

**教訓**: 「成分ごとの電荷判定ロジック」を何度も丁寧にチューニングして
きた `normalize.rs` の中身を疑う前に、そもそも入力グラフがその関数の
前提 (不変条件) を満たしているかを確認する価値があった — 今回はロジック
自体は (該当分子に関しては) 正しく、前提条件の破れによる早期リターンが
原因だった。「同じ部分構造なのに関係ない原子の有無で結果が変わる」という
不自然さが調査の出発点になった。

**次に着手するなら**: `h` (33 件、over/under-merge の残りサブパターン)・
`t` (22 件)・`Fhpq` の残り (10 件)・`b` (9 件、`t`/`b` は I52 で自己同型
探索/新規検出関数が必要と判明済み)・`c` (5 件、`ichican2.c` 未読) が
引き続き優先度高い。

# 追記: I56 (2026-08-15) — 成分順序タイブレークの接続表が実 InChI の
# `nConnTable` と違う構造だった (I48/I53 のさらに先)

`c` バケツ (5 件) を調べたところ、いずれも「同じ Hill 式 (I53 で修正済み)
の異性体成分どうしが 2 個以上あり、その並び順そのものが逆」という形。
I48 で「レンダリング前の整数配列を数値比較」に直したはずだったが、その
整数配列の**組み立て方自体**が実 InChI と違っていた。

## 調査方法

`inchi-1` で構築した小分子 (o-キシレン/p-キシレン混合物など) では I48 の
実装 (`Component::adj` を正準番号順にそのままフラット化: 各原子の**全**
隣接 (前方・後退どちらも) を昇順で並べる) がたまたま実 InChI と同じ順序
を出したため、これまで見逃されていた。しかし PubChem の 50 原子級の
2 成分タイでは逆順になるケースがあり、`ichimake.c::CompINChI2` が比較する
`nConnTable` の実体 (`ichimak2.c` で `pCS->LinearCT` をコピーしているだけ
と分かったので、その `LinearCT` の構築元である `ichicano.c::
UpdateFullLinearCT` を読んだ) は、I48 の想定と違う構造だった:

```c
#define CT_ATOMID CT_ATOMID_IS_CURRANK  /* mode.h のデフォルト */
for (rank = 1; rank <= num_atoms; rank++) {
    i = nAtomNumber[rank - 1];
    COMPARE_WITH_CT(LinearCT, nCTLen, rank, ...);      /* ← 自分自身の正準番号を積む */
    /* 隣接を正準番号 (rank) 昇順にソートしてから */
    for (k = 0; k < num_neigh; k++) {
        if (r_neigh = nRank[...]; r_neigh CT_NEIGH_SMALLER_THAN rank)  /* ← 自分より小さい番号だけ */
            COMPARE_WITH_CT(LinearCT, nCTLen, r_neigh, ...);
    }
}
```

つまり各原子について「自分自身の正準番号 → 自分より小さい正準番号を持つ
隣接 (後退辺) だけを昇順」という**インターリーブされた**列であり、I48 の
「全隣接をフラット化」とは別物。自分自身の番号を挟むこと自体は両成分で
共通 (1,2,3,...,N と同じ) なので比較結果に影響しないように見えるが、
**後退辺の蓄積ペースが成分ごとに違う**ため挟み込みの位置がずれ、結果として
「どの値とどの値が同じ配列位置で比較されるか」が変わり、全隣接フラット化
版とは異なる箇所で最初の差が現れる (実際に符号が逆転するケースを確認)。

`inchi-1` で構築した検証例 3 組 (異性体 2 成分ペア×2、異性体 3 成分×1、
合計 6 成分) すべてで「自分の番号 + 後退辺のみ、降順比較」モデルが実際の
並び順と一致することを Python でのプロトタイプ比較により確認してから実装
した。

## 修正

`layers.rs::compare_conn_table_numeric` 内の `flat` クロージャを、
「原子 1..n について `i` 自身 → `adj[i]` のうち `< i` の要素のみ (既に
昇順ソート済みなのでそのままフィルタで足りる)」を積むように書き換えた。
比較の方向 (「配列が長い方が先」「同じ位置なら値が大きい方が先」) 自体は
I48 のまま変更なし。

**効果**: PubChem 99.38% → **99.42%** (18448/18563 → 18455/18563、+7)。
`inchi_gate` は変化なし (100%/100%)。`c` バケツは 5→0 とは限らない
(タイブレーク以外の理由で `c` が出ているケースが混ざっている可能性がある
ため、次回 `pubchem_diff` で再確認すること) が、成分順序が絡む `c`/`h`/`m`
などの複合バケツにも波及した可能性が高い。

**次に着手するなら**: `h` (over/under-merge の残りサブパターン)・`t`
(自己同型探索が必要、I52)・`b` (mobile-charge-without-H 検出、I52)・
`Fhpq` の残りが引き続き優先度高い。`ichican2.c` はこの増分で `LinearCT`
構築部分 (`ichicano.c`) のみ読んだので、正準番号そのものの決定ロジック
(候補ランキングの比較) はまだ手つかず。

# 追記: I57 (2026-08-16) — ボロン酸 B-OH が「非炭素なら酸性」判定に誤爆

`Fhpq` の残りを再調査中、`B(C1=CC(=CC=C1)C[N+]2=CC=C(...)...)(O)O` のような
ボロン酸 + 第四級アンモニウム (ピリジニウム型) の分子で、molrs の H 総数が
実 InChI より 1 個少ないケースを見つけた (`C27H23...` vs 正しい
`C27H24...`)。最小化した `c1ccc(C[n+]2ccccc2)cc1B(O)O` で `inchi-1` と
比較したところ、実 InChI は:

```text
want: …h1-9,15-16H,10H2/q+1        ← B(OH)2 の両方の O-H とも保持、/q+1 のまま
got : …h1-9,15H,10H2/p+1           ← O(16) の H を奪って /p+1 に変換
```

## 原因

`normalize.rs::neutralize` の第 2 パス (I31/I36/I38 で拡張、量的にはコリン
`C[N+](C)(C)CCO` のような「外せる陽イオン H が無い成分の残り陽電荷を、
中性の酸性 O-H から借りる」処理) が使う `is_acidic_oh` は、O の隣接原子が
「炭素でない」だけで無条件に酸性側と判定していた。意図はヒドロキサム酸・
ヒドロキシルアミン型の N-O-H (`I38`) を拾うためだったが、ボロン酸の
B-O-H もこの「非炭素」に該当してしまい、無関係な四級アンモニウムの +1 を
中和する借り先として誤って選ばれていた。ボロン酸の O-H は (`inchi-1` で
確認した通り) このような無関係な陽電荷の相手にはならず、実 InChI は中性の
まま `/q+1` に残す。

## 修正

`is_acidic_oh` の「隣接原子の元素が炭素でない」条件から B (ホウ素) だけを
除外 (`a.symbol != "C" && a.symbol != "B"`)。N-O-H (ヒドロキサム酸等) は
従来どおり対象のまま。

**効果**: PubChem 99.42% → **99.43%** (18455/18563 → 18457/18563、+2)。
`inchi_gate` 変化なし (100%/100%)。`cargo test --release` 全体 (235 ユニット
テスト + 全統合スイート) 変化なし・パス。

**教訓**: `is_acidic_oh` のような「例からの逆算で決めた包括条件」(ここでは
「非炭素なら酸性」) は、新しい元素の組み合わせ (今回は B) が corpus に
現れるたびに誤爆しうる。同種の「炭素以外は全部許可」というガード節を
他にも探す価値があるかもしれない (Si-OH 等はまだ未検証)。

**次に着手するなら**: `h` (over/under-merge の残りサブパターン)・`t`
(自己同型探索が必要、I52)・`b` (mobile-charge-without-H 検出、I52)・
`Fhpq` の残り (`W(=O)(=O)(O)O` のような水和物型、`S(=O)(=O)[O-]` を含む
双性イオンの成分内中和漏れ、カルバニオン `[CH2-]` のプロトン化判定など、
まだ複数の異なるサブパターンが混在) が引き続き優先度高い。

# 追記: I58 (2026-08-16) — 単純アルコキシドが孤立イオンでも誤ってプロトン化されていた

`Fhpq` の残りを再調査中 (I57 時点で 8 件)、`[Li+].CC(C)(C)[O-]...` のような
アルコキシド塩を含む複数の分子で、molrs が該当成分に余分な `/p` を付けて
いるのを見つけた。`inchi-1` で最小化して確認:

```text
smi  CC[O-].[Na+]              (エトキシドナトリウム)
want …h2H2,1H3;/q-1;+1          ← O⁻ のまま、/p なし
got  …h2H2,1H3;/q;+1/p-1        ← O⁻ をプロトン化して中性エタノールにしてしまう

smi  CC(C)(C)[O-].[Li+]         (tert-ブトキシドリチウム、上と同型)
同様に want は /p なし、got は /p-1
```

一方、カルボキシラート・フェノラート・エノラートは対照実験で全て
プロトン化されることを確認済み (`CC(=O)[O-]`・`c1ccccc1[O-].[Na+]`・
`CC(=C)[O-]` すべて `/p-1`)。**チオラート**は対照的で、単純アルキル炭素
隣接でも実 InChI がプロトン化する (`CC[S-].[Na+]` → `/p-1`) — O だけが
「隣が単なる sp3 アルキル炭素なら非酸性」という例外を持つ。

## 原因

`normalize.rs::is_protonatable` → `protonation_neighbor_ok` は「隣が炭素
なら (元素を問わず) 常に許可」という無条件のショートカットを持っていた。
カルボキシラート/フェノラート/エノラートの隣接炭素がたまたま酸性化要因
(カルボニル・芳香族) を持っていたため、これまでの corpus 例ではこの
ショートカットが常に正解と一致してきたが、アルコキシド (隣接炭素が単なる
sp3 アルキル) には過剰適用だった。

同じ判定基準 (芳香族 OR 二重結合を持つ炭素) は既に `neutralize` 内の
`is_acidic_oh` (第 2 パス、中性の酸性 O-H を脱プロトンする側) で使われて
いたもの — 今回は同じ考え方を第 1 パスの O⁻ プロトン化可否判定にも
適用した。

## 修正

`protonation_neighbor_ok` に `owner_is_o: bool` を追加し、隣が炭素の場合の
判定を「`owner_is_o` なら芳香族または二重結合を持つ炭素のときだけ許可、
そうでなければ (S/Se/Te のとき) 従来どおり常に許可」に変更。新しい
`is_acidic_carbon` ヘルパーを追加 (`is_acidic_oh` の炭素判定部分と同じ
ロジック)。ペルオキシ酸 (I54) やスルホン酸・リン酸型 (S/P 中心を
`acidic_center` で判定する分岐) は変更なし。

**効果**: PubChem 99.43% → **99.44%** (18457/18563 → 18459/18563、+2)。
`inchi_gate` 変化なし (100%/100%)。`cargo test --release` 全体 (235 ユニット
テスト + 全統合スイート、`rdkit_compat` 含む) 変化なし・パス。既存の単純
アルコキシドを想定したユニットテストは存在しなかった (壊れる古いテストは
無し)。

**未解決の観察**: `[NH3+]O` (ヒドロキシルアンモニウム、`NH3+-OH`) は
`inchi-1` で確認すると脱プロトンされず `/q+1` のまま残る一方、
`C[NH2+]O` (N-メチル置換の同型化合物) は通常どおり脱プロトンされ
`/p+1` になる — 一見矛盾する挙動で、`is_deprotonatable` 側 (プロトン付き
陽イオンの脱プロトン化可否) にも何らかの隣接元素依存の例外があることを
示唆するが、この 1 例だけでは規則を確定できず、今回は着手しなかった
(該当する PubChem 内の実例も未確認)。次にこの方向を調べるなら、まず
`inchi-1` で第二級・第三級のヒドロキシルアンモニウム類縁体を複数構築して
「N に炭素が付いているかどうか」以外の説明変数がないか確認してから
着手すること。

**次に着手するなら**: `h` (over/under-merge の残りサブパターン、33 件)・
`t` (自己同型探索が必要、I52、22 件)・`b` (mobile-charge-without-H 検出、
I52、9 件)・`mt`/`bh` (7/6 件、`h`/`b`/`t` の複合バケツなので個別に解決
すれば自然に減る可能性が高い)・`Fhpq` の残り (6 件、水和物型・双性イオン
の成分内中和漏れ・カルバニオンなど複数のサブパターンが混在、上記の
ヒドロキシルアンモニウムの謎もここに含まれる可能性がある) が引き続き
優先度高い。

# 追記: I59 (2026-08-16) — スルホニル S(=O)(=O) の二重結合がブロッサム
# マッチングを非対称に壊し、環外酸素を可動 H 群に誤併合していた

`h` バケツ (33 件) を再調査していて、ベンゾチアジアジン 1,1-ジオキシド
(サイアザイド系利尿薬の母核) にグアニジン型の可動 H 群が付く分子で、
`inchi-1` に対する再現最小例 `NC1=NS(=O)(=O)C2=CC=CC=C2N1` を作って
確認した:

```text
want …h1-4H,(H3,8,9,10)      ← グアニジン中心 (C7) 周りの N8/N9/N10 だけ
got  …h1-4H,(H3,8,9,10,12)   ← スルホニル酸素 (O12) まで誤って群に併合
```

## 原因

`mobile_groups` のブロッサム探索用グラフ構築で、各原子の実在二重結合を
`matched[]` (交互パスがそのまま辿ってよい「マッチ辺」) に登録している。
このループは `g.bonds` を素直に 1 本ずつ処理するだけで、**1 つの原子が
実在二重結合を 2 本同時に持つ**場合 (スルホニル S(=O)(=O) が典型) を
想定していない: `S3=O4` を処理した時点で `matched[S3]=O4` と設定した後、
同じループ内で `S3=O5` を処理すると `matched[S3]=O5` に**上書き**される
一方、`matched[O4]=S3` は他に上書きする結合が無いためそのまま残る。結果、
O4 は「S3 とマッチ済み」と主張するのに S3 側は O5 しか指していない、
という非対称な状態になる。

ブロッサム法の交互パス探索は「非マッチ辺は探索グラフを辿る、マッチ辺は
`matched[]` を直接使って飛ぶ」ため、S3 がベンゾ環側 (芳香環-ヘテロ原子の
辺として許可されている `S3-C6`) から到達可能になった時点で、`matched[S3]`
経由で O5 まで無条件にジャンプできてしまう — スルホニル S=O は本来
Kekule 構造の選択の余地が一切ない (常に固定の二重結合) にもかかわらず、
通常の芳香環的な「二重結合の付け替え」と同じものとして扱われていた。

## 修正

`mobile_groups` に `real_double_count(atom)` (その原子が持つ実在二重結合の
本数) を追加し、`matched[]` への登録条件に「結合の両端がそれぞれ実在
二重結合をちょうど 1 本しか持たない」を追加した。スルホニル・スルホン酸・
硫酸エステル等、1 原子が実在二重結合を 2 本以上同時に持つ結合は
`matched[]` に一切登録されなくなり (どちらの端点も「マッチ辺」として
交互パスに使われない)、ブロッサム探索がそこで正しく止まる。

**効果**: PubChem 99.44% → **99.45%** (18459/18563 → 18461/18563、+2)。
`inchi_gate` は変化なし (100%/100%)。`cargo test --release` (235 ユニット
テスト + `rdkit_compat` 含む全統合テスト) も退行なし。

ヒットしたのは狭いスコープ (該当構造がコーパス内にたまたま 2 分子だけ)
だったが、`matched[]` の非対称上書きという不変条件バグ自体は硫酸エステル・
リン酸トリエステルなど他の過原子価中心にも共通しうる構造的な穴であり、
I53/I55/I56 と同系統の「機構バグ」に分類される。

**次に着手するなら**: `h` の残り (over/under-merge、31 件程度)・`t` (I52、
自己同型探索が必要)・`b` (I52、mobile-charge-without-H 検出)・`Fhpq` の
残り (水和物型・双性イオンの成分内中和漏れ・カルバニオン・ヒドロキシル
アンモニウムの謎) が引き続き優先度高い。

# 追記: I60 (2026-08-16) — 未定義立体源性二重結合 `?` の環除外が広すぎた

`b` バケツ (9 件) を再調査。`inchi_gate`/`pubchem_gate` の想定と異なり、
実 InChI は**大員環 (マクロ環) の C=C にも E/Z を認識する**ことが分かった。

```text
smi  CC1=C(C=CC(=C)C=C(C=CC1=C)F)/C=C\C=C   (10 員環ポリエン)
got  …/b7-6-                                 ← 環外の1本だけ
want …/b7-6-,10-8?,11-9?,16-15?,17-12?       ← 環内二重結合4本も未定義扱いで併記
```

`molrs` のデバッグ出力で確認: この分子の `ring_atom_sets` は
`[[1,11,10,9,8,7,5,4,3,2]]` (10 員環) 一本で、6 本ある環内二重結合の
うち 4 本が実 InChI では立体源性として `?` 付きで出ている。

## 原因

`stereo.rs::double_bond_layer` の未定義 `?` 判定ブロックに
`// 環内二重結合は対象外 (InChI は小環の E/Z を持たない)` というコメント
付きの**環サイズ不問の**除外があった。これは I29 系列のどこかで小環
(シクロヘキセン等、幾何学的に trans を取れない環) の誤検出を防ぐために
入れられたものだが、「小環」を「環内全部」に過剰に一般化してしまって
いた。実 InChI は 8 員環以上のマクロ環では真に E/Z 異性が幾何学的に
可能なため、通常の非環二重結合と同様に (未定義なら `?` で) 認識する。

なお別の分子 (橋かけ二環系、ノルボルネン型) では実 InChI が `?` を出す
のに CIP ランクの同点 (`ranks[a]==ranks[b]`) で `end_stereogenic` が
弾いてしまうケースも見つけたが、これは今回とは別の原因 (CIP 判定その
ものが橋頭型の縮環系で同点を誤って出す、I52 の `/t` 自己同型問題と同系統
の根深い問題) と判断し、深追いせず今回のスコープ外とした。

## 修正

環除外の条件を「両原子を含む最小の環が 8 員未満のときだけ対象外」に
縮小した (`g.ring_atom_sets` から両原子を含む環を全て集め、その中の
最小サイズが 8 未満なら従来通りスキップ、それ以外は通常の
`end_stereogenic` 判定に委ねる)。定義済み二重結合の側 (最初のループ) は
元々環除外を持っていなかったので、今回で未定義側の挙動も揃った形になる。

**効果**: PubChem 99.45% → **99.46%** (18461/18563 → 18463/18563、+2)。
`inchi_gate` 変化なし (100%/100%)。`cargo test --release` (235 ユニット
テスト + `rdkit_compat` 含む全統合テスト) 退行なし。

**次に着手するなら**: `h` の残り (over/under-merge、31 件程度)・`t`/`b`
の CIP 同点問題 (I52・今回で 2 例確認、橋かけ二環系・縮環系で
`cip_ranks` が本来異なるべき原子を同点にしてしまう、根本修正には
自己同型を考慮した tie-break が必要で影響範囲が広い)・`Fhpq` の残り
(水和物型・双性イオンの成分内中和漏れ・カルバニオン) が引き続き優先度
高い。

# 追記: I61 (2026-08-16) — ヒドロキシルアミニウム型 `[NH3+]O` は `/p` を
使わず `/q` のまま残す

I58 が置き土産にした未解決リード「`[NH3+]O` は `inchi-1` で `/q+1` の
まま残るが、`C[NH2+]O` は普通に脱プロトンする — 単一例だけでは一般化
できない」を、まず `inchi-1` で複数の構成例を突き合わせて検証した:

```text
smi  [NH3+]O    (N の重原子隣接は O だけ)   → H4NO/q+1        (/p 不使用)
smi  [NH3+]OC   (同上、O の先に C)          → CH6NO/q+1       (/p 不使用)
smi  C[NH2+]O   (N が C と O の両方に隣接)  → CH5NO/…/p+1     (通常どおり脱プロトン)
smi  [NH3+]N    (ヒドラジニウム、N-N)       → H4N2/…/p+1      (通常どおり脱プロトン)
smi  [NH4+]     (対イオンなし単体)          → H3N/p+1         (通常どおり脱プロトン)
```

パターンが明確になった: **N の重原子隣接が O ただ 1 個だけ**のとき
(`NH3+-O-R` 型、N が O 以外の重原子を持たない) だけ脱プロトンしない。
N が O に加えて C を持てば (`C[NH2+]O`)、あるいは隣接が O でなく N
(ヒドラジニウム) なら、通常どおり `/p` で脱プロトンする。

## 原因

これは `normalize.rs::is_protonatable` が O⁻ 側の判定で既に持っていた
「隣が N (ヒドロキシルアミン型 N-O 結合) ならプロトン化しない」という
境界の**鏡像**が、N が陽電荷を持つ側の脱プロトン判定
(`is_deprotonatable`) には実装されていなかった、というだけの話だった。
`is_deprotonatable` は N/O/S/Se/Te なら無条件に脱プロトン対象にしており、
`[NH3+]O` の N もこれに該当してしまい `H3NO/p+1` に変換していた。

さらに、`is_deprotonatable` 側だけ直しても `neutralize` の第 2 パス
(残り予算があれば酸性 O-H から借りる) が横から同じ O-H を掴んでしまう
ことが判明した — `is_acidic_oh` は「隣が炭素でなければ酸性」という
条件だったため、隣が **N (しかもその N 自身が陽電荷を持つ)** でも通って
しまい、結局 `H3NO/p+1` に戻ってしまった。ヒドロキサム酸
`C(=O)N-O-H` (中性の N を介して**別の**四級 N+ から借りる、既存の正しい
挙動) と、`[NH3+]O` (O-H の隣の N が**まさにその借りたい相手自身**) は
見た目は同じ「隣が N」でも意味が違う — 後者は自己参照になってしまう。

## 修正

1. `is_deprotonatable` を `sym: &str` から `(g, atom)` を取るように変更し、
   N の重原子隣接が O 1 個だけのときは `false` を返すようにした。
2. `is_acidic_oh` の「隣が炭素でなければ酸性」条件に
   `!(a.symbol == "N" && a.formal_charge > 0)` を追加し、O-H の隣が
   **それ自身陽電荷を持つ N** のときは対象外にした (ヒドロキサム酸の
   ような「中性 N 経由で別の陽イオンへ貸す」パターンは `formal_charge`
   がゼロなので影響を受けない)。

**効果**: PubChem 99.46% → **99.47%** (18463/18563 → 18464/18563、+1)。
`inchi_gate` 変化なし (100%/100%)。`cargo test --release` (236 ユニット
テスト + `rdkit_compat` 含む全統合テスト) 退行なし。新規ユニットテスト
`normalize::tests::hydroxylammonium_stays_charged` を追加。

**教訓**: 「N-O 結合は正規化の境界」という既存ルールが片方向
(O⁻ をプロトン化しない) にしか実装されておらず、鏡像方向 (N+ を脱プロト
ンしない) が抜けていた。既存の非対称ルールを見つけたら、逆方向にも
同じ境界が適用されるべきでないか確認する価値がある。

**次に着手するなら**: `h` の残り (over/under-merge、31 件程度、
トリアゾリウム/テトラゾリウム系の可動 H 群の分岐が特に手強い — 同じ
陽イオン環でも「隣接原子を可動 H 群に含めるべき」場合と「群自体を無効化
すべき」場合が両方存在し、I47 で一度回帰した領域)・`t`/`b` の CIP 同点
問題 (I52・I60 で計 2 例確認、自己同型を考慮した tie-break が必要)・
`Fhpq` の残り (タングステン酸水和物・双性イオンの成分内中和漏れ・
カルバニオン) が引き続き優先度高い。

# 追記: I62 (2026-08-17) — 孤立 `[H+]` が仮想プロトンとして二重計上されていた

`Fchpq` (SMILES に明示的な `[H+]` 成分を含む塩、4 件) を再調査。共通の形:

```text
smi  [H+].COC1=CC2=...(アミン骨格).[Cl-]
got  InChI=1S/C20H27N3OS.ClH.H/c...;;/h...;1H;/q;;+1/p-1
want InChI=1S/C20H27N3OS.ClH/c...;/h...;1H
```

`[Cl-]` 単体は `is_protonatable` (I31 で確認済み、ハロゲン化物は孤立なら
常にプロトン化対象) 経由で正しく `ClH` になる。しかし SMILES 上の
明示的な `[H+]` 原子自体は誰にも消費されず、そのまま独立した `.H` 成分
として残ってしまい、`/q+1/p-1` が余計に付いていた。

## 原因

`neutralize` の電荷正規化は「各成分の正味電荷を中性化する」ロジックで、
`[Cl-]` 成分 (Cl 原子だけの 1 原子成分、budget=-1) は明示的な `[H+]` の
存在とは無関係に、常に仮想的な `n_add` (プロトン追加数) としてカウント
され `/p` に現れる。一方 `[H+]` はそもそも重原子 (`n_heavy`) のループ
対象外 (H 原子なので) であり、誰からも中性化されないまま「孤立 H」保持
ループ (I20、金属水素化物切断由来の孤立 H を独立成分として残す仕組み)
に拾われ、素通りで別成分になっていた。

`inchi-1` で最小例を確認: `[H+].[Cl-]` → `ClH/h1H` (`/q`・`/p` 皆無)、
`[Na+].[Cl-]` (`[H+]` なし) → `ClH.Na/h1H;/q;+1/p-1` (`/p-1` が付く)。
同じ `Cl-` でも、**実体としての `[H+]` が入力に存在するかどうか**で
`/p` の有無が変わる — 実 InChI は「仮想的に足りないプロトンを `/p` で
帳尻合わせする」のと「実在するプロトン原子をそのまま酸性中心へ結合させて
中性分子として数える」を区別しており、後者は `/p` を消費しない。

## 修正

`n_add` (中性化のために仮想的に足したプロトン数) を数え終えた直後、
孤立 `[H+]` 原子 (どの重原子にも結合しない、電荷 +1 の H) を最大 `n_add`
個まで「消費済み」としてマークし、その数だけ `n_add` を減らしてから
`p = n_remove - n_add` を計算する。消費された `[H+]` は、末尾の孤立 H
保持ループ (`is_lone_h` ベース) から除外し、独立成分として再生成しない
— 結果として `final_h` (どの重原子に何個 H が付くか) は従来と同じ値の
まま、それが「仮想の `/p`」ではなく「実在の消費済み `[H+]` で賄われた」
ことになり、`.H` という余分な成分も消える。

早期リターンの最適化 (「変化なし」なら元のグラフをそのまま返す) が
`n_add==0 (消費後)` を見て誤発火しないよう、`consumed_lone_h_plus` が
空でないときはこの早期リターンをスキップするガードを追加した (消費前は
`n_add>0` で実際に `final_h` が変更されているため、素通りさせると
その変更が失われてしまう)。

**効果**: PubChem 99.47% → **99.49%** (18464/18563 → 18468/18563、+4)。
`inchi_gate` 変化なし (100%/100%)。`clippy --all-targets -- -D warnings`
クリーン。`cargo test --release` (236 ユニットテスト + `rdkit_compat`
含む全統合テスト) 退行なし。`inchi-1` で `[H+].[Cl-]`・`[H+].CC.[Cl-]`・
`[H+].CC(=O)[O-]`・`[Na+].[Cl-]`・`[H+].[Br-].[H+].[Cl-]` (2 個の孤立
`[H+]` が別々のハロゲン化物にそれぞれ吸収される多重ケース) を突き合わせ、
いずれも完全一致を確認済み。

## 調査したが未着手のリード (2 件、根が深いので今回は見送り)

### `bh` 4/6 件: アシ型ニトロ (nitronic acid) の可動 H 群が検出されない

`CC/C(=[N+](/O)\[O-])/CO` 系統: `C=[N+](OH)(O-)` の 2 個の N-O 単結合
酸素が実 InChI では `(H,x,y)` の可動 H 対として扱われ (`inchi-1` で
`CC(=[N+](O)[O-])C` → `.../h1-2H3,(H,5,6)` を確認)、そのため C=N の
`/b` 記述子も付かない。molrs は `seed_groups` が正しくこの星型 (中心 N、
2 個の O 端点、`donor_relay`) を候補として生成するところまでは確認した
が (デバッグ出力で確認)、`hub_allowed_endpoints` の厳密な実現可能性判定
(ブロッサム法によるフロー計算) がこの候補を **必ず** 棄却する — 原因は
モデルの構造的な前提: `Shift::Fewer` (「この端点は H を持てない」を
検証する側) は、端点がその容量を「別の実二重結合として使う」ことでしか
補えないと仮定しているが、この分子の中心炭素 (末端 =CH2) は C=N 以外に
二重結合の相手を一切持たないため、どちらの O から H を外しても中心の
炭素が孤立してしまい、必ず不可能判定になる。しかし実際の互変異性は
「N が二重結合の相手を変える」のではなく「2 個の O 間で H と電荷を
そのまま交換するだけ (結合次数の変更ゼロ)」という、現在のモデルが
表現できないパターン。`hub_allowed_endpoints`/`Shift` の根本的な拡張が
要る — 単体では 4 分子程度の効果なので、今回は見送り。

### `mt` 7 件: 定義済み立体中心 1 個だけパリティが反転し `/m` も連動して反転

7 件とも無関係な構造 (糖・大環状ラクタム・スルホニルプロパンアミド等)
だが同じ形の不一致: `/t` の複数の定義済み中心のうち **最後の 1〜2 個**
だけ molrs の生パリティが実 InChI と逆になっており、`/m` の基準
(`tetrahedral_layers` の `invert = defined[0].1 == '+'`) がその 1 個の
反転につられて 0⇔1 反転する。これは I52/I60 で見つかった「CIP ランクの
同点判定が真の自己同型を考慮していない」自動同型探索ギャップとは別種の
バグで、`is_undefined_tetra` (どの中心を `?` にするか) ではなく
`assign_cip_ranks`/`tetra_raw_parity` (定義済み中心の生パリティ計算)
そのものの CIP 優先順位比較に潜在バグがある可能性が高い。1 分子で
再現・特定するデバッグ出力までは行ったが、`assign_cip_ranks` の階層的
digraph 比較ロジックは複雑で、狭い当てずっぽうの修正は退行リスクが高い
と判断し、今回は見送った。次に着手するなら、まず `mt` バケツの中で
**最小の分子** (できれば立体中心 2〜3 個程度) を探し、`cip_ranks` の
出力を直接ダンプして、どの部分木の比較で優先順位が入れ替わっているかを
特定するところから始めるべき。

**次に着手するなら**: `h` の残り (over/under-merge、31 件程度)・上記の
未着手 2 件 (`bh` のアシ型ニトロ、`mt` の CIP パリティ反転)・`Fhpq` の
残り (タングステン酸水和物・双性イオンの成分内中和漏れ・カルバニオン)
が引き続き優先度高い。

# 追記: I63 (2026-08-18) — `neutralize` の H 再構築が `parser_to_graph` を
# 別の原子にすり替えていた (`/i` 層のバグ)

`i` バケツ (2 件、同位体層のみ不一致) を調べた。共通の形:

```text
smi  [2H]C([2H])([2H])[S+](=O)(CC1=CC=CC=C1)C([2H])([2H])[2H]
got  .../i1D3,6D,8D2
want .../i1D3,2D3
```

`want` は「CD3 基が 2 個、正準番号 1 と 2」。`got` は 6 個の D のうち 3 個
(canon=1 の分) しか正しく拾えておらず、残り 3 個が芳香環 CH (canon=6) と
ベンジル CH2 (canon=8) に誤って散らばっている — この 2 原子は本来 D を
持たない (通常の軽水素) はずの位置。

## 原因

`layers::isotope_layer` 自体 (`comp`/`g` に対して単体でテストすると常に
正しい) ではなく、`to_inchi` パイプラインが呼ぶ `normalize::neutralize`
の側にあった。`neutralize` は電荷正規化のたびに (何も変えない場合ですら
早期リターンできないケースがある限り) H ノードを**重原子の出現順で
まるごと作り直す**。これは実 InChI 側の SMILES 解析順序とは無関係な、
新しい通し番号を H に割り振り直す処理だが、再構築後のグラフ
(`ng.parser_to_graph`) には**古いグラフの `parser_to_graph` をそのまま
`clone()` しただけ**の値を入れていた。

明示同位体原子 (`[2H]` 等) の位置は「SMILES での出現順」で決まり、暗黙
H (AddHs で後から足された普通の軽水素) は `g.parsed`/`parser_to_graph`
に一切対応を持たない。この分子では旧グラフの並びは「重原子 (0-10) →
明示 D 6 個 (11-16、SMILES 出現順) → 暗黙 H 7 個 (17-23、重原子順)」
だったが、`neutralize` の再構築は「重原子 (0-10) → 各重原子ごとに
`final_h` 個の H を重原子順で機械的に追加」なので、新しい並びは元の
「明示原子が先頭側に固まっている」構造をまったく保存しない。結果、
クローンされた古い `parser_to_graph` の値 (例: 同位体原子は旧インデックス
14,15,16) が、新グラフでは**まったく別の原子** (再構築後にたまたま
14,15,16 番になった、無関係な軽水素) を指してしまっていた。

## 修正

1. `parser_to_graph` を `clone()` ではなく**明示的に付け替える**。H
   ノードの再構築を「同じ重原子に結合していた旧 H を、対応が分かる形で
   引き継ぐ」方式に変更 (`h_remap: HashMap<旧 idx, 新 idx>` を構築)。
   `final_h` が `cur_h` を超える (新規プロトン追加) 分だけ、対応なしの
   真に新しい H ノードを足す。
2. **副作用として見つかった別の不変条件**: `crate::stereo::build_ctx`
   は「`parser_to_graph` に対応を持つ (= 明示的に解析された) 原子は
   グラフ先頭から連続する範囲を占める」ことを前提にしている (同位体を
   持ちうる原子だけを CIP ランクのノードとして数え、残りの暗黙 H は
   `n_h` スカラーに畳み込むため、`n = parser_to_graph` の `Some` 件数を
   その範囲の境界として使う)。単純に「旧 H を同じ位置に引き継ぐ」だけ
   だと、明示原子 (同位体 H・孤立イオン H `[H+]`/`[H-]`) が暗黙 H の
   間に散らばってこの連続性が壊れ、`build_ctx` の `graph_to_parser[gi]`
   書き込みが配列外参照でパニックする (`pubchem_gate` 実行で 8 分子が
   即死することが分かった — `find_panic` という使い捨てのスキャン用
   example で特定)。対処として H 再構築を 3 段階に分離した:
   - パス 1: 旧グラフで重原子に結合していた「明示的に解析された H」
     (同位体標識の有無を問わない) を、元の解析順のまま重原子の直後へ
     詰め直す。
   - パス 1.5 (既存の孤立 H 再付加ループ、位置を移動): 金属水素化物
     切断由来や `[H+]`/`[H-]` のような孤立 H も「明示的に解析された
     原子」の一種なので、暗黙 H を埋めるパス 2 より**前**に置く。
   - パス 2: 残り (真に新規のプロトン、または元から暗黙だった H) を
     重原子順に追加— 常に上記より後ろに来るので、明示原子の連続範囲を
     壊さない。
3. 新しい `parser_to_graph` は、重原子はインデックス不変のためそのまま、
   H は `h_remap` にあれば新番号、無ければ (脱プロトン等で消えた原子)
   `None` にする。

**効果**: PubChem 99.49% → **99.50%** (18468/18563 → 18470/18563、+11)。
最初は同位体分子 2 件の修正のつもりだったが、上記のパニック 8 件
(`[H+]`/`[H-]` を含む分子が `pubchem_gate` 実行そのものをクラッシュさせて
いた — 従来 `try_inchi` は `catch_unwind` していないので、mismatch として
数えられる代わりにテストプロセス全体が落ちていた) も一緒に解消されたため、
直接の同位体修正 (2 件) 以上の純増になった。`inchi_gate` 変化なし
(100%/100%)。`clippy --all-targets -- -D warnings` クリーン。
`cargo test --release` (236 ユニットテスト + `rdkit_compat` 含む全統合
テスト) 退行なし。

**教訓**: `parser_to_graph`/`g.parsed` のような「元の解析結果への
逆参照」を持つフィールドは、グラフを再構築する処理 (`neutralize` に
限らず) すべてで**明示的に付け替える**必要がある — `clone()` で
済ませられるのは atoms/bonds のトポロジーが完全不変のときだけ。加えて、
この手の逆参照が「先頭が連続する」といった**別モジュール側の暗黙の
不変条件**に使われていることがあるので、素朴な remap だけでなく
生成順序そのものを見直す必要がある場面もある (今回がまさにそれ)。

## 副次調査: `mt`・`b`・`ms` バケツの精密診断 (未修正、次のリード)

今回のメイン修正とは別に、残る主要バケツをデバッグ出力で深掘りした。
いずれも「狭い当てずっぽうの修正」では退行リスクが高いと判断し、
コードは変更していない。

### `mt` (7 件): 生パリティ自体は正しい。バグは `/m` (どちらを反転
### 基準にするか) の側

デバッグ出力で `tetra_raw_parity` を直接ダンプして確認: 例えば
`C[C@@H]1CN(C[C@@H](O1)C)C(=O)[C@H](C)S(=O)(=O)CC2CCCCC2` の3中心
(canon 7,8,9) について、**各中心の生パリティは実際には常に安定** —
canon=9 (アシル-スルホニル間の CH) の生パリティは、環側の 2 中心が
両方定義済みでも片方だけ定義済みでも、常に `raw='-'` (chiral_tag='S') で
不変だった (`inchi-1` でも同じ分子の縮小版で確認: canon=9 相当は常に
`9-` を示す)。

問題は現在の `/m` 決定ロジック
(`tetrahedral_layers`: `invert = defined[0].1 == '+'`、真なら**全中心の
生パリティを反転して表示**) が、実 InChI の実際の挙動と食い違うこと。
実測: 環側両方定義済みの分子で、実 InChI は canon=7,8 を反転表示するが
canon=9 は**反転せず**生のまま表示する (`want: t7-,8+,9-/m0`, molrs の
`got: t7-,8+,9+/m1` — 7,8 は一致、9 だけ違う)。つまり実 InChI の `/m`
正規化は「先頭の 1 中心の符号で全体を一括反転する」という単純な規則
ではなく、**分子全体とその鏡像それぞれを別々に正準化し、辞書順で小さい
方を選ぶ**(エナンチオマー比較) 方式になっているはず — molrs の
`defined[0].1=='+' なら全反転` は、この「本来の比較」を「先頭中心の符号
だけ見る」に単純化した近似であり、多中心分子では容易に食い違う。

`is_achiral` (メソ体判定の自己同型探索) と地続きの問題で、I52/I60 の
自動同型ギャップとも根が近いが、**別の症状** (今回は生パリティ自体は
壊れていない、`/m` の選び方だけが違う) なので区別して記録する。

**次に着手するなら**: `tetrahedral_layers` の `/m` 決定を、`invert`
フラグの単純規則ではなく「全中心を反転した場合としない場合の両方で
`/t` 文字列を生成し、何らかの正準比較で決める」方式に置き換える必要が
ありそう。ただし実 InChI が実際に何を比較しているか (単に `/t` 文字列の
辞書順か、もっと深い正準化か) はまだ未確認 — 着手するならまず
`ichi_bns.c`/立体関連の C ソースで `/m` 決定ロジックを直接確認すること。

### `b` (5/7 件): テトラゾリル-ビニル系で `/b` が消える。ただし単純な
### 「可動 H 群に共役していれば除外」という仮説は反例で棄却済み

`.../C=C/C4=NNN=N4` (ビニル-テトラゾール) を含む分子が 5 件、実 InChI は
この C=C に `/b` を一切付けない。仮説「可動 H 環に共役した二重結合は
除外すべき」を単純な構築例で検証したところ**反例で即座に棄却**:

```text
CC/C=C/C1=NNN=N1  →  実 InChI: .../h...,(H,6,7,8,9)/b4-3+
```

可動 H 環 (テトラゾール) が普通に存在してもこの単純な例では `/b` は
**ちゃんと付く**。つまり pubchem の 5 件で `/b` が消える原因は
「テトラゾールへの共役」ではなく、**分子の反対側にある巨大な複素環
(グアニジン/アミド型の可動 H 群、`(H2-,...)` のように 10 原子超に
またがる) の存在**と何らか関係しているはず — ただし正確な機構は未特定
(該当原子がその巨大な可動 H 群の直接メンバーなのか、共役で間接的に
影響されるのかも未確認)。**広げるなら必ず複数の反例で先に仮説を
検証すること** — 今回のように 1 つの反例で簡単に崩れる。

**次に着手するなら**: 実際の pubchem 分子 1 つを最小化しながら
(側鎖を落としていって `/b` が消える/消えないの境界を探る) 原因を特定
すること。

### `ms` (4 件中 2 件): 融合二環系で `is_achiral` が偽陽性

`C1CC[C@@H]2[C@H](C1)N/C(=C/C(=O)C3=CC=CC=C3)/C(=C/C(=O)C4=CC=CC=C4)/N2`
とそのエナンチオマー (@ タグを入れ替えたもの) の両方で、molrs は `/m`・
`/s` を**まったく出さない** (メソ体と誤判定)。実 InChI は一方を `/m1`、
他方を `/m0` として区別する (本来はキラル)。2 つの環縮合中心を繋ぐ
5 員環の両アーム (`N/C(=CHC(=O)Ph)/`) が構成的に同一なため、`is_achiral`
の自己同型探索がこの 2 中心を入れ替える (偽の) 対称性を見つけてしまって
いる可能性が高い — 実際にはシクロヘキサン環を含む縮環系全体を正しく
写像する真の automorphism にはなっていないはずで、探索側の検証が
不十分 (辺の対応まで厳密にチェックできていない) と疑われる。I52 の
「未定義四面体中心の判定に自己同型検索が必要」ギャップとは逆方向の
症状 (今回は偽陽性のキラリティ抑制)。

残り `ms` 2 件は逆に、未定義中心 (`?`) が複数ある対称性の高い環 (ペンタ
エステル化シクロヘキサン等) で実 InChI が `/m` を**省略**するのに molrs
が `/m0` を出してしまうケース — `?` の中心の実際の配置次第でメソにも
キラルにもなりうるため実 InChI が判断を保留していると見られ、
`is_achiral` に未定義中心を「自由変数」として考慮する拡張が必要になる
(さらに難度が高い)。

**次に着手するなら**: 融合二環の偽陽性 2 件から着手するのが narrower。
`is_achiral` のバックトラッキング探索が候補の automorphism を最終的に
どう検証しているか (辺集合全体の対応を確認しているか) をまず読むこと。

# 追記: I64 (2026-08-18) — 裸のプロトン `[H+]` を `/p` に繰り込む (`Fpq` バケツ)

I62 は「`[H+]` が他の重原子成分に消費できる (`n_add` がある) 場合」を
修正したが、`[H+]` **単独**が入力全体という極端なケースが残っていた:

```text
smi  [H+]
got  InChI=1S/H/q+1
want InChI=1S/p+1          ← 式そのものが無い

smi  [H+].[H-]
inchi-1 実測: InChI=1S/H/q-1/p+1   ← H- 側だけが「物質」として残り、
                                     余った H+ は /p に回る
```

## 原因

`disconnect::hydrogen_component_sizes`/`hydrogen_component_charges` は
「重原子に結合していない孤立 H」をすべて同列に扱い、電荷があれば無条件で
`/q` 層に出していた。しかし実 InChI の規約では、**明示的に解析された
負電荷またはニュートラルな孤立 H (`[H-]`・水素分子) だけが「物質」**として
式・`/q` に現れ、**正電荷の孤立 H (`[H+]`、= 裸のプロトン) は物質として
数えず常に `/p` に繰り込まれる** — 消費先の重原子成分があってもなくても、
また同じ入力に `[H-]` が同時にあってもなくても同じ規則。`[H+]` 単独では
「物質」がゼロになるので式そのものが空になり `InChI=1S/p+1` という
`/` すら出ない形になる。

## 修正

`hydrogen_components` という共通の走査ヘルパを新設し、(サイズ, 電荷) の
組で各孤立 H 連結成分を返すようにした上で:
- `hydrogen_component_sizes`/`hydrogen_component_charges`: サイズ 1 かつ
  電荷が正の成分 (裸のプロトン) を除外。
- 新設 `bare_proton_charge`: 除外した分の電荷合計を返す。

`to_inchi` (`mod.rs`) で `neutralize` が返す `p` にこの値を加算。式が
完全に空になる場合 (`formula.is_empty()`) は `InChI=1S/...` の `/` 区切り
自体を出さないよう `format!` 分岐を追加。

**効果**: PubChem 99.50% → **99.51%** (18470/18563 → 18472/18563、+2)。
`inchi_gate` 変化なし (7452/7452・7244/7244 とも 100%)。
`clippy --all-targets -- -D warnings` クリーン。`cargo test --release`
(254 テスト、全統合スイート含む) 退行なし。`[H+]`・`[H+].[H+]`・`[H-]`・
`[H+].[H-]`・`[H+].[H+].[H-]`・`[H+].[Cl-]`・`CC(=O)O.[H+]` を
`inchi-1` の実測値と突き合わせて確認済み。

## 副次調査: `mt`・`b`・`ms` の続き (未着手、次のリード)

I63 が精密診断した 3 バケツを深追いした:

- **`mt`/`m` (8 件)**: `/m` の決定ロジック (`invert = defined[0].1=='+'`
  で全中心を一括反転) が実 InChI の実際の挙動と食い違う、という I63 の
  診断を、多成分の `/m` 専用ミスマッチ (`m` バケツ、`(H,...)`
  形式は無関係) でも再確認した。実測: 4 成分の分子で `got m0011` /
  `want m0101` — 成分ごとの `/m` 判定自体も一括規則では説明できない。
  ソース側の該当ロジック (`ichi_bns.c` か立体関連ファイル) をまだ特定
  できておらず、狭い当てずっぽうは避けて見送り。
- **`b` (7 件)**: 5 件がテトラゾリル-ビニル系 (`/C=C/C4=NNN=N4` が
  巨大な可動 H 環 (イミダゾリノン-チアゾール) に共役)。`tgroup[a] ||
  tgroup[c]` (`double_bond_layer` の除外条件) は `mobile_groups` が返す
  **末端原子だけ**を見ており、末端どうしを繋ぐ「中継」原子 (この
  ビニル炭素 2 個) はメンバーに入らない。実 InChI はおそらく BNS の
  交互パス上にある結合すべてを立体対象外にしており、末端メンバーシップ
  だけでは説明できない。ここを広げる試みは過去 (I47 前後) に 2 回退行
  しており、今回もコードは変更していない。残り 2 件のうち 1 件は I52 が
  最初に見つけた「可動電荷のみ・H なしの push-pull 系」の実例そのもの
  (`CN(C)/C=C/C=[N+](C)C.F[P-]...`) と確認できた (既知の未着手ギャップ)。
- **`ms` (4 件、うち 2 件が縮環偽陽性)**: `is_achiral` の `check` 閉包は
  四面体中心 (`centers` 引数) しか検証しておらず、**二重結合の E/Z
  立体中心は一切考慮していない**。該当分子
  (`.../N/C(=C/C(=O)Ph)/C(=C/C(=O)Ph)/N2` 型、2 本のエナミノン二重結合を
  持つ縮環系) では、見つかった置換 π が 2 つの環腕を入れ替えることで
  四面体中心側だけは「メソ」条件を満たしてしまうが、その π が両アームの
  E/Z 二重結合の配置と整合するかは検証されていない可能性が高い —
  ただし E/Z はミラー反転で反転しない (幾何学的には cis/trans は保たれる
  はず) ので、四面体中心と同じ XOR ロジックをそのまま流用はできず、
  「π で番号が入れ替わっても同じ E/Z 判定になるか」という別の一致条件が
  必要になる。この符号関係を誤ると退行しやすいので、今回はコードを
  変更せず仮説の記録のみに留めた。

**次に着手するなら**: `h` の残り (31 件、over/under-merge・巨大複素環の
可動電荷群・リン酸二重成分の併合など複数の異なる原因が混在) が引き続き
最大バケツ。上記 3 件はいずれも実 InChI 側のソース (`ichi_bns.c` の
BNS フローネットワーク、または立体関連ファイルの `/m` 決定ロジック) を
先に読んでからでないと安全に手を出しにくい。

# 追記: I65 (2026-08-18) — 成分並び順の主キーから「合計 H 数」を除去
(`Fch` バケツ、`ichimake.c::CompINChI2` の比較段の順序を修正)

`Fch` (F・c・h 3 層が同時に違う) を再調査。同じ骨格 (縮環系/複素環) の
飽和体・不飽和体が両方含まれる多成分混合物 (脂肪酸誘導体のスクリーニング
セット等) で、実 InChI は同じ骨格どうしを**隣接**させて並べる (例:
`...C9H16O2.C9H14O2.C9H16O2.C9H14O2.C9H16O2.C9H14O2...` と交互) のに対し
molrs は式 (H 込み) ごとにひとまとめにして `3C9H16O2.3C9H14O2` のように
出していた。

## 原因

`ichimake.c::CompINChI2` (1711〜1798 行) の比較チェーンを確認したところ、
実際の順序は次の通り:

1. `CompareHillFormulasNoH` (H を除いた Hill 式)
2. 原子数 (H 抜き)・各元素の原子番号配列 (① が一致すれば通常ここも一致)
3. **`nConnTable` (接続表、レンダリング前の整数配列、I48/I56 で実装済み)**
4. **合計 H 数 (`num_H2 - num_H1`、多い方が先)**
5. 原子ごとの個別 H 数配列

molrs の `component_sort_key` (`formula.rs`) は、②までの主キーに加えて
**合計 H 数までも同じタプルに混ぜて**いた
(`(hill_no_h_key, Reverse(atoms.len()), Reverse(h), formula)`)。
`build_components` (`layers.rs`) 側で③の conn table 比較を
`.then_with()` として正しく後段に置いていても、その前の主キー比較の
時点で④ (合計 H 数) がすでに決着してしまうため、実質的に③より④が
**先に**効いてしまっていた。骨格 (conn table) が同一で飽和度だけ違う
成分どうしは、実 InChI では③で一致 → ④で飽和体が先、という順に
隣接するはずが、molrs では④が先に効いて全体が「H 数ごとの塊」に
分離されてしまっていた。

さらに `formula::formula_layer` は `layers::build_components` とは
**別に**独自に `connected_components` + 主キーのみのソートを行っており
(conn table によるタイブレークを経由しない)、F 層の成分順序が c/h 層の
真の順序とそもそも同期していない、という二重のバグもあった。

## 修正

1. `component_sort_key` から `Reverse(h)` (合計 H 数) と末尾の `formula`
   文字列を除去し、`(hill_no_h_key, Reverse(atoms.len()))` の 2 要素だけ
   に縮小 (`ichimake.c` の①②相当)。
2. `layers::build_components` の `.then_with()` チェーンに、conn table
   比較の**後**の段として合計 H 数比較を追加
   (`.then_with(|| total_h(&b.inv).cmp(&total_h(&a.inv)))`、多い方が先)。
3. `formula::formula_layer` は独自のソートをやめ、`layers::build_components`
   が返す並びをそのまま再利用するよう変更 (F/c/h が同じ成分順序を共有する
   ことを保証)。

**効果**: PubChem 99.51% → **99.54%** (18472/18563 → 18478/18563、+6)。
`Fch`・`Fbch`・`Fchmt` バケツが完全に解消 (4+1+1 → 0)。`inchi_gate` 変化
なし (100%/100%、`formula_layer_matches_rdkit` の既存の金属水素化物系
ミスマッチも変化なし・無関係)。`clippy --all-targets -- -D warnings`
クリーン。`cargo test --release` (236 ユニットテスト + `rdkit_compat`
含む全統合テスト) 退行なし。構築例 (同一骨格の飽和/不飽和カルボン酸
3 対の混合物) を `inchi-1` の実測値と完全一致することを確認済み。

**教訓**: 複数フィールドを 1 つの比較キー (タプル) にまとめる実装は、
各フィールドの「本来の比較順序」を暗黙に決めてしまう — 後から
`.then_with()` で別の段を追加しても、それより**前の**フィールドが
タプル内に紛れ込んでいると効かない。実 InChI の比較チェーンを移植する
ときは、tie-break の各段を独立した `.then_with()` として明示的に並べ、
どのフィールドがタプルの「主キー」に属すべきかをソース (`CompINChI2`
の行番号) で都度確認すること。

**次に着手するなら**: `h` の残り (31 件、over/under-merge・巨大複素環の
可動電荷群・リン酸二重成分の併合など複数の異なる原因が混在) が引き続き
最大バケツ。`mt`/`b`(テトラゾリル)/`ms` の 3 件 (I63/I64 で精密診断済み、
未修正) も依然として次点のリード — いずれも `ichi_bns.c` や立体関連の
C ソースを先に読む必要がある。

# 追記: I66 (2026-08-18) — ニトロ基/N-オキシドの N+ を種の中心にした
ときに無関係な N-H 端点を巻き込むバグ (`h` バケツ)

`h` バケツを再調査。ニトログアニジン型 (グアニジン/アミジンの末端 N が
`-N[N+](=O)[O-]` のようにニトロ基へ単結合で繋がる) や芳香族 N-オキシド
隣接の分子で、molrs がニトロ基/N-オキシドの酸素までグアニジン側の可動
H 群に誤って併合していた:

```text
smi  NC(=N)N[N+](=O)[O-]                (ニトログアニジン)
want (H4,2,3,4)                          ← グアニジンの 3N だけが 1 群
got  (H4,2,3,4,6,7)                      ← ニトロ基の O2 個まで誤併合
```

## 原因

`seed_groups` は中心原子ごとに星型パターン (受容体端点・供与体端点) を
探すが、中心の種別チェックは「中心が C か N」であることしか見ておらず、
**正電荷 N がすでに固定された電荷分離構造 (ニトロ基・N-オキシド) の
一部である**場合を除外していなかった。ニトロ基の N (`[N+](=O)[O-]`) 自身
が中心として星型探索の対象になり、`=O` を受容体端点、`-O-` (負電荷) を
供与体端点として拾うところまでは硝酸のような正当なケース (後述) と同じ
だが、**単結合で繋がる別の N (グアニジン側の N-H)** まで
「供与体端点」として拾ってしまっていた — 中心が N のとき、通常の
N 端点除外条件 (`!center_is_c_or_n(center)`) が「中心が C でも N でも
ない場合」しか弾かないため、中心自身が N の場合は素通りしてしまう。

この結果、ニトロ基中心の種 (`{O, O, N4}`) とグアニジン中心の種
(`{N1, N3, N4}`) が共有原子 `N4` を持ち、`mobile_groups` の union-find
(各種内で `grp[0]` に対して残りを union) が推移律でこの 2 つの無関係な
種を 1 つの群に合体させてしまう。

**単純に「正電荷 N を中心から除外」では直せない**: 硝酸
`O=[N+]([O-])O` は実 InChI でも 3 つの酸素が本当に 1 群 `(H,2,3,4)` の
互変異性 (これは N の O 隣接どうしの正当な共鳴) なので、これを壊すと
`inchi_gate` の `formula_layer_matches_rdkit` 相当のテストが退行した
(最初の実装でこれを検出、リバートして再考)。

## 修正

中心の除外ではなく、**端点候補の除外**に変更した: 中心が正電荷 N かつ
負電荷 O に隣接する (`nitro_like_center`) 場合に限り、その中心の隣接
原子を端点候補にする際に **N 型の隣接原子だけを弾く** (`sym == "N"` の
とき `continue`)。O どうしの互変異性 (硝酸) はそのまま許可しつつ、
ニトロ基/N-オキシドの N に単結合で繋がる無関係な N-H (硝酸には存在
しない配置) だけを遮断する。

**効果**: PubChem 99.54% → **99.55%** (18478/18563 → 18479/18563、+1)。
`h` バケツ 31→30。`inchi_gate` 変化なし (100%/100%)。`clippy
--all-targets -- -D warnings` クリーン。`cargo test --release` 全体
(236 ユニットテスト + `rdkit_compat` 含む全統合テスト) 退行なし。構築
例 3 種 (ニトログアニジン・N-メチル体・環状トリアゾール型) と硝酸を
`inchi-1` の実測値と完全一致することを確認済み。

**教訓**: 「不要なパターンを弾く」修正は中心 (ノード) 単位より端点
(エッジ) 単位のほうが安全な場合がある — 中心ごと除外すると、その中心
自身が持つ正当な内部互変異性 (硝酸の O 群) まで一緒に消えてしまう。
症状が「無関係な外部原子が巻き込まれる」ことなら、巻き込まれる側の
接続 (このケースでは中心→外部 N の辺) だけを狙って遮断する方が、
既存の正当な挙動を壊さずに済む。

**次に着手するなら**: `h` の残り (30 件) は依然 over/under-merge・巨大
複素環の可動電荷群・リン酸二重成分の併合など複数の異なる原因が混在した
最大バケツ。`mt`/`b`(テトラゾリル)/`ms` の 3 件 (I63/I64 で精密診断済み、
未修正) も次点のリード — いずれも `ichi_bns.c` や立体関連の C ソースを
先に読む必要がある。

# 追記: I67 (2026-08-18) — 孤立供与体ループがロック済みゼウィッタリオン O⁻ を拾っていた

I65/I66 で `h` の様々なサブパターンを潰したあと、`pubchem_diff --layer h`
を再度取り直して新鮮に triage した。目についたのは
`C1=C2C(=C(C(=C1Cl)Cl)[N+](=O)[O-])[N+](=CC(=O)N2)[O-]`
(ジクロロ・ニトロ置換の芳香族 N-オキシド融合ラクタム) で、`got` が
`(H,11,14,15)`、`want` が `(H,11,14)` — molrs はラクタムの N-H/C=O
(11,14) に加えて芳香族 N-オキシドの O⁻ (15) まで同じ可動 H 群に誤って
取り込んでいた。

最小化した再現例で `inchi-1` に確認:

```text
smi  O=C1C=[N+]([O-])c2ccccc2N1        (ピリジン N-オキシド融合ラクタム)
want …h1-5H,(H,9,11)                    ← ラクタムだけ、O-オキシド O⁻ は無関係
got  …h1-5H,(H,9,11,12)                 ← O-オキシド O⁻ (12) を誤併合
```

## 原因

`number.rs::mobile_groups` の「孤立供与体」ループ (どの中心の候補にも
ならなかった H/負電荷ヘテロ原子をブリッジ探索の起点に加える処理) は、
負電荷を持つヘテロ原子なら無条件に起点にしていた。ニトロ基・芳香族
N-オキシドの O⁻ は `is_locked_zwitterion_neg` (隣に陽電荷があり、共有結合
中性形の書き換えに過ぎない) で判定される「本当は可動でない」電荷分離
表現だが、この関数は `is_salt_endpoint` (群の電荷を `-` サフィックスとして
数えるかどうか) では既に除外に使われていたのに、孤立供与体ループの起点
選定には使われていなかった。

この O⁻ 自身は H を持たず、受容体二重結合も持たない (`[n+]([O-])` は
単結合) ため `seed_groups` の星型検出には一切乗らず、群に入る唯一の経路が
この孤立供与体ループだった。ループで起点になると、芳香環を経由した
ブロッサム探索の交互到達可能性判定がラクタムの N-H/C=O 群の既マッチ頂点
まで届いてしまい、化学的に無関係な 2 つの官能基が union-find で 1 群に
併合されていた。

硝酸 `O=[N+]([O-])O` のように N 中心自身が星型 (`seed_groups`) で 3 酸素
まとめて 1 群として捕捉されるケースは、その O⁻ が既に `members` に入って
いるため孤立供与体ループ自体を通らず、この修正の影響を受けない
(`inchi-1` で確認済み、`/h…,(H,2,3,4)` のまま変化なし)。

## 修正

孤立供与体ループの負電荷条件に `!is_locked_zwitterion_neg(g, i)` を追加
(H を持つ場合の条件は変更なし)。

**効果**: PubChem 18479/18563 (99.55%) → **18481/18563 (99.56%)** (+2)。
`inchi_gate` 変化なし (100%/100%)。硝酸・ニトログアニジン
(`NC(=N)N[N+](=O)[O-]`、I66 節参照) とも `inchi-1` の出力と一致することを
個別確認済み。`cargo test --release` (236 ユニットテスト + 全統合テスト
含む `rdkit_compat`) 全通過、`clippy --all-targets -- -D warnings` clean。

**次に着手するなら**: `h` の残り (約28 件) は依然 over/under-merge・巨大
複素環の可動電荷群・リン酸二重成分の併合など複数の異なる原因が混在した
最大バケツ。今回見つかった「孤立供与体ループが `is_locked_zwitterion_neg`
のような『ロック済み』端点を誤って拾う」という切り口は、同じループの
他の見落とし (例えばアジド型 N⁻ や他の電荷分離パターン) にも当てはまる
可能性があり、次点で確認する価値がある。`mt`/`b`(テトラゾリル)/`ms` の
3 件 (I63/I64 で精密診断済み、未修正) も引き続きリード — いずれも
`ichi_bns.c` や立体関連の C ソースを先に読む必要がある。

# 追記: I68 (2026-08-18) — アシニトロ酸の O,O 間 H 交換はブロッサム法では
# 原理的に表現できない専用パターンだった

I67 の「locked charge treated as mobile」系統をさらに探したが、`number.rs`
内に同種の見落としは見つからなかった。代わりに `h`/`bh` バケツを再ざっと
見て、`bh` の 6 件中 5 件が同一パターン (脂肪族アシニトロ酸) であることに
気づいた:

```text
smi  CC/C(=[N+](/O)\[O-])/CO
got  …h4,7-8H,2-3H2,1H3/b5-4+                    ← C=N+ を固定二重結合として
                                                    E/Z 判定、O は無関係な
                                                    ただの -OH・-O⁻
want …h6H,2-3H2,1H3,(H,7,8)                      ← C=N+ の E/Z 自体を出さず、
                                                    2 O 間の H を可動扱い
```

## 原因

これは通常のニトロ基 (`R-[N+](=O)[O-]`、中心 N が O と実二重結合) とは
形が違う: アシニトロ型は N+ の実二重結合の相手が **炭素** で、`seed_groups`
の受容体判定はヘテロ原子隣接だけを見るため、この二重結合自体はどの中心の
種にもならない。

一方で 2 つの末端 O (中性の `-OH` と負電荷の `-O-`) の間で H/負電荷の位置を
交換するのに、C=N+ の二重結合位置を変える必要は一切ない (どちらの解釈でも
N-O 結合は常に単結合のまま)。つまりこれは「二重結合の付け替えを伴う」
通常の互変異性ではなく、結合次数を一切変えない純粋な位置の交換 — molrs の
ブロッサム法ベースの可動 H 検出は「二重結合の位置がどこにあるか」という
辺のマッチングを前提にしたモデルなので、そもそもこのパターンを表現できる
枠組みがない (`inchi-1` で確認: `CCC(=[N+](O)[O-])CO` は
`(H,7,8)` になり、C=N+ 側の `/b` は一切出ない)。

## 修正

ブロッサム法の外側に、この形だけを直接検出する専用関数
`aci_nitro_hubs` を追加 (`number.rs`): N+・重原子次数 3・実二重結合の
相手がちょうど 1 個で炭素・残り 2 個が末端 (次数 1) の O、という形を検出し、
`mobile_groups` の最終結果へ (末端 O 2 個だけの) 群として直接追加する
(ブロッサム法の結果と衝突する原子があればスキップ)。

`/b` 側の除外は `tautomer_group_members` (= `/h` に印字される群メンバー)
とは別に `nitro_relay_hub_atoms` という新しい関数で N+ ハブ自身の集合を
返し、`double_bond_layer` の除外条件に追加した — ハブの N は末端 O とは
違い実 InChI の `/h` 印字には出てこない (`(H,7,8)` は O 2 個だけ) ので、
`/h` 側の群メンバーには含めず `/b` 除外だけに使う、という非対称な使い分けが
必要だった。

**効果**: PubChem 99.56% → **99.59%** (18481/18563 → 18486/18563、+5)。
リポジトリ内コーパス (`inchi_gate`) は変化なし (100%/100%)。3 つの構築例
(`CCC(=[N+](O)[O-])CO`・元の `bh` バケツの 2 例) すべて `inchi-1` の出力と
完全一致を確認。clippy クリーン、`cargo test --release` 全体 (236 ユニット
テスト + 全統合スイート、`rdkit_compat` 含む) パス。

**次に着手するなら**: `mt`/`b`(テトラゾリル)/`ms` の 3 件 (I63/I64 で精密
診断済み、未修正) が引き続き最有力のリード — いずれも `ichi_bns.c` や
立体関連の C ソースを先に読む必要がある。`h` バケツの残り (over/under-merge
系、charged `H-` 群、リン酸クロス成分マージ) も未着手のまま。今回見つけた
「ブロッサム法の辺ベースモデルでは原理的に表現できない、結合次数を変えない
純粋な位置交換」という切り口は、他の官能基 (例えばスルフィン酸・亜リン酸の
互変異性など) にも当てはまる可能性があり、次点で確認する価値がある。

# 追記: I69 (2026-08-19) — `is_achiral` が二重結合の E/Z を検証していなかった

I60/I64 で「`ms` バケツはメソ体誤判定 (`is_achiral` の対称性探索が四面体
中心しか見ていない) が原因」と診断されていたが未修正だった。今回その通りの
バグを特定し、狭く修正した。

## 原因

`ms` バケツ 4 件を精査すると、2 つの正反対のバグが混在していた:

```text
smi  C1CC[C@@H]2[C@H](C1)N/C(=C/C(=O)C3=CC=CC=C3)/C(=C/C(=O)C4=CC=CC=C4)/N2
got  …/b21-15-,22-16+/t19-,20+            ← /m・/s が一切出ない (キラルと誤判定できていない)
want …/b21-15-,22-16+/t19-,20+/m1/s1      ← 実際はキラル

smi  CC(C)CC(=O)O[C@@H]1[C@@H](C(...)OC(=O)...)...  (シクロヘキサン六置換エステル)
got  …/t26?,27-,28+,29-,30+,31?/m0/s1     ← キラルと判定 (誤り)
want …/t26?,27-,28+,29-,30+,31?           ← 実際はメソ体 (/m・/s なし)
```

前者 2 件 (縮環ビシクロ・ジアリリデンジケトン) は `is_achiral` の自己同型
探索が四面体中心のパリティ反転だけを検証し、**二重結合 E/Z を一切見ていない**
ことが原因: 探索が見つけた「四面体中心を正しく反転する」置換 π が、実際には
分子の E/Z 配置を壊してしまうものだったにもかかわらず、それをチェックせず
鏡像自己同型として受理していた。二重結合の E/Z は平面的な幾何なので鏡映で
反転しない (R/S とは違う) — つまり π が鏡像自己同型として正当であるためには、
四面体中心のパリティは反転する一方、二重結合の E/Z は π で写した先でも
**同じ**でなければならない。この後者の条件が実装されていなかった。

後者 2 件 (立体二重結合を持たないシクロヘキサン多置換体) はこの二重結合
チェックとは無関係の別バグ (`?` 混在時のメソ体探索が失敗している) で、
今回は未修正のまま次のリードとして残す。

## 修正

`double_bond_layer` の冒頭 (エントリ計算部分) を `defined_double_bonds`
として切り出し、`is_achiral` に第4引数として渡すようにした。`is_achiral`
内の `check` クロージャに、`bond_parity: HashMap<(hi,lo), char>` を使った
検証を追加: 既知の二重結合それぞれについて、π で写した先の原子対も
**同じパリティの**二重結合として登録されていなければ π を棄却する。

**効果**: PubChem 99.59% → **99.60%** (18486/18563 → 18488/18563、+2)。
`ms` バケツ 4→2 件。リポジトリ内コーパス (`inchi_gate`) は変化なし
(100%/100%)。clippy クリーン、`cargo test --release` 全体 (272 テスト)
パス。対象 2 分子とも `inchi-1` の出力と完全一致を確認。

## 調査メモ: 未着手のまま残る他のリード

- `t` バケツ (22 件) を再確認したところ、大半 (15 件以上) は「未定義
  四面体中心 `?` の見落とし」(I52 の自己同型探索ギャップと同根) だが、
  **明確に別種の 4 件**が見つかった: 隣接する糖鎖様の類似立体中心どうしで
  パリティの符号がそっくり入れ替わっている (例: `t7-,8-,9+,10-` であるべき
  ところ `t7-,8-,9-,10+` になる、`t28?,29-,30+,31-,32+,33?` であるべき
  ところ `31-,32+`)。これは大昔から `/t の「パリティ入れ替わり」4 件` として
  未着手のまま残っている項目そのもの。`molrs/src/stereo.rs::assign_cip_ranks`
  は RDKit 由来の Morgan 法 CIP ランク近似で、CIP の Rule 4/5 (隣接立体中心
  同士の R/S による順位付け) を実装していない — 隣り合う 2 つの立体中心の
  置換基がトポロジー的に鏡像対称なとき、どちらを先に並べるかが本来
  Rule 4/5 で決まるが、今の実装はそこを見ていないため系統的に入れ替わる
  可能性がある。ただし `assign_cip_ranks` は R/S 表示・E/Z 判定・
  `refined_colors` など多くの箇所から使われる基盤コードなので、迂闊に
  触ると影響範囲が広い。着手するなら、まず InChI 自身の正準化アルゴリズム
  (`ichican2.c`) が CIP Rule 4/5 相当をどう扱っているか (あるいはそもそも
  CIP 準拠ではなく別の正準化基準を使っているか) を確認してから、
  `assign_cip_ranks` に手を入れるのではなく `is_undefined_tetra`/
  `tetra_raw_parity` 側で局所的に対処できないか検討すること。
- `t` バケツにはさらに、重水素標識分子で `/i` の後に**もう一つ `/t` 層が
  追加される**という特殊ケースが 2 件ある (`/i12D/t12?,16-,17?` のように)。
  同位体標識によって非同位体正準化では区別できなかった原子が区別可能になり、
  同位体専用の別の立体層が必要になるケースと見られる。件数が少なく複雑
  なので優先度は低い。
- `Fhpq` バケツ (5 件) は個別性が強く、タングステン酸水和物
  (`O[W](=O)(=O)O` → `2H2O.2O.W` + `/q;;;;+2/p-2` という金属配位水/酸化物の
  正規化)・ポルフィリン様 Fe 錯体・ケイ酸塩/Zn 錯体・Ru カルベン錯体・
  多硫化物チオスルホニウムアニオンと、共通の根本原因が見当たらない。
  1 件ずつ潰すしかなさそうで、費用対効果は低め。
- `ms` バケツの残り 2 件 (メソ体シクロヘキサン六置換エステル) は
  `is_achiral` が `?` 混在時に正しい鏡像自己同型を発見できていない別バグ。
  次点で調査する価値あり。

# 追記: I70 (2026-08-19) — 3 件調査、いずれもコード修正は見送り (99.60% 据え置き)

新鮮な `pubchem_diff` (75 件) を確認: `h`28・`t`22・`b`7・`mt`7・`Fhpq`5・
`ms`2・その他各 1 件。3 方向を調べたが、いずれも狭いパッチでは対応できない
根深いバグだと判明したため、コード変更はせず知見を記録するに留めた。

## 1. `h` バケツ: 同一式・同形骨格の互変異性体どうしで正準番号が食い違う

```text
smi  CC1C=NC2=CC=CC=C12.CC1=CNC2=CC=CC=C12   (3-メチル-3H-インドール + 3-メチルインドール)
got  h2-7H,1H3;2-6,10H,1H3
want h2-6,10H,1H3;2-7H,1H3
```

2 成分は互変異性体 (結合次数だけが違う同一骨格) で、`/c` は骨格が同型な
ため文字列として完全一致し `2*` 圧縮の対象になる (これ自体は正しい — 実
InChI も一致させる)。しかし各成分の**実際の H パターンがどちらの成分に
割り当てられるか**が molrs では入れ替わっている。

原因の当たり: `canonical_numbering` (`number.rs`) のテストコメントに
「H はトポロジー後の第 2 段 (昇順)」とあるとおり、正準番号のタイブレーク
に**各原子の実 H 数**を使っている。この 2 成分は骨格 (結合次数を無視した
隣接関係) は同型だが、実際の H 分布 (3H-インドール型は C3 が sp3 で 1H、
インドール型は N1 が NH) が異なるため、同じ骨格でも成分ごとに**異なる**
正準番号付けが生成される。/c 層自体は H 数を見ないので気づかれないが、
/h 層をその番号で読むと実質的に「どちらの成分がどのパターンか」が
入れ替わって見える。

実 InChI がここで具体的にどんな不変量順序 (原子番号 → 次数 → 電荷 → 環
→ H 数 → …) を使っているかは `ichican2.c` を読まないと確定できない —
「H 数を数値のタイブレークに使うこと自体をやめる」のか「使うが結合次数
非依存の別の基準にする」のかで直し方がまったく変わるため、今回は手を
付けなかった。1 分子だけでの確認だが、同一式の構成異性体混合物という
形自体は PubChem コーパスでは稀ではないため、優先度は中程度で残す。

## 2. `mt` バケツ: `/m` の「単純反転」症状は実は `t` バケツの CIP Rule 4/5
   ギャップと同一の根本原因 (新知見)

I63/I64 は `mt` バケツの症状を「`/m` 層のシリアライズが単純な全反転
ヒューリスティックになっている」と診断していたが、今回具体的な数値で
追ってみると別の説明の方が正確だとわかった:

```text
smi  C1[C@H]([C@@H](O1)OC(=O)C(=O)O[C@H]2[C@H](CO2)N)N
got  t3-,4+,7+,8+/m1
want t3-,4+,7-,8-/m0
```

`m1` は「先頭の定義済み中心 (この場合 atom3) の生パリティが `+` なら
全反転して表示する」という `tetrahedral_layers` の規則 (569 行目
`let invert = defined[0].1 == '+';`) の結果。逆算すると:

- got の生 (反転前) パリティ: 3:`+`, 4:`-`, 7:`-`, 8:`-`
- want が m0 (無反転) で表示している値をそのまま生パリティとみなすと:
  3:`-`, 4:`+`, 7:`-`, 8:`-`

つまり **atom 7・8 の生パリティは molrs と実 InChI で完全に一致してい
て**、食い違っているのは隣接する atom 3・4 (環内で隣り合う 2 つの立体
中心) の生パリティだけ。この 2 中心の符号がちょうど入れ替わっている、
という形は I69 が `t` バケツで見つけた「隣接する類似立体中心どうしで
パリティの符号がそっくり入れ替わる」パターン (`assign_cip_ranks` が
CIP Rule 4/5 = 隣接立体中心同士の R/S によるタイブレークを実装していない
ことが原因、と診断済み) と完全に一致する。`mt` バケツの残り 6-7 件も同じ
検証をしていないが、形が酷似しているため同根の可能性が高い。

`assign_cip_ranks` (`molrs/src/stereo.rs:192`) の中身を確認したところ、
RDKit の `buildCIPInvariants`/`iterateCIPRanks` 相当の Morgan 法拡張
(原子番号・同位体・結合次数を反復的に隣接原子のランクへ畳み込むだけの
純粋なトポロジー的精密化) で、CIP の階層的有向グラフ探索や R/S 優先順位
比較は一切行っていない。Rule 4/5 相当をここに追加するのは RDKit 自身が
専用の別モジュールを要したほど本質的に大きな仕事で、既存の R/S 表示・
E/Z 判定・`refined_colors` など多くの下流処理が依存する基盤コードでも
あるため、今回は着手を見送った。着手するなら、`assign_cip_ranks` 自体を
拡張するのではなく、`is_undefined_tetra`/`tetra_raw_parity` 側の局所的な
後処理 (「隣接する 2 つの立体中心が同じ色クラスに落ちたときだけ R/S を
個別に比較して順序を確定する」というような限定パッチ) で対応できないか
を、まず `t`/`mt` 両バケツ合わせて 10 件程度の具体例を集めてから検討する
こと。`t`(4 件相当)+`mt`(6-7 件) を合わせて解消できる可能性がある分、
費用対効果は高いが、リスクも相応に高い。

## 3. `b` バケツ: テトラゾリル-ビニルクラスタは単純なテトラゾール隣接では
   再現しない — 実際はより広い縮環系のビニローグ互変異性が原因

`b` バケツ 7 件中 5 件は同一骨格 (フューズドピリミジノン-ラクタム二環系
+ ビニル + テトラゾリル置換基) を共有する:

```text
smi (簡略) …C3=NC(=C(C(=O)N3C=C2)/C=C/C4=NNN=N4)N5…
got  …/b7-6+  (ビニル C=C を定義済み E/Z として出力)
want  (該当ボンドの /b 記載なし)
```

**検証**: 単純化した「ビニル基がテトラゾールに直結しているだけ」の
分子 (`C/C=C/c1nnn[nH]1`) を `obabel --gen2D` + `inchi-1` で確認したところ、
実 InChI は普通に `/b3-2+` を出力した (テトラゾールの可動 H 自体は
ビニル結合の E/Z を消さない、というのが単独では正しい)。同じ手法を
コーパスの実分子そのもの (置換基を含むフル SMILES) に通すと `want` と
完全一致 (`/b` なし) を確認できたので、パイプライン自体は信頼できる。

つまり抑制の引き金はテトラゾールそのものではなく、**ビニル基が付いて
いる側の縮環ラクタム環自身が持つ互変異性** (環内の `C=N-C(-NR2)=C(-ビニル
置換基)-C(=O)-NH-` という、2-アミノピリミジン-4(3H)-オン型の骨格 —
アミノ/イミノ互変異性とラクタム/ラクチム互変異性が縮環で連結している)
が、ビニル結合まで共役的に (2 結合先まで) 波及しているとみられる。I42 で
実装した「ビニローグなアミジン-カルボニル互変異性の橋渡し」機構が、この
ケースでは橋渡しの範囲が 1 結合分足りず、外側のビニル結合まで届いていない
可能性が高い。

過去にこのバケツで似た「候補集合を広げる」修正を 2 回試みてゲートを退行
させた記録があるため (`RUST_INCHI_I29_PLAN.md` 内 I47 節周辺)、今回は
最小再現分子 (縮環ラクタム環 + ビニル + テトラゾリル、余分な置換基なし)
を先に作って `inchi-1` で挙動を確定してから `number.rs` の該当ロジック
(`hub_allowed_endpoints` あるいは I42 のビニローグ橋渡し部分) を狭く
拡張する、という手順を踏むべきだが、今回はその最小分子の構築まで至らず
時間切れとした。次回の最優先候補として残す。

## 4. `q` バケツ (1 件): モリブデン/チオ尿素/硫化物錯体、個別対応向き

`CCCCCCCCCCCCCCCCCCN(CC(CC)CCCC)C(=O)S.C(=O)(N)S.S=[Mo]` 系の錯体で、
got は Mo に `+2`・遊離 S に `-2` を割り当てるが want は電荷層自体が
無い (全体中性、成分をまたいだ配位子-金属間の電荷分配が違う)。共通する
一般則が見えない一分子限りの個別ケースで、費用対効果は低い。次点。
(→ I71 で修正済み。実際は一般則があった。)

# 追記: I71 (2026-08-19) — 金属=カルコゲンの多重結合を中性で切る + CIP
# パリティの根本原因を「R/S 経由の遠回り」と再定義 (99.60% → 99.60%)

今回は主目標を CIP Rule 4/5 ギャップ (I69/I70 が `t`/`mt` バケツ共通の
根本原因と特定) の解消に置いたが、精査の結果**基盤コードの全面書き直しに
なる**と判明したため見送り、代わりに単発で確実に潰せる `q` バケツの金属
錯体ケースを修正した。加えて CIP ギャップの根本原因の理解を大きく前進
させた (下記) ので、次回はより狭いスコープで着手できる。

## 実装した修正: `disconnect_metals` の金属=カルコゲン多重結合

I70 で「個別ケース・費用対効果低」と切り捨てた `q` バケツの
`...S=[Mo]` (チオモリブデン酸型) を `inchi-1` で再検証したところ、実は
明確な一般則があった:

```text
S=[Mo]                 → Mo.S            (電荷なし)
O=[Mo]=O               → Mo.2O           (電荷なし)
S=[Fe]=S               → Fe.2S           (電荷なし)
O=[Cr](=O)([O-])[O-]   → Cr.4O/q;;;2*-1  (=O は中性、元の O⁻ だけ -1 残る)
CO[Mo]                 → CH3O.Mo/q-1;+1  (単結合は異方開裂で電荷)
COCCO[Hg]              → C3H7O2.Hg/q-1;+1 (単結合、I41 で確認済み)
```

つまり**金属-配位子結合の電荷移動は order 1 (単結合) のときだけ**で、
order ≥ 2 の多重結合 (金属=O/S/N 等) は**均等開裂で中性**に切る。
`molrs/src/inchi/disconnect.rs::disconnect_metals` は従来すべての次数で
`charges[ligand] -= order; charges[metal] += order;` と結合次数分の電荷を
移していたため、`S=[Mo]` を Mo⁺²/S⁻² にしていた。異方開裂ブロックの
条件に `&& order == 1` を追加し、多重結合は電荷移動も metal-lock もせず
中性で切るようにした。

**効果**: PubChem 99.60% → **99.60%** (18488/18563 → 18489/18563、+1)。
`q` バケツ 1→0。`inchi_gate` 変化なし (100%/100%)。clippy クリーン、
`cargo test --release` 全体パス (単体テスト `metal_chalcogen_double_bond_
breaks_neutral` を追加)。他バケツへの新規不一致なし (総数 75→74)。

## 主目標の再定義: CIP Rule 4/5 ギャップ = 「/t パリティが R/S を経由して
   いる」設計上の遠回りが原因 (次回向けの精密化されたリード)

`t` バケツのパリティ入れ替わり (`t7-,8-,9+,10-` であるべきところ
`t7-,8-,9-,10+`) を精査し、**根本原因を I69/I70 の「CIP Rule 4/5 未実装」
よりさらに具体的に特定した**。着手スコープが従来認識より狭いことが
分かったので記録する。

### 機構

molrs の `/t` パリティは `inchi/stereo.rs::tetra_raw_parity` が
**R/S ラベル経由**で計算している:

1. `stereo.rs::assign_atom_chiral_codes` が CIP ランク (`assign_cip_ranks`)
   を使って各中心の R/S を決める。
2. `tetra_raw_parity` が `rs_bit`(R/S) ⊕ `pp`(CIP 昇順↔正準昇順の置換
   パリティ) ⊕ `needs_flip` で正準番号基準のパリティに変換する。

この 2 回の CIP 変換は代数的に打ち消し合うので、**R/S さえ正しければ
正準パリティも正しい**。ところが `assign_cip_ranks` は RDKit の Morgan 法
CIP 近似 (原子番号・同位体・結合次数の反復畳み込み) で、**CIP Rule 4/5
(隣接立体中心の R/S によるタイブレーク) を実装していない**。隣り合う
2 つの立体中心が互いの枝の優先順位を決め合う配置 (糖鎖・エポキシド・
ポリオール/ポリエステル鎖でよく出る) では Morgan 近似では枝がタイに
なり得ず**誤った R/S** を出す。その結果 2 中心の `/t` パリティがそっくり
入れ替わる。正準**番号**は number.rs 側の別経路 (InChI 流 Morgan) で
決まるため got/want で一致し、**符号だけ**がずれる — 観測される症状と
完全に一致する。

### 肝心の洞察: InChI の `/t` パリティは R/S を一切経由しない

実 InChI はパリティを**正準番号順の幾何配置から直接**計算し、CIP R/S を
使わない。molrs だけが R/S を経由しているため、InChI が本来持たない
CIP Rule 4/5 依存性を持ち込んでいる。したがって正しい修正は
「CIP Rule 4/5 を実装する」(巨大) ではなく **「`/t` パリティを R/S 経由を
やめて、生のキラル配置 (`@`/`@@`) から正準番号順への直接の置換パリティ
として計算する」(1 関数 + 1 ヘルパー公開の中規模)** である。

- 生配置データは既に存在: `stereo.rs::adjusted_chiral_tags` が
  `g.parsed` の `chirality`/`neighbor_order` から、各中心のタグを
  `ctx.atom_bonds[gi]` 順基準の `Chirality`(Clockwise/Anticlockwise) に
  補正して返している (現状は `assign_atom_chiral_codes` 内でのみ消費)。
- これを (参照近傍順とともに) `inchi/stereo.rs` へ公開し、
  `tetra_raw_parity` を「生タグ ⊕ (参照順↔正準昇順の置換パリティ)」に
  書き換えれば R/S・CIP を完全にバイパスできる。

### なぜ今回やらなかったか / 次回の注意

`tetra_raw_parity` は**全四面体中心の `/t` パリティを生成する中核**で、
`assign_atom_chiral_codes` の 3 重原子 + 暗黙 H / 孤立電子対の仮想第 4
置換基の扱い (`n_swaps += 1` 補正) と `tetra_raw_parity` の `needs_flip`
補正が微妙に噛み合っている (I30/I34 で慎重に調整した経緯あり)。直接
パリティ経路を新設する際はこの仮想第 4 置換基の位置規約を厳密に一致
させる必要があり、外すと全 `/t` が退行する。18563 + 14697 + rdkit_compat
の 3 コーパス全通しでの検証が必須。過去 2 ラウンド (I69/I70) がこの
基盤コードの広い影響範囲ゆえに着手を見送ったのと同じ理由で、フォーク
1 回の予算では安全に完遂・検証しきれないと判断し、代わりに `q` の
金属修正を確実に landing した。次回はこの直接パリティ設計を、まず
`t`(4 件相当) + `mt`(7 件) の具体例を手元で全部 `inchi-1` 突き合わせて
から、専用セッションで腰を据えて実装すること — 成功すれば ~11 分子を
一度に解消できる高価値リード。

# 追記: I72 (2026-08-19) — I71 の「R/S 経由が原因」仮説を実装で反証、
# 真の原因は number.rs の正準番号タイブレークと再特定 (99.60% のまま)

I71 が設計した「`/t` パリティを R/S を経由せず生タグから正準番号順への
直接の置換パリティとして計算し直す」修正を実装し、`crate::stereo` に
`raw_chiral_frames` (生タグ + 参照近傍順を公開する新関数) を追加、
`inchi/stereo.rs::tetra_raw_parity` を全面的に書き換えた。

## 実装 → 検証 → 反証

新旧の等価性を**代数的に証明してから**実装した (旧式: rs_bit ⊕
perm(CIP昇順→正準昇順) ⊕ needs_flip、新式: 生タグ ⊕ perm(参照順→正準
昇順) ⊕ 1 — 仮想 4 番目は正準昇順側で末尾、参照順側で**先頭**に置くの
が旧式との等価性から導出した位置)。`cargo test --release --test
inchi_gate` は 100%/100% を維持し、clippy もクリーンだったが、
`pubchem_gate` を実行すると **18489/18563 (99.60%)、旧実装と完全に
同じ件数**。`pubchem_diff` で `t`/`mt` バケツの内訳を見ても 22/7 の
まま 1 件も動いていない。

これは実装ミスではなく、**旧式と新式が数学的に厳密に等価**だったことを
意味する — 置換パリティの合成則 `perm_parity(A,C) = perm_parity(A,B) ⊕
perm_parity(B,C)` は中間順序 B (旧式では CIP ランク順) が**何であっても**
成り立つ恒等式で、B が「正しい CIP ランク」である必要はない。旧実装の
`rs_bit` (R/S 計算時の CIP ランク) と `tetra_raw_parity` 内で使う CIP
ランクは同じ `assign_cip_ranks` (Rule 4/5 未実装の同一アルゴリズム) を
呼んでいるため、たとえ Rule 4/5 が必要な同点局面でも**両方の呼び出しが
同じ (間違った) タイブレークを再現し、往復で厳密に打ち消し合う**。
**つまり I69/I70/I71 が追ってきた「R/S 経由で CIP Rule 4/5 の欠落が
`/t` パリティに漏れ出す」という仮説は誤りだった** — R/S 経由は数学的に
中立で、`/t` パリティの計算式自体に CIP ランクの正しさは一切影響しない
ことを、独立実装 (CIP を一切使わない直接パリティ計算) の全数一致に
よって実証した。この直接パリティ実装は正しさの面では旧実装と同値
(かつ設計としては素直) だが、**実利益ゼロ・複雑性だけ増える**ので
コミットせず `git checkout --` で元に戻した。

## 反証後の実例トレース: `mt` の実際の不一致パターン

`mt` バケツの実例 (`C1[C@H]([C@@H](O1)OC(=O)C(=O)O[C@H]2[C@H](CO2)N)N`)
を手で追うと:

```text
got  (表示): t3-,4+,7+,8+/m1
want (表示): t3-,4+,7-,8-/m0
```

`/m` 正規化 (先頭定義済み中心の生パリティが '+' なら全反転して m=1) を
逆算して**生パリティ**を復元すると:

```text
molrs 生: [3:'+', 4:'-', 7:'-', 8:'-']
実 InChI 生: [3:'-', 4:'+', 7:'-', 8:'-']
```

**原子 7・8 の生パリティは実は完全一致**しており、表示上の食い違いは
`/m` の全反転 (どの中心を「先頭」として基準にするか) が原子 3・4 の
食い違いに引きずられて出た副作用に過ぎない。実際に食い違っているのは
**正準番号 3 番と 4 番の生パリティが入れ替わっている** ことだけ。

この 2 分子 (エポキシド/オキセタン型の対称なジエステル) では原子 3 と
4 はおそらく**位相的に対称** (連結性だけでは区別できず、それぞれの
局所的な立体配置によってのみ区別される) — この場合、`/c` 層 (実際
一致している) には現れない形で、**どちらの物理原子が正準番号 3 を
名乗るか 4 を名乗るか**という正準番号付け自体のタイブレークが、実
InChI の規約と逆になっている可能性が高い。もしそうなら、原因は
`tetra_raw_parity` (パリティの**計算式**) ではなく、`number.rs` の
正準番号付けアルゴリズムが**立体配置を使ったタイブレーク** (実質的に
InChI 版の "CIP Rule 4/5 相当") を実装していないこと — 個々の中心の
パリティ計算はどちらも正しいが、パリティが**貼り付けられる番号側が
入れ替わっている**、という仮説になる。

## 次に着手するなら

- **`stereo.rs` (パリティ計算式) ではなく `number.rs` (正準番号付け) を
  疑うこと。** 今回の実装 (`raw_chiral_frames` 等) は反証目的で全て
  元に戻し済みなので、次回は最初からこちらに集中してよい。
- 具体的な調査の入口: `number.rs` の正準番号割り当てアルゴリズムが、
  連結性だけでは同順位になる 2 原子 (今回の例の原子 3・4 のような) を
  区別するために**立体配置 (chiral_tag や生パリティ) を入力に使って
  いるか**を確認する。使っていなければ、それが real InChI の
  `ichican2.c`/`ichiring.c` 等 (連結性正準化に立体を混ぜるロジック、
  未読) にあるはずの機構で、molrs 側の欠落箇所。
- 上の 1 分子例 (原子 3・4 が対称な二置換基を持つオキセタン環) を
  最小化した合成分子で `inchi-1` と突き合わせ、正準番号がどちらの原子に
  3/4 を割り振るかを狭く確認してから着手すること — 今回と同じ轍を
  踏まないよう、まず反例で仮説を壊せないか試すこと。
- `t`/`mt` 以外に `t` バケツの一部 (今回覗いた 3 件中 2 件) は実は
  「パリティ入れ替わり」ではなく、`is_undefined_tetra` が拾いそこねて
  いる追加の `?` 中心 (縮環した多環系の未定義立体中心) だった —
  こちらは `ms` バケツの自己同型ギャップ (I69 で着手、鏡像自己同型のみ
  検証、非鏡像の一般対称性は未検証) と同系統の可能性がある。`t` バケツに
  手を付けるなら、まず `pubchem_diff --layer t` の全件を「パリティ
  入れ替わり型」と「`?` 欠落型」に仕分けてから着手すること。

# 追記: I73 (2026-08-20) — number.rs の正準番号タイブレークに `/t` 層を
# 追加、`t`/`mt` バケツの「パリティ入れ替わり」を解消 (99.60% → 99.62%)

I72 が特定した通り、原因は `stereo.rs` のパリティ計算式ではなく
`number.rs::number_component` 側にあった。

## 原因

`resolve()` が同値クラスに残ったタイを解くとき、比較キー `CandKey` は
`(c 層, h 層, q 層, b 層)` の 4 つだけで **`/t` 層 (四面体パリティ) が
候補比較に一切使われていなかった**。位相的に同値な (連結性・元素・次数・
可動 H フラグ・電荷・固定 H 数がすべて同じ) 2 つの四面体中心があるとき、
`c`/`h`/`q`/`b` のどのシグネチャでも区別が付かなければ、どちらの物理原子に
小さい正準番号を割り振るかは `resolve()` の再帰分岐順 (`members` の列挙
順、原子のグラフ内出現順に依存する任意の順序) で決まってしまい、実 InChI
の規約と噛み合わない場合があった。

I72 の実例 (`C1[C@H]([C@@H](O1)OC(=O)C(=O)O[C@H]2[C@H](CO2)N)N`、対称な
2 つのオキセタン環をオキサル酸ジエステルで橋渡しした分子) で確認した通り、
正準番号 7・8 の生パリティは molrs と実 InChI で完全一致するが、3・4 は
入れ替わっていた — これは 2 つの物理原子 (それぞれ NH2 側・エステル O 側の
環炭素) が `c`/`h`/`q`/`b` レベルでは区別できないタイのままで、`/t` 層だけ
が実際には異なる、というシグネチャそのままの症状だった。

## 修正

`super::stereo::tetra_raw_parity` と同じ計算式 (CIP 昇順↔正準昇順の置換
パリティ ⊕ R/S ⊕ 仮想 4 番目補正) を、**候補ごとの numbering を使って**
再計算する `TStereo`/`t_signature` を `number.rs` に追加し、`CandKey` に
`b_signature` の次 (InChI の層順 c→h→q→b→t に合わせた位置) として追加した。
`BStereo`/`b_signature` と全く同じ設計 (番号付けに依存しない材料だけを
事前計算し、候補ごとに正準昇順部分だけ再計算) を踏襲している。

`/t` の印字は先頭定義済み中心の生パリティが `+` なら全反転する `/m`
正規化を経るが、この反転は全中心に一様にかかるため候補間の相対順序を
変えない — 生パリティ (未反転) をそのまま比較材料にしてよい。`-` を内部
コード 0 (小さい) 、`+` を 1 とする規約は `/b` 層の `-`=1<`+`=2 (odd<even)
という既存の規約と向きを合わせた上で、上記の実例 (`-`,`+` の並びが実
InChI の選択、`+`,`-` が molrs の誤った選択) から導出し、候補キー全体の
辞書順最小化と整合することを確認した。

`perm_parity` (旧: `stereo.rs` 内 private) を `pub(crate)` にして
`number.rs` から再利用し、パリティ計算式そのものの二重実装・将来的な
乖離を避けた。

**なぜ `stereo.rs` 側 (`tetra_raw_parity`) を直接直さなかったか**:
I72 が数学的に証明した通り、`tetra_raw_parity` の計算式自体 (R/S 経由か
生タグ直接かに関わらず) は与えられた正準番号に対して常に正しい相対パリティ
を返す — 壊れているのは「どちらの物理原子が正準番号 N を名乗るか」という
`number.rs` 側の割り当てだけであり、`stereo.rs` は変更不要というのが
I72 の分析通りだった。

## 検証

- PubChem: 18489/18563 → **18493/18563 (99.60% → 99.62%、+4)**。`t`
  22→21、`mt` 7→4 (合計 -4)。総不一致 74→70、他バケツへの新規不一致
  なし (`c` 単独バケツは 0 のまま — 正準番号の変更が接続層に影響していない
  ことを個別に確認)。
- `inchi_gate`: 変化なし、100%/100% (7452/7452、7244/7244)。
- clippy クリーン、`cargo test --release` 全体 255 件 (16 スイート) パス。
- I72 でトレースした実例分子を直接確認し、`t3-,4+,7-,8-/m0` が実 InChI と
  完全一致することを確認済み。

## 次に着手するなら

- `t` バケツ残り 21 件・`mt` 残り 4 件は、まだ「パリティ入れ替わり型」
  以外の原因 (I72 が触れた「`is_undefined_tetra` が拾いそこねている `?`
  中心」型など) が混在している可能性が高い。`pubchem_diff --layer t`
  の全件を再仕分けしてから着手すること。
- `ms` バケツ (2 件) は I69 で鏡像自己同型のみ検証済み、非鏡像の一般
  対称性検証は未着手のまま。
- `b` バケツのテトラゾリル-ビニル系クラスタ (7 件) はリレー原子の
  パス所属を `tgroup` に持たせる必要があるという設計はあるが、
  過去の類似実装が退行を招いた経緯があるため要注意。
- `h` バケツ (28 件) は over/under-merge・帯電 `H-` 群・多成分間リン酸
  併合など複数のパターンが未着手のまま残っている。

# 追記: I74 (2026-08-20) — `is_undefined_tetra` が中性 3 員環 N を
# 見落としていた

I73 の「次に着手するなら」で予告していた「`is_undefined_tetra` が拾い
そこねている `?` 中心」型を、`t` バケツ 21 件を再仕分けして確認した。
21 件のうち約 17 件が「実 InChI の `/t` に molrs が完全に脱落させている
追加の `N?` (または追加の `C?`) が 1〜2 個ある」という共通パターンで、
うち少なくとも 5 件は SMILES 上に 3 員環 (`N1...C1` や `N2...C2` の
隣接ペア) が明示的に含まれる分子だった。例:

```text
smi  CC(C)(C)[C@@H]1[C@H](N1C2=CC=C(C=C2)F)C(=O)C3=CC=CC=C3
got  ...t16-,18+/m1/s1
want ...t16-,18+,21?/m1/s1        ← 21 はアジリジン環の N
```

## 原因の特定 (`inchi-1` で先に検証)

最初は「窒素は中性なら常に立体源性にならない (鎖状アミンは室温で反転が
速い)」という molrs の既存の前提そのものを疑い、`obabel -ismi -oinchi`
経由で `inchi-1` に複数の変形分子を通した (`obabel -ismi -omol -O
t.mol && inchi-1 t.mol` という従来の手順は、座標なし SMILES から生成
した mol ファイルで立体タグが欠落するため今回は使えなかった —
`obabel -ismi -oinchi` で直接変換する方が信頼できる):

```text
smi  CN1C(C)C1CC                    (アジリジン環だが環炭素側も未定義)
want …h5-6H,4H2,1-3H3               ← /t 自体が付かない (環炭素も N も
                                        立体源性なし)

smi  C[C@@H]1[C@H](CC)N1CC          (環炭素側に @ を付けて定義済みに)
want …t6-,7+,8?/m1/s1               ← N (8) に ? が付く

smi  C[C@H](CC)N(C)CC               (鎖状第三級アミン、環なし)
want …t7-/m1/s1                     ← N には何も付かない (対照区)

smi  C[C@@H]1[C@H](CC)CN1CC         (4 員環アゼチジン、N-アルキル)
want …t7-,8-/m1/s1                  ← N には何も付かない (3 員環限定)
```

つまり実 InChI の規則は「N が中性でも、孤立電子対中心 (重原子 3・H 0、
= N-アルキル置換) かつ 3 員環 (アジリジン型) の一部なら立体源性」という
ピンポイントな例外で、鎖状第三級アミンにも 4 員環以上 (アゼチジン等)
にも適用されない。分子力学的にも筋が通る (3 員環の反転障壁だけが立体
配置を固定するほど高い)。

## 修正

`is_undefined_tetra` の `ok_elem` 判定で、N の分岐を
`a.formal_charge > 0 && !lone_pair_center` (荷電 N のみ) から、荷電 N
はそのまま維持しつつ、中性かつ `lone_pair_center` の場合は追加で
「重原子隣接 3 個のうち 2 個が互いに直接結合している (= その 2 個と N
で 3 員環を構成する)」を要求するよう分岐した。実装は
`heavy[i+1..].any(|nb2| g.adjacency[nb1].contains(&nb2))` の総当たりで、
新しいグラフ探索や環検出ロジックは追加していない (既存の隣接情報だけで
判定できる)。

## 検証

- PubChem: 18493/18563 → **18497/18563 (99.62% → 99.64%、+4)**。`t`
  21→17、他バケツへの新規不一致なし (総不一致 70→66)。
- `inchi_gate`: 変化なし、100%/100% (7452/7452、7244/7244)。
- clippy クリーン、`cargo test --release` 全体 273 件パス。
- 上記の 4 つの変形分子すべてで molrs の出力を `inchi-1` の出力と直接
  突き合わせて一致を確認済み。

## 次に着手するなら

- `t` バケツ残り 17 件のうち、少なくとも 2 件は「パリティ入れ替わり型」
  (I72 が見つけた `mt` 系と同根と思われる、隣接立体中心間の CIP タイ
  ブレークではなく、まだ特定できていない別の原因)。もう 2 件は同位体層
  `/i` が絡む分子で、実 InChI が `/i12D` の**後**に別の `/t` 副層
  (`/t12?,16-,17?` のような同位体由来の立体源性) を追加で出す — これは
  molrs が全く実装していない機能 (同位体置換が新たな立体中心を作る
  ケース) で、範囲が大きいため今回は着手していない。残りは大きい分子
  1〜2 個ずつの散発的な `?` 脱落で、`is_undefined_tetra` の 1-WL 精緻化
  が対称性を証明しきれていない (I52/I60 が繰り返し指摘している一般の
  自己同型探索の不足) 可能性が高い。
- `mt` バケツ残り 4 件は今回未再確認。`t` の残りと同じ原因が多いか、
  一度 `pubchem_diff --layer mt` を単独で仕分けし直すこと。
- `ms` バケツ (2 件) は I69 で鏡像自己同型のみ検証済み、非鏡像の一般
  対称性検証は未着手のまま。
- `b` バケツのテトラゾリル-ビニル系クラスタ (7 件) はリレー原子の
  パス所属を `tgroup` に持たせる必要があるという設計はあるが、
  過去の類似実装が退行を招いた経緯があるため要注意。
- `h` バケツ (28 件) は over/under-merge・帯電 `H-` 群・多成分間リン酸
  併合など複数のパターンが未着手のまま残っている。

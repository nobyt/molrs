# SMILES → InChI / InChIKey (pure Rust) 実装計画

> **v1 実装状況 (2026-08-05 更新)**: I0–I28 実装済み。実測
> (コーパス 7,453 分子、RDKit オラクル):
> - 式層 (Hill): 99.14% 一致 (残差は電荷正規化)
> - 正準番号 (AuxInfo /N:): **99.54%** 一致 (残差は AuxInfo 側の規約差)
> - **InChIKey 機構** (`inchi_key_from_string` vs RDKit `InchiToInchiKey`,
>   非立体 InChI 7,245 件): **7245/7245 = 完全一致**。SHA-256 (FIPS 180-4) +
>   base-26 (`ikey_base26.c` 移植、除外トリプレット対応) は公式とビット一致
> - フル InChI 文字列・InChIKey: コーパス **7,453 件全件が完全一致
>   (100.00%)** (I4 時点は 74.17%、詳細な推移は下記)。
>   I20 で多成分に対応したため 7,453 件**全件**が生成対象
> - CLI: `cargo run --bin inchi` (stdin SMILES → JSONL)
> - **立体対応済み (I6-I8, 2026-07-19)**: `/b` (E/Z 二重結合)・`/t/m/s`
>   (四面体) 層を実装。四面体パリティは molrs の R/S を再利用し
>   `raw = '-' iff (rs_bit XOR perm(CIP昇順→正準昇順))`、/m は最初の中心を
>   '-' に正規化。InChIKey の minor 文字列は立体層 (先頭 '/' 込み) の
>   **2 重連結** を SHA-256 (公式 ikey.c 準拠)。立体分子 208 件中 151 件が
>   InChI 文字列・キー完全一致 (残りは骨格側の可動 H/電荷が原因)。
> - **可動 H 認識を一般化 (I9, 2026-07-23)**: 中心原子に結合したヘテロ端点
>   (受容体 = 二重結合、供与体 = H/負電荷) で 1,3-互変異性群を作る一般規則に。
>   端点は末端でなくてよい (アミド/ラクタムの二級 N)。N 端点は中心が炭素
>   または (末端 N かつ 中心が二重結合 O ≥2 = スルホニル級) のとき。O/S だけで
>   酸系を成すなら N を除外 (カルバミン酸)。アミド/アミジン/グアニジン/尿素/
>   ラクタム/一級スルホンアミドを新たに正しく処理。
> - **電荷 q/p + c/h 層修正 (I10, 2026-07-23)**: normalize.rs で電荷正規化
>   (負の酸点・ハロゲン化物イオンをプロトン付加で中性化、陽イオン塩基を
>   脱プロトン、残余 → /q、移動 → /p)。c 層の分岐は全非末端項目を 1 つの
>   カンマ括弧に統合する規則へ修正 (四級 N の (6-2,7-3)8-4)。h 層の可動群は
>   カンマなし連結 ((H,5,6)(H,7,8))。単一成分の荷電分子 53/53 完全一致。
>   フル InChI 一致 **79.04%** (5880/7439)。
> - **zwitterion/イリド中性化のスキップ (I11, 2026-07-23)**: 電荷分離した
>   隣接原子 (N-オキシド・ニトロ・アジド等) はプロトン化・脱プロトン化の
>   対象から除外 (InChI は共有結合の中性形のまま扱う)。フル InChI 一致
>   **79.6%**。
> - **芳香環の可動 H 検出修正 (I12, 2026-07-26)**: `mobile_groups` (I9) が
>   結合次数の判定に `g.bond_orders` (芳香族は 1.5 に潰れる) を使っていたため、
>   芳香族として認識される環 (イミダゾール/トリアゾール/テトラゾール/
>   ピリジノン様の縮合環など) では中心原子の二重結合受容体を一切検出できて
>   いなかった。`g.kekule_bond_orders` (Kekule 化済みの実値 1/2) を使うよう
>   修正。単一中心 (中心 1 原子 + ヘテロ端点 2 個) パターンの環内版が新たに
>   拾えるようになり、フル InChI 一致 **79.6% → 84.50%** (6286/7439)。
> - **芳香環をまたぐ多中心互変異性検出 (I13, 2026-08-02)**: `mobile_groups`
>   を「種 (中心 1 原子の星型検出、旧 I9) → ブリッジ統合」の 2 段に拡張。
>   Kekule 構造を「マッチング」とみなし、種の各メンバー・孤立供与体
>   (どの中心の候補にもならなかった H 保持ヘテロ原子、例: トリアゾール環の
>   中心 N) から「自由結合 → 二重結合 (マッチング辺) で反転」を繰り返す
>   交互パス探索 (union-find で統合) を実装。誤爆抑制のため
>   (a) 中継原子は芳香族限定 (孤立アルケン・スルホニル中心越しの誤統合を防止)、
>   (b) 環内に入ったら単一の SSSR 環にロックし、縮環の共有原子越しに
>   別の環へ「乗り換える」ことを禁止 (インダゾール/アザインドール型で
>   ピロール N-H とピリジン N を誤って同一群にしない)、
>   (c) 酸除外規則 (カルバミン酸で N を落とす等) で意図的に除外された原子は
>   孤立供与体として再導入しない、の 3 点を実装。
>   ピリジン置換アミド・2-アミノピリミジン・キナゾリンジオン・
>   トリアゾール/テトラゾール環等が新たに正しく群化。
>   フル InChI 一致 **84.50% → 89.11%** (6629/7439)。
> - **I14: 可動 H 検出をブロッサム法ベースに刷新 (2026-08-03)**: 縮合ヘテロ
>   二環 (インダゾール/アザインドール系) の可否判定を、IUPAC 公式 InChI
>   ソース (`github.com/IUPAC-InChI/InChI`, v1.07,
>   `INCHI-1-SRC/INCHI_BASE/src/ichitaut.c`・`ichi_bns.c`) を直接参照して
>   解明した。実際の可動 H 判定は単純なグラフ探索ではなく、
>   `nGetEndpointInfo()` の**価数スラック**による原子ごとの供与体/受容体
>   分類 (記号による特別扱いではなく純粋な算術) と、
>   `bExistsAltPath`/`RunBalancedNetworkSearch` による Kocay–Stone の
>   容量付きバランスドネットワークフロー (Edmonds のブロッサム法を一般化
>   したもの) の組み合わせ。実機 `inchi-1` バイナリをビルドし、
>   デバッグ計装で実際の中心点走査を観測して検証した。
>   molrs が扱う「1 原子は高々二重結合 1 本」という通常の有機分子の
>   Kekule 構造では容量は常に 0/1 であり、標準的な (容量なしの) 一般
>   グラフマッチングのブロッサム法と数学的に等価になるため、フル
>   フローネットワークではなく**標準ブロッサム法** (`molrs::inchi::blossom`、
>   純粋グラフ理論として単体テストで独立検証) で足りると判断し実装:
>   1. **受容体判定を価数スラック方式に修正**: `seed_groups` (I9) が
>      「中心-隣接原子間のこの特定の結合が二重か」を見ていたのを、
>      「隣接原子自身が集約的に二重結合を 1 本持っているか
>      (結合次数合計 − 次数 = 1)」に変更 (`is_acceptor_from`)。芳香環内で
>      「どの結合が Kekule 二重か」は任意に選んだ 1 通りの共鳴構造に
>      過ぎないため、特定の結合で判定すると Kekule 化の向きにだけ依存する
>      非対称な誤りが生じていた。ただし非芳香族の隣接原子には引き続き
>      特定結合での判定を使う (孤立した遠くの二重結合まで誤って受容体
>      扱いする過剰検出を防ぐ — 例: スルホキシド S=O から見て同じ S に
>      単結合する無関係な -OH)。
>   2. **種形成時の局所 H 要件を撤廃**: ある中心に有効な受容体対 (例: 縮環の
>      共有原子が両環のヘテロ原子と直接隣接) があっても、その場に供与体
>      (H を持つ端点) がなければ種として成立しなかった。真の供与体は
>      別経路のブリッジ探索で後から合流すればよいので、群の妥当性判定
>      (`total_h + total_neg > 0`) は全ブリッジ完了後に [`mobile_groups`]
>      側でまとめて行うよう変更。
>   3. **単一環ロックを頂点分割に置換**: `mobile_groups` のブリッジ探索を
>      ブロッサム法 (`blossom::alternating_reachable`) に刷新。旧 I13 の
>      「単一 SSSR 環へのロック」ヒューリスティックは廃止し、代わりに
>      **縮環の共有原子を環ごとに別頂点へ分裂**させる (頂点分割) ことで、
>      ある環の辺を通って共有原子に到達した探索が別の環の辺へ無条件に
>      「乗り換える」ことを防止 (ブロッサム法自体は変更しない汎用アルゴリズム
>      のまま、グラフ構築側だけの工夫)。芳香環をまたぐ正当な互変異性
>      (縮環の共有原子自体が両環のヘテロ原子と直接隣接する場合) は 1. の
>      修正により種の段階で拾われるため、ブリッジ探索側で縮環をまたぐ
>      必要がなくなった。
>   検証: `Cc1[nH]nc2ncccc12` (縮環をまたぐべき、3 端点) と
>   `Cc1[nH]nc2cccnc12` (またがない、2 端点) — トポロジー的にほぼ同一な
>   位置異性体だが縮環共有原子とヘテロ原子の直接隣接関係だけが異なる —
>   および `Cc1c[nH]c2cccnc12` (可動 H なし) の 3 分子全てで実機 `inchi-1`
>   の出力と完全一致することを確認。
>   フル InChI 一致 **89.11% → 89.33%** (6645/7439)。
> - **I15: `seed_groups`/`mobile_groups` の細部修正 5 件 (2026-08-03)**:
>   I14 完了直後にコーパス残差を再分析し、以下を追加修正。
>   1. **種が共有端点を持てるように**: `seed_groups` が使っていた `used[]`
>      早期排除 (ある中心の端点が既に他の中心で使われていたら種を作らない)
>      を撤廃。2 つの中心が同じヘテロ原子を端点として共有する場合、両方が
>      独立した種を形成し `mobile_groups` の union-find がまとめて 1 つの
>      群に統合する — 片方だけを弾くと、共有原子の反対側の端点が群から
>      完全に脱落していた (例: 縮環系でベンズイミダゾール型の中心と
>      ピリジン型の中心が N を共有するケース)。
>   2. **累積二重結合・三重結合の誤検出を修正**: `is_acceptor_from` の
>      非芳香族側フォールバックが `>= 2.0` (二重**以上**) だったため、
>      ニトリル (三重結合) を誤って受容体と判定していた。`== 2.0` に修正。
>      また中心自身が「単結合で繋がる自由な端点」を 1 つも持たない場合
>      (例: N=C=N の中心 C、両端点とも中心からは二重結合) は、両端点が
>      個別に価数スラックを持っていても単純な星型として無効とするガード
>      を追加 (`has_single_from_center`)。スルホン酸 (S に二重 O が 2 つ
>      あっても単結合の -OH が残る) は該当しないため引き続き機能する。
>   3. **縮環の共有原子の仮想マッチ**: 縮環の共有原子の実際の Kekule
>      二重結合が「別の」環に割り当たっている場合、その環では二重結合を
>      持たないように見えてしまい、ブロッサム探索がそこで途切れる (環の
>      中心寄りのヘテロ原子まで届かない)。同じ環内で隣接し合う「その環
>      では二重結合を持たないが実際にはどこかに持つ」共有原子どうしを、
>      その環内限定の仮想的なマッチとして対応付けることで対処。
>   4. **ブリッジ探索の辺を環外ヘテロ原子限定に**: 旧ルール (両端の
>      少なくとも一方が芳香族なら辺を採用) だと、芳香環が「環外側が
>      ヘテロ原子でない」辺 (例: カルボン酸の環外 C) まで辿ってしまい、
>      本来無関係な 2 つの官能基 (例: 同じベンゼン環上のカルボン酸と
>      アセトアミド) が誤って 1 つの群に統合されていた。環外側がヘテロ
>      原子の場合のみ辺を採用するよう修正。
>   検証: `Cc1nc2cccnc2[nH]1` (共有端点統合)、`N=C=N`/`N=C=O`/`NC#N`
>   (累積二重結合・三重結合)、`Cc1n[nH]c2ncccc12` (仮想マッチ)、
>   `OC(=O)c1ccc(NC(C)=O)cc1` (無関係な官能基が分離) の全てで実機
>   `inchi-1` の出力と完全一致することを確認。
>   フル InChI 一致 **89.33% → 93.33%** (6943/7439)。
> - **I16: バランスドネットワークの調査と `has_search_slack` (2026-08-03)**:
>   I15 の残存ケース (縮環系の橋頭ヘテロ原子に隣接する環外ドナーの誤った
>   橋渡し) に対応するため、IUPAC 公式 InChI の `ichi_bns.c`
>   `BalancedNetworkSearch` (Kocay–Stone の容量付きバランスドネットワーク
>   フロー) を完全抽出し、実機 `inchi-1` を計装して複数の分子ペアを追跡した
>   (2 回の専用調査、詳細は調査ログ参照)。判明した点:
>   1. **原子ごとの価数スラック** (`MAX_AT_FLOW(atom) = chem_bonds_valence
>      − 次数`) が 0 の原子 (橋頭ヘテロ原子など、二重結合も H/負電荷も
>      持たない) は、実 InChI ではどの結合辺にも一切マッチングできない。
>      これを `has_search_slack` として実装 (`mobile_groups` のブリッジ
>      探索の辺条件に追加)。理論的には正しいが、単独ではコーパス精度に
>      対して**中立** (退行なし、改善もなし — 6943/7439 で完全に同一)。
>      既知の残存ケース (`Oc1ccnc2cccn12`) の誤った橋渡しは橋頭原子自身の
>      辺を経由しないため、このフィルタだけでは直らない。
>   2. **縮環をまたぐブリッジの真の規則を実機で確認**: 縮環越しの互変異性
>      ブリッジが成立するのは、縮環の共有原子 (どちらか一方) が両方の環で
>      それぞれヘテロ端点に直接隣接しているときに限る、という規則
>      (I14 で既に採用していたもの) を、`Cc1c[nH]c2ncccc12` (橋渡しする)
>      と `Cc1c[nH]c2cccnc12` (橋渡ししない) の対比で実機 `inchi-1` により
>      直接検証した。この規則は `seed_groups` の「中心原子 1 個 + 直接
>      ヘテロ端点」検出でそのまま自然に捉えられており、**追加の実装は
>      不要**と判明 (回帰テスト
>      `mobile_h_fused_ring_bridges_when_fusion_atom_touches_both_heteroatoms`
>      で固定)。
>   3. **頂点分割の撤廃は大幅な退行**: 実 InChI のように縮環の共有原子を
>      環ごとに分裂させず単一頂点として扱う実験 (容量フィルタと併用) を
>      行ったところ、フル InChI 一致が **93.33% → 87.08%** に悪化した。
>      理由: 単一頂点モデルだとブロッサム探索が共有原子経由でもう一方の
>      環の無関係な辺へ自由に「乗り換え」てしまう (規則 2 の局所パターン
>      要件を経由しない不正な橋渡し)。頂点分割 (I14) はこの防止に必須
>      であり撤廃は不可。共有原子どうしを `matched[]` で直接対応付ける
>      折衷案も試したが、ブロッサム法の LCA 不変条件 (`matched[]` の対称性)
>      を壊し他分子でパニックを誘発したため撤回。
>   4. **残る未解明点**: `Oc1ccnc2cccn12` の橋頭 N9 自体の辺を経由しない
>      環 1 内だけの誤った橋渡し (O → C1 → C2 → C3 → N4) は、上記のどの
>      規則にも当てはまらず、2 回の実機計装調査 (うち 1 回は本ケース
>      専用) でも根本メカニズムを特定できなかった。手計算では「環 1 が
>      橋頭原子の存在により Kekule 構造の自由度を持たない (共鳴構造が
>      一意に定まる) ため環全体が互変異性の対象外になる」という環レベル
>      の適格性判定が疑われるが、実機トレースでこれを直接示す分岐点は
>      未発見。単純な追加ヒューリスティックでの対応は (a) 環外ドナーを
>      芳香環メンバー限定にすると正当な環外アミノ基のケースが壊れる、
>      (b) 頂点分割撤廃は上記 3. の通り大幅退行、の両方が確認済みで
>      不可。この 1 ケース種別のみ未解決として残す。
>   フル InChI 一致は **93.33%** で変化なし (`has_search_slack` は中立、
>   規則 2 は元々正しく実装済みだったため)。
> - **I17: 電荷分離 (zwitterion) 負電荷を可動プロトンから除外 (2026-08-04)**:
>   コーパス残差を層ごとに分類したところ、最大カテゴリが「余分な可動 H 群」
>   (114 件) で、その大半がニトロ基 (`N+(=O)O-`)・芳香族 N-オキシド
>   (`n+ - O-`) の O- を可動プロトンとして誤検出したものだった。実 InChI は
>   電荷分離で固定された負電荷 (隣接原子が正電荷) を除去可能なプロトンと
>   して扱わない (I11 の zwitterion 中性化スキップと同じ思想)。修正:
>   `is_locked_zwitterion_neg` (負電荷かつ重原子隣接に正電荷) の O- は
>   可動 H 数に数えない。ただし群メンバーとしては残す — ニトロの 2 つの O
>   は対称であり正準番号付けで等価化する必要があるため (可動数 0 の群を
>   `mobile_groups` が返し、h 層生成側で数 0 の群を出力から除外する)。
>   例: `CC[N+](=O)[O-]` は `(H,4,5)` を出さなくなり (`h2H2,1H3`)、
>   `O=[N+]([O-])O` (硝酸) は `(H2,2,3,4)` → `(H,2,3,4)` (可動 H は O-H
>   由来の 1 のみ)。回帰テスト
>   `mobile_h_zwitterion_charge_not_counted_as_mobile_proton` で固定。
>   加えて、別々の芳香環を単結合でつなぐ**ビアリール連結結合** (両端が
>   芳香族だが結合自体はどの環にも属さない = 共有環なし) をブリッジ探索の
>   辺から除外した。この結合はどの Kekule 構造でも単結合のままで二重結合の
>   付け替えに参加できず、環をまたぐ誤った橋渡し (例: `Nc1ccc(-c2ccncc2)cc1`
>   のアミノ基→対側環のピリジン N) を生んでいた。縮環 (共有原子あり) には
>   影響しない。回帰テスト `mobile_h_does_not_bridge_across_biaryl_bond`。
>   フル InChI 一致 **93.33% → 93.51%** (6956/7439)。
> - **I18: c 層枝順序・不定立体 C=N・縮環可動 H の一括改善 (2026-08-04)**:
>   コーパス残差 (485 件) を層別に分類し、大きい順に 4 件を修正:
>   1. **c 層の枝順序** (最大カテゴリ 187 件): 分岐点の子の出力順を
>      「部分木の原子数」だけでなく「部分木内で言及される環閉合 (後退辺)
>      の数」も加えた重みで決めるよう修正。実 InChI は環閉合も 1 項目として
>      数える (例: `CC1=CNC(=O)N1` は `4(7)6-3` ではなく `4(7)6-3` … 末端
>      =O の枝が環閉合を含む枝より先)。**93.51% → 95.91%** (+179)。
>   2. **不定立体 C=N の `?` 併記**: 定義済みの立体二重結合がある分子で、
>      立体源性だが未定義の C=N (イミン/ヒドラゾン) を `/b` 層に `hi-lo?`
>      として併記。両端が立体源性 (C は 2 置換基が CIP で異なる、N は
>      孤立電子対が 2 つ目) かつ非環・非累積・非芳香・可動 H 群非メンバー。
>      **95.91% → 96.05%**。
>   3. **縮環孤児の貪欲ペアリング**: I15 の「孤児が丁度 2 個」限定を、環に
>      沿って隣接する孤児を貪欲にペアリングする一般形に拡張。アクリドン型
>      の中央環 (全炭素の実二重結合が外側環/環外 =O に割り当たり孤児 4 個
>      以上) を交互閉路として探索でき N-H → C=O を 1 群に。**96.05% →
>      96.12%**。
>   4. **環内カルボニル受容体 C の辺追加**: ブリッジ探索の辺条件に「共有環
>      ありかつヘテロへの二重結合を持つ C (カルボニル型受容体)」を追加。
>      イサチン/フタルイミド型の非芳香族 5 員環で、芳香環経由のビニロガス
>      経路が両カルボニル O に到達し 3 端点 1 群に。**96.12% → 96.18%**。
>   回帰テスト: `undefined_stereogenic_cn_gets_question_mark` (stereo.rs)、
>   `mobile_h_acridone_vinylogous_amide`・`mobile_h_isatin_reaches_both_carbonyls`
>   (number.rs)。
> - **I19 (2026-08-04 開始)**: [RUST_INCHI_I19_PLAN.md](RUST_INCHI_I19_PLAN.md)
>   に基づき残差 284 件をタスク化して着手。§3.1 完了: `seed_groups` の
>   非 C/N 中心での一級アミド端点許可条件を一般化。硫黄 (スルホンアミド)
>   は S=O が 2 個必要だが、リンは P=O 1 個でも一級 NH2 (リン酸トリアミド
>   `NP(=O)(N)N` 等) が可動になる。**96.18% → 96.20%**。回帰テスト
>   `mobile_h_phosphoric_triamide`。§3.2 完了: O/S だけの酸系対に対する
>   N 除外規則 (カルバミン酸のような**置換**二級 N を除外) が、末端の
>   一級 NH2 (heavy_deg==1) まで過剰に除外していたのを修正。ジチオ
>   カルバミン酸 `NC(=S)S`・スルファミン酸 `NS(=O)(=O)O` の NH2 は酸対と
>   一緒に可動になる。**96.20% → 96.21%**。回帰テスト
>   `mobile_h_primary_amine_joins_os_acid_group`。
>
>   §3.3 完了 (広範囲に影響する重要な修正): 縮環の共有原子 (頂点分割で
>   複数の分身を持つ) が、環ごとに独立した別々の探索から「たまたま両方
>   とも通過する」ことがあった。その原子自身がヘテロ原子でない単なる
>   経由点 (例: 縮環の一方の環の炭素) であっても、`mobile_groups` の
>   union-find が**原子 ID ベース**で併合していたため、頂点分割が探索
>   グラフ上では正しく 2 つの環を分離していても、最終統合段でその区別が
>   失われ、本来無関係な 2 つの互変異性系が誤って 1 群に統合されて
>   いた (例: `Oc1cc2[nH]ncc2cn1` — 縮環の一方の環の環外フェノール性
>   OH↔環内ピリジン N の系と、もう一方の環内のピラゾール N-N-H の系は
>   本来無関係だが、共有炭素を経由点として両方の探索が通過するために
>   1 群 `(H2,7,8,9,10)` に誤併合されていた)。到達原子がヘテロ原子の
>   ときだけ union するよう修正 (非ヘテロの経由点まで union する必要は
>   ない — 頂点分割は探索グラフレベルで既に環を正しく分離している)。
>   正しく `(H,7,10)(H,8,9)` の 2 群に分離するようになった。縮環越しの
>   正当なブリッジ (I16 で検証済みの「共有原子が両環のヘテロ端点に直接
>   隣接」規則、`Cc1[nH]nc2ncccc12` 等) は退行なし。**96.21% →
>   97.41%** (+89)。回帰テスト
>   `mobile_h_independent_ring_systems_stay_separate`。
>
>   §3.2 追加精緻化: 「末端一級 NH2 は酸対から除外しない」規則が広すぎ、
>   無置換カルバミン酸 `NC(=O)O` (NH2 も一級) まで誤って可動化していた
>   (実 InChI は N 固定・O,O のみが酸対)。ジチオカルバミン酸 (酸対に S
>   を含む) やスルファミン酸 (中心が S) と異なり、**中心が炭素かつ酸対が
>   純粋に酸素だけ** (アミド性) の場合は一級 NH2 でも引き続き除外する
>   よう精緻化。**97.41% → 97.42%**。
>
>   **§3.4 完了 (I16 以来の既知の残差を解消)**: コーパス残差の最大カテゴリ
>   (可動 H 過剰検出 84 件) を分析したところ、半数以上が I16 で未解決だった
>   縮環系の橋頭ヘテロ原子パターン (`Oc1ccnc2cccn12` の系統) だった。橋頭
>   ヘテロ原子 (価数スラック 0・次数 3 以上、二重結合を一切持たない) を
>   含む環は、その環自体だけでなくもう一方の融合相手の環も含めて、複数
>   原子をまたぐブリッジ探索 (辺・孤立供与体起点) の対象から完全に除外
>   する新関数 `poisoned_ring_atoms` を追加。
>
>   実装は 2 段階の反復修正を要した。(1) 最初の実装は環内の**芳香族**原子
>   全てを毒したが、フラン型の次数 2 環ヘテロ原子 (橋頭ではない、単に
>   自分の 2 本の環内結合が常に単結合というだけの局所的事実) まで誤って
>   毒し、ベンゾオキサゾロンの N-H/C=O 互変異性を壊す退行が出た → 次数 3
>   以上 (真の縮環共有原子) に限定して修正。(2) それでもなお、橋頭原子を
>   含む環に属する原子への**局所的な単一中心パターン** (`seed_groups`、
>   実在する二重結合を使う直接の O-C=N 型) まで誤って毒し、別の退行が
>   出た → 局所パターンは橋頭原子の容量に一切依存しないため (実在する
>   二重結合を使うだけで、仮想的な環全体の交互性を必要としない)、毒化は
>   ブリッジ探索 (`mobile_groups` の辺構築・孤立供与体選択) のみに限定し、
>   `seed_groups` は毒化の影響を受けないよう修正。この 2 段階の絞り込みで
>   最終的に**退行ゼロ・純増 19 件**を達成。**97.42% → 97.61%**。回帰
>   テスト `mobile_h_bridgehead_ring_blocks_bridge_search_only`
>   (橋頭ケース・局所パターン・フラン型・飽和縮環の 4 対照を一括検証)。
>
>   **§3.5 完了**: 複数の可動 H 群を持つ分子で、実 InChI は群を**メンバー数
>   (端点原子数) 昇順**に出力するが、`layers.rs` の `build_components` は
>   最小 canonical 番号昇順で出力していた (例: アルギニンの guanidino 基
>   `(H4,8,9,10)` [3 端点] とカルボキシル `(H,11,12)` [2 端点] は、後者が
>   先であるべき)。実測で発見した H 位置タイブレーク問題 (§3.5 として当初
>   計画していた「同一原子への固定 H 割り当て」ではなく) — ソートキーに
>   群サイズ (昇順、同数なら従来どおり最小番号昇順) を追加するだけの
>   単純かつ安全な修正で、コーパス中の全 25 件の純粋な順序不一致が解消。
>   **97.61% → 97.94%** (+25)。回帰テスト
>   `hydrogen_mobile_groups_sorted_by_size_ascending` (layers.rs)。
>
>   **§3.4 追加修正**: §3.4 のブリッジ探索限定の毒化では防ぎきれない、
>   もう 1 つの抜け穴を発見。橋頭ヘテロ原子自身 (価数スラック 0、全ての
>   結合が恒久的に単結合) が `seed_groups` の**中心**として使われるケース
>   — その両隣の 2 つのヘテロ原子がそれぞれ独立した実在の二重結合を持つ
>   (橋頭原子とは無関係、集約判定で「受容体」に見える) だけなのに、橋頭
>   原子自身がそのどちらとも二重結合を作れるかのように誤って星型を形成
>   していた (例: `Oc1cc2cccnn2n1` — 橋頭 N の両隣にある独立した 2 つの
>   N を誤って拾い、環外フェノール性 OH の局所的な 2 端点群に無関係な
>   1 端点が混入)。中心自身にも `has_search_slack` (実在の二重結合を
>   最低 1 本持つ) を要求するよう修正 — 通常の正当な中心 (カルボン酸・
>   アミド等) は定義上必ず自分自身の実在二重結合を持つため影響なし。
>   **97.94% → 98.24%** (+22)。回帰テスト
>   `mobile_h_bridgehead_atom_cannot_be_seed_center`。
>
>   **§3.4 追加修正 2**: `is_acceptor_from` の集約判定 (I12 由来、芳香族
>   隣接原子が「どこかに」実在二重結合を持てば center-nb の特定の結合が
>   単結合でも受容体とみなす) は通常の柔軟な芳香環では正しい (Kekule 化の
>   向きは任意選択に過ぎない) が、橋頭ヘテロ原子を含む環では誤り —
>   その環の Kekule 構造は橋頭原子によって一意に固定されているため、
>   隣接原子の実在二重結合が center-nb の位置まで移動できるとは限らない
>   (例: `Oc1cn2cccc2cn1` — 環外フェノール性 OH が結合する炭素の実際の
>   二重結合は別の隣接炭素にあるが、環内のもう一方の隣接 N は橋頭 N とは
>   無関係な独立した実在二重結合を持つため集約判定では受容体に見える。
>   しかしその N は橋頭 N を含む環のメンバーであり、環全体が橋頭原子で
>   固定されているため実際には橋渡しできない)。nb が橋頭を含む環の
>   メンバーの場合は集約判定を無効化し、厳密な結合次数チェック
>   (center-nb が正確に二重結合か) にフォールバックするよう修正 —
>   `poisoned_ring_atoms` を `seed_groups`/`is_acceptor_from` にも渡す。
>   通常の柔軟な芳香環 (アミノピリジンの橋渡し等) は退行しない。
>   **98.24% → 98.75%** (+38)。回帰テスト
>   `mobile_h_aggregate_acceptor_disabled_in_bridgehead_ring`。
> - v1 の非対応 (→ `Unsupported`): 多成分 (塩)・同位体 (i)・有機金属切断。
>   残る文字列不一致の主因は (a) 一部の縮合環 c 層直列化 (番号付け)、
>   (b) 一部の可動 H 群 (RDKit と InChI の芳香族性モデル差に起因する
>   長距離/非対応パターンの過剰/過少検出、InChI Technical FAQ で「keto-enol
>   や長距離互変異性は既定では非対応」と明記されている既定のスコープ制限)。
>   詳細はコーパス実測で継続監視 (`tests/inchi_gate.rs` 参照、残タスクは
>   [RUST_INCHI_I19_PLAN.md](RUST_INCHI_I19_PLAN.md))。


関連文書: [RUST_PORT_HANDOFF.md](RUST_PORT_HANDOFF.md) (開発サイクル)、
[RUST_3D_PLAN.md](RUST_3D_PLAN.md)・[RUST_2D_PLAN.md](RUST_2D_PLAN.md) (完了済み)。

**残課題の実装計画**: [RUST_INCHI_I19_PLAN.md](RUST_INCHI_I19_PLAN.md)
(I18 時点の残差 284 件をカテゴリ別にタスク化。検証ワークフロー・優先順位・
各タスクの代表分子と退行注意を記載)。

## 目的

`MoleculeGraph` から **標準 InChI (InChI=1S/…)** と **InChIKey** を生成する。
IUPAC 公式 InChI (C 実装) と**ビット完全一致**させる — InChIKey は InChI 文字列の
SHA-256 ハッシュなので、公式キー (PubChem 等) と一致させるには InChI 文字列を
公式と完全一致させることが前提。molrs は依存クレートゼロのため **SHA-256 を
自前実装** する (FIPS 180-4)。

**v1 の対象**: 骨格層 — 式 (Hill)・接続 `c`・水素 `h` (共通 O/N 系の可動 H 群
`(H,n,m)` を含む)・電荷 `q`/`p`・InChIKey。立体 (`b`/`t`/`m`/`s`)・同位体 (`i`)・
一般の互変異性正規化は v2 以降。

## 差分検証の武器 (RDKit venv)

- `Chem.MolToInchi` — フル InChI 文字列
- `Chem.MolToInchiKey` — InChIKey
- `inchi.MolToInchiAndAuxInfo` の AuxInfo `/N:` — **公式の正準番号付け**。
  最難関の番号を文字列生成と切り離して単独ゲートできる
- `inchi.InchiToInchiKey` — 任意 InChI 文字列 → キー。SHA-256/base-26 の
  キー機構を InChI 生成と独立に検証できる

## モジュール構成 (`molrs::inchi`)

```
molrs/src/inchi/
  mod.rs        # 公開 API・InchiError・統括
  sha256.rs     # FIPS 180-4 SHA-256
  base26.rs     # InChIKey base-26 + キーレイアウト
  formula.rs    # Hill 式・多成分
  number.rs     # InChI 正準番号付け
  normalize.rs  # 電荷 q/p・可動 H 群認識
  layers.rs     # c/h/q/p 層直列化
```

公開 API:
```rust
pub fn to_inchi(g: &MoleculeGraph) -> Result<String, InchiError>;
pub fn to_inchi_key(g: &MoleculeGraph) -> Result<String, InchiError>;
pub fn inchi_key_from_string(inchi: &str) -> String;
pub fn inchi_of(smiles: &str) -> Result<String, InchiError>;
pub fn inchi_key_of(smiles: &str) -> Result<String, InchiError>;
```

## ステップ (I0–I5)

- **I0**: 本計画保存
- **I1**: sha256 (NIST ベクタ) + base26 + InChIKey レイアウト
  (`AAAAAAAAAAAAAA-BBBBBBBBSA-N`: 骨格 14 + 副層 8 + フラグ `SA` + プロトン化 1)。
  ゲート: `inchi_key_from_string` == RDKit `InchiToInchiKey`
- **I2**: Hill 式。ゲート: 式層一致
- **I3**: 正準番号付け (初期色 → Morgan 精緻化 → 正準最小化)。最難関。
  ゲート: AuxInfo `/N:` 一致
- **I4**: c/h/q/p 層 + `to_inchi`。ゲート: フル文字列一致 (v1 範囲)、全体一致率
- **I5**: `to_inchi_key` 結線 + CLI + `tests/inchi_gate.rs`。文書化

## リスク

| リスク | 対策 |
|---|---|
| 正準番号の公式完全一致 (最大) | AuxInfo `/N:` を単独ゲート化、文字列前に確定 |
| 可動 H (酢酸すら `(H,3,4)`) | v1 は共通 O/N 酸系に限定、逸脱は適用外として追跡 |
| コーパス全数一致は非現実的 | v1 範囲で 100%、全体は一致率報告 (単調増加) |
| SHA-256/base-26 実装ミス | NIST ベクタ + `InchiToInchiKey` 二重オラクル |

## 検証

フィクスチャは smiles2iupac `tools/` (RDKit venv) で AuxInfo `/N:`・フル InChI・
InChIKey をコーパスから採取し `MOLRS_ROOT/corpus/` へ。`tests/inchi_gate.rs` が
(a) 番号一致 (b) v1 範囲のフル文字列一致 (c) キー一致 (d) 全体一致率 を検査。
キー機構は `inchi_key_from_string` == `InchiToInchiKey` で恒久検証。

### I20 — 金属切断と多成分直列化 (2026-08-05)

標準 InChI は有機金属を「金属を切り離した形」で表現する (再結合形 `/r` 層は
非標準オプション `RecMet` 専用なので標準 InChI には現れない)。これに合わせて
`inchi::disconnect` を新設し、多成分の直列化を実装した。

**切断規則** — 金属原子に接続する全ての結合を切る。金属判定は公式 InChI の
金属表に従い、半金属のうち Sb/Sn/Bi/Po は金属側、B/Si/Ge/As/Te は非金属側
(コーパスで `C[SbH2]` は切断、`CC[AsH2]`・`Br[SiH2]C` は連結のままと確認)。

**電荷配分は結合相手で決まる** (コーパス実測):

- 金属-C / 金属-H → **等方開裂**。双方中性のまま。`C[Hg]C` → `2CH3.Hg` に
  `/q` が付かないのはこのため。金属-H の H は独立した `H` 成分になる
  (`[BiH3]` → `Bi.3H`)。
- 金属-ヘテロ原子 → **異方開裂**。配位子が陰イオン・金属が陽イオンになり、
  配位子側は後段の中性化で `/p`、金属側は `/q` に出る
  (`C[Hg]Cl` → `CH3.ClH.Hg/h1H3;1H;/q;;+1/p-1`)。

**成分順序** (コーパス 32 例から導出): **炭素を含む成分が先 → 重原子数の昇順
→ H 数の降順 → 式の辞書順**。炭素優先が必要な根拠は、硫酸ナトリウムが
`2Na.H2O4S` (Na が先) なのに安息香酸カリウムが `C7H6O2.K` (有機が先) になる
こと — 重原子数だけでは説明できない。重原子を含まない H だけの成分は常に末尾。

**直列化** — 各層を成分ごとに計算して `;` で連結し、連続する同一文字列は
`N*` で圧縮する (`c2*1-2;`)。全成分が空なら層自体を省略する
(`InChI=1S/Bi.3H` に c/h 層がない)。`/q` は成分ごと、`/p` は分子全体で 1 つ。

**水素分子の特例** — `[H][H]` は孤立 H が 2 個ではなく、「骨格原子 1 個 + その
結合 H 1 個」の単一成分として `InChI=1S/H2/h1H` になる。孤立 H の数え方を
連結成分ベースにして区別している。

**効果**: 金属化合物 19 件が一致するようになり、さらにそれまで
`Unsupported("multi-component")` で生成すらできなかった塩 14 件が出力対象に
なった。フル InChI **98.75% → 99.01%** (+33)、生成カバレッジ 7,439 →
**7,453 件 (全件)**、InChIKey 98.80% → 99.06%。

### I21 — メソ体の `/m`・`/s` 省略 (2026-08-05)

実 InChI は構造とその鏡像を両方正準化し、結果が一致すれば (= その分子が
アキラルなら) `/m`・`/s` 層を出力しない。メソ酒石酸が
`.../t1-,2+` で終わり `/m` が付かないのはこのため。

鏡像は全ての生パリティを反転したものなので、判定条件は「全パリティ反転を
実現するグラフ自己同型 π が存在するか」になる。π で正準番号を付け替えると、
中心 c のパリティは**隣接原子の正準昇順が π によって並べ替わる分だけ**反転
する (`new(π(c)) = old(c) XOR σ_c`、σ_c はその置換のパリティ)。CIP ランクと
R/S は自己同型で不変なのでこの項だけが効く。したがって全ての中心について

    old(c) XOR σ_c == !old(π(c))

を満たす π を探せばよい。π の探索は 1-WL 精緻化で求めた色クラス内での
バックトラッキング (ノード数に上限を設け、超えたら「アキラルと証明できな
かった」として `/m` を出す安全側に倒す)。

**効果**: メソ酒石酸・cis-シクロヘキサン-1,2-ジオール・meso-1,2-ジクロロ-
1,2-ジフルオロエタンの 3 件が一致。光学活性な 47 件には回帰なし。
フル InChI **99.01% → 99.05%** (+3)。

### I22 — 可動 H 群の厳密検証フィルタ (2026-08-05)

**理論**: 現在の Kekule 構造の二重結合を「マッチング M」とみなすと、供与体 d
から受容体 a へ H が移動できる条件は「d を根とする M-交互パスで a に到達
できる」ことと**厳密に同値**になる。理由は対称差の議論: H が d にある構造の
二重結合集合と、H が a にある構造の二重結合集合の対称差は、d-a 間の交互パス
1 本と交互閉路の集まりに分解される。したがって**現在の 1 通りの Kekule 構造
だけで判定して過不足がない** (全 Kekule 構造を列挙する必要がない)。単純路
制約 (同じ原子を 2 度通らない) は奇閉路の収縮 = ブロッサム法が正確に扱う。

**適用範囲を限定した理由**: この厳密判定で群そのものを作ると 88.8% まで悪化
することを実測で確認した。「何を端点と認めるか」(元素・価数・電荷の条件) は
実 InChI 固有の規則で、緩く取ると群を作りすぎるため。そこで端点の認定は従来
どおり `seed_groups` / ブリッジ探索に任せ、その結果に対して「本当に交互パスが
通るか」だけを検証する**偽陽性フィルタ**として使う。

**容量の扱い**: スルホニル S やホスホリル P は二重結合を 2 本持つため、
1 原子 1 マッチの単純マッチングでは Kekule 構造を表現できない。容量 (= その
原子が持つ二重結合の本数) の分だけ原子を複製する標準的な次数制約部分グラフ →
マッチングの帰着で対応する。これを入れないとスルホン酸の 3 つの O が 1 群に
ならず、かえって退行する (98.55%)。

**効果**: 局所的には 1,3 パターンに見えるが、必要な再 Kekule 化の連鎖が
N-H の価数などで破綻するケース (例: 6-5 縮環アザインドール型 + 環外 OH の
`[H]Oc1c[nH]c2ccnc-2n1`) を正しく非可動と判定できるようになった。これは I16
以来「長距離再 Kekule 化カスケード」として未解決だった残差で、2 度の
ヒューリスティック (距離カットオフ、縮環共有原子の相手判定) が失敗していた
ものを理論的に正しい方法で解決した。フル InChI **99.05% → 99.37%** (+24)、
InChIKey 99.06% → 99.39%。

### I23 — I19 §3.4 の近似の撤去 (2026-08-05)

I19 §3.4 では `is_acceptor_from` の集約判定を「`nb` が橋頭ヘテロ原子を含む
環のメンバーなら無効化」していた。これは「隣接原子の実在二重結合を当該結合
位置まで移動させる再 Kekule 化が本当に成立するか」を**局所情報で近似**する
回避策だった。I22 で厳密な交互パス検証が入り同じ判定を正確に行えるように
なったため、この近似は不要になっただけでなく**有害**になっていた —
橋頭 N を持つ縮環 (`Oc1ccn2ccnc2n1` 型) で本来より小さい群しか作れず、
16 件が不一致のままだった。

撤去により `seed_groups` / `is_acceptor_from` から `poisoned` 引数が消え、
コードも単純になった。なおブリッジ探索の**辺**での poisoning は依然有効で、
外すと 99.40% に低下することを実測したため残している。

**効果**: フル InChI **99.37% → 99.58%** (+16)、InChIKey 99.39% → 99.61%。

### 試行して棄却: 大域的 Kekule 充足可能性による群判定 (2026-08-05)

**動機**: I22 の「供与体を根とする単一の交互パス」判定は必要条件として強すぎる
場面がある。`[H]Oc1cc2c[nH]nc2nn1` (want `(H,7,10)(H,6,8,9)`) では、環外 OH の
H が環内 N7 へ移る際に**もう一方の群の N-H も同時に**別の N へ移る必要がある。
実際に有効な構造を手で構成して確認したところ、2 つの構造の二重結合集合の
対称差は 2 本の独立した交互パス (一方は増加路、他方は減少路) に分かれ、
1 本の路では O10 と N7 が繋がらない。

**試したこと**: 判定を「その端点に H を置いた Kekule 構造が分子全体として
存在するか」という大域的充足可能性に置き換えた (全群の端点の H 配置を自由に
組み替えてよく、露出端点の個数だけ保存する DFS)。

**結果**: 狙ったポリアザ縮環 12 件は正しくなり、I22 が直したケースと
スルホン酸も維持できたが、`[H]Xc1...c1-2` 型 (縮環結合が単結合で書かれる
6-5 縮環) の **16 件が新たに退行**し、差し引きで 99.58% → 99.54% と悪化した。

**理由の考察**: 実 InChI の t-group は「有効な構造が存在するか」ではなく、
`ichitaut.c` の 1,3/1,5 パターン検出とその併合、および BNS による検証という
固有の手続きの結果である。両ファミリーは局所パターン (O-C(-N)、N は別の
結合に二重結合を持つ) が完全に同一で、大域的にはどちらも「有効な構造が存在
する」ため、充足可能性では区別できない。一方 I22 の単一交互パス判定は
`[H]Xc1...c1-2` 型を正しく弾けており、実 InChI の挙動をよく近似している。

**結論**: 単一交互パス判定 (I22) を維持する。ポリアザ 12 件を正しく扱うには、
実 InChI の t-group ハブ (端点に +1 容量を与える仮想頂点) を含む BNS を
そのまま実装する必要があると思われる。→ **I24 で実装し、この見込みは正しかった**
(退行ゼロで 12 件を解消)。

### I24 — BNS t-group ハブ (2026-08-05)

上の棄却節が示唆したとおり、実 InChI (`ichi_bns.c`) と同じ **t-group ハブ**を
実装した。`hub_allowed_endpoints` (`molrs/src/inchi/number.rs`) が I22 の
`exact_reachability` を置き換える。

**グラフの作り** (頂点分割なしの π 全体グラフ):

- 原子 `i` を `n_double(i) + donor_h(i)` 個に複製する (最低 1 個)。
  `donor_h` は「その原子が現に持つ可動 H 相当の数 (H + 負電荷)」で、
  これが実 InChI の「端点はハブ経由で `MAX_AT_FLOW` が +1 される」に当たる。
  1 個の複製は「二重結合 1 本」か「H 1 個」のどちらかを表す。
- 候補群ごとに可動 H 数 `k` だけのハブ頂点を置き、その群の全端点の全複製と
  結ぶ。ハブは**群ごとに別々** (1 個の大域ハブだと群をまたいだ H 移動を
  許してしまう)。
- どの候補群にも属さない供与体 (非互変異性の N-H など) には自分だけに繋がる
  **専用ハブ**を与え、その H を動かないよう固定する。
- この構成で初期マッチング (実在の二重結合 + 「現に H を持つ端点 ↔ ハブ」) は
  **完全マッチング**になる。

**判定基準は「H 数が変わりうること」**。端点 `a` がメンバーである条件は、
そこに載る H の数が現状と違う値も取れること — すなわち

- `Fewer`: `a` のハブ辺を張る複製を 1 個減らしたグラフ、または
- `More`: `a` の結合辺を張る複製を 1 個減らしたグラフ (= 二重結合が 1 本
  少ない = H が 1 個多い)

のどちらかで最大マッチングのサイズが変わらないこと。複製どうしは隣接関係が
同一なので「先頭/末尾の何個を繋ぐか」で一般性を失わず上限を課せる。

片側だけでは不十分なことを実測で確認した:

- 「受け取れるか」だけ → 元々 H を持つ端点が自明に真になり、固定 O-H を
  取り込む (`[H]Oc1nc[nH]c2ccnc1-2` 型の 6-5 縮環が 10 件退行)。
- 「手放せるか」だけ → 容量の都合で必ず H を持つ端点を落とす
  (アミジン `CC(=N)N` の =NH。相方の NH2 が既に容量一杯だが、この =NH は
  「H を 2 個持つ」側 `CC(N)=N` へ動けるので可動)。約 24 件が退行し 99.01%。

**最大マッチング**が必要なので `blossom.rs` に `max_matching` (Edmonds の
増加路探索、頂点除去用の `skip` マスク付き) を追加した。既存の `lca` /
`mark_path` を再利用している。`alternating_reachable` はブリッジ探索側で
引き続き使う。

**効果**: フル InChI **99.58% → 99.75%** (+12、退行ゼロ)、
InChIKey 99.61% → 99.78%。狙ったポリアザ縮環 12 件がちょうど解消し、
候補群 (`seed_groups` + ブリッジ探索の union-find 出力) は元から実 InChI と
同じ分割になっていたことも確認できた — 誤っていたのはフィルタだけだった。

### I25 — ブリッジ探索にシフト位置条件 (2026-08-05)

ブリッジ探索の union に、実 InChI (`ichitaut.c`) が互変異性として認める
**シフト位置関係**の条件 (`taut_shift_ok`) を課した。

ブロッサム法の交互到達判定は「その位置に H を置いた再 Kekule 化が成立
するか」という**必要条件**しか見ない。実 InChI が互変異性とみなすのは
次の 3 つだけなので、それ以外を弾く:

| 種別 | 条件 | 例 |
|---|---|---|
| 1,2 | 直接隣接 | ピラゾール/トリアゾール環の N-N、インダゾールの 1H/2H |
| 1,3 | 共通の中心原子 | 単一中心の星型パターン (`seed_groups` と重なる) |
| 1,5 | `a-x-y-z-b` の中間 3 原子が同一の**交互環**に載る | 4-ヒドロキシピリジンの環外 O ↔ 環内 N (6 員環)、イサチンのケト O ↔ アミド N (縮環 5 員環) |

交互環の判定 (`is_alternating_ring`) は「環内の二重結合を 3 本数える」では
なく**全メンバーが `has_search_slack` を持つか**で見る。前者は縮環で共有辺の
二重結合をどちらの環が「借りる」かという Kekule 化の任意性に左右されるが、
後者は

- ナフタレン型 (環内二重結合が 2 本しかない側の環) でも、全原子が隣の環との
  共有辺で二重結合を持つので正しく交互環と判定する
- キノロンの環内 N-H のような「二重結合の代わりに H を持つ端点」が環を
  塞がない (これを `has_own_double` で見ると 4-キノロン等 58 件が退行した)
- クマリンのピラノン環はラクトン O が二重結合も H も持たないので弾かれる

**効果**: フル InChI **99.75% → 99.83%** (+6、退行ゼロ)。内訳は
1,4 シフト 3 件 (`O=Nc1ccc[nH]1` `[N-]=[N+]=Nc1ccc[nH]1`
`OS(=O)(=O)c1ccc[nH]1` — いずれも 5 員環の奇閉路をブロッサム収縮すると
到達できてしまう) と、交互環を経由しない 1,5 シフト 3 件
(`O=c1cc(O)c2ccccc2o1` `O=c1cc(N)c2ccccc2o1` `O=c1oc(O)cc2ccccc12`)。

> I24 計画が棄却した「結合距離のカットオフ」とは別物。距離だけでは同距離で
> 正反対の正解を持つ分子対を分離できないため、**交互環という環の条件と
> 組み合わせる**のが要点。

### I26 — h/q 層の文字列最小化 (2026-08-05)

正準番号付けの段 2/3 で使っていた「固定 H 数」「電荷」の **per-atom 順序
キーを外し**、(c 層, h 層, q 層) の辞書順最小化として `resolve` のタイブレーク
に移した (`h_signature` / `q_signature`)。

実 InChI は骨格 (H なし) の正準化で残った**自己同型の中から、層の文字列を
最小化する**番号付けを選ぶ。これは per-atom な不変量では原理的に表せない:

- 単純な **H 昇順**だと、骨格上対称な 2 原子のうち H を持たない方が先になる。
  シアナミド `NC#N` が `h3H2` (実 InChI は `h2H2`)、α-ヒドロキシケトン
  `O=C1CCCC[C@@H]1O` が `h6,8H` (実 InChI は `h5,7H`) になる。
- 逆に「**H を持つ方が先**」にすると 1-ブチン `C#CCC` を誤る。実 InChI は
  `h1H,4H2,2H3` で、H を持たないアルキン炭素が 3 番。末端の選択 (1↔2) と
  中間の選択 (3↔4) は骨格の自己同型として**連動**しているため。

h 層の比較キーは (H 数, 番号昇順リスト) を H 数昇順に並べた列。h 層は H 数の
昇順にグループ化され各グループ内は番号昇順、かつ **H を持たない原子は
現れない**。番号付けは置換なので「どの H 数がいくつ現れるか」は候補間で
不変であり、この列の辞書順比較が印字文字列の比較と一致する。q 層も同じ作り。

電荷も層の順序 (c → h → q) に従うので、メチルイソシアニド `[C-]#[N+]C` は
電荷ではなく先に h 層で決まり `h1H3` (CH3 が 1 番) になる。

**効果**: フル InChI **99.83% → 99.97%** (+11、退行ゼロ)。c 層の番号付け 5 件、
電荷・イソシアニド 3 件、立体 + 互変異性 2 件 (番号が直れば `/t` も追随)、
固定 H の位置 1 件 (`c1c[nH]c2cncc-2n1`)。正準番号も 99.38% → 99.53%。

### I27 — /b 層の最小化 (2026-08-05)

同じ層最小化を `/b` 層にも広げた (`BStereo` / `b_signature`)。

無水物 `C/C=C/C(=O)OC(=O)/C=C\C` は 2 つの半分が骨格の自己同型で入れ替わり、
c/h/q 層はどちらの番号付けでも同一になる。どちらの半分が小さい番号を取るかは
`/b` 層でしか決まらない。InChI の内部パリティは**奇 (`-`) = 1 < 偶 (`+`) = 2**
なので Z 側が小さい番号を取り `/b5-3-,6-4+` になる。

`/b` のパリティは「正準番号が最大の隣接どうしが二重結合の同じ側か反対側か」で
決まり**番号付けに依存する**ため、候補ごとに再計算する必要がある。番号非依存の
材料 (両端の非 H 隣接、CIP ランク最大の隣接、CIP 基準の E/Z) だけを `BStereo`
に持たせ、`b_signature` が候補の番号付けから毎回パリティを求める。

**効果**: フル InChI **99.97% → 99.99%** (+1、退行ゼロ)。

### I28 — 尿酸型の縮環炭素を種の中心に (2026-08-05)

`seed_groups` の星型判定を緩め、中心に受容体 (二重結合したヘテロ端点) が
無くても、**中心自身が実在の二重結合を持ち供与体端点が 2 つ以上ある**なら
種にするようにした (`donor_relay`)。

尿酸 `O=c1[nH]c(=O)c2[nH]c(=O)[nH]c2[nH]1` は 2 つの環の N-H (N7/N8) が
縮環炭素 C2 を中心とする 1,3 の関係にある。C2 の二重結合は相方の縮環炭素 C1 に
向いていて受容体端点が無いため従来は種にならなかったが、C1 は芳香系の中で
二重結合を手放せるので `C2=N7` / `C2=N8` は成立し、実 InChI は 4 つの N-H と
3 つの O を 1 群 `(H4,6,7,8,9,10,11,12)` にする。

ブリッジ探索は縮環の共有原子を環ごとに分裂させる (頂点分割) ため縮環を
またげず、この関係を拾えない。**頂点分割を外す実験も試したが**、インダゾール/
アザインドール型の誤検出が復活して 7,453 件中 367 件が落ちた (95.1%)。分割は
そのままに局所検出側で拾うのが正しい。

**効果**: フル InChI **99.99% → 100.00%** (+1、退行ゼロ)。

### 到達点: コーパス全件一致 (2026-08-05)

コーパス `corpus/inchi_dump.jsonl.gz` の **7,453 分子すべて**で、フル InChI
文字列と InChIKey が RDKit 経由の実 InChI と完全一致した。ゲート
(`molrs/tests/inchi_gate.rs`) は率の閾値ではなく `assert_eq!` による完全一致に
切り替えてある。

推移: I4 74.17% → … → I23 99.58% → I24 99.75% → I25 99.83% → I26 99.97%
→ I27 99.99% → I28 **100.00%**。

補助ゲートの `formula_layer_matches_rdkit` (99.14%) と
`canonical_numbering_matches_auxinfo` (99.54%) が 100% でないのは**規約差**で、
フル InChI の正しさとは矛盾しない:

- 式層ゲートは公開ヘルパ `inchi::formula()` を RDKit の式と比べている。
  InChI の式層は陰イオンでも**中性の式**を書き `/p-1` で脱プロトンを表す
  (酢酸イオン `CC(=O)[O-]` → `C2H4O2/…/p-1`) が、`formula()` は電荷を
  反映した `C2H3O2` を返す。`to_inchi` は正しく前者を出す。
- 番号付けゲートは RDKit AuxInfo `/N:` と比べており、多成分系の成分順などの
  規約が InChI 本体と異なる。

コーパス外への一般化 (異種元素・多成分・複雑な立体) は未検証なので、
新しい分子群を扱うときは改めて差分検証すること。

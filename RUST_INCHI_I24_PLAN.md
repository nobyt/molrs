# I24 作業指示書 — BNS t-group ハブの実装

> **この文書だけを読んで着手できるように書いてある。**
> 全体の設計思想と経緯は `RUST_INCHI_PLAN.md`、直前の残差整理は
> `RUST_INCHI_I19_PLAN.md` を参照。

## 現在地 (2026-08-05, commit `a69fcc3`)

コーパス `corpus/inchi_dump.jsonl.gz` (RDKit オラクル、7,453 分子) に対して:

| 指標 | 値 |
|---|---|
| フル InChI 完全一致 | **99.58%** (7,422 / 7,453) |
| InChIKey 完全一致 | 99.61% (7,217 / 7,245) |
| 正準番号 (AuxInfo `/N:`) | 99.38% |
| 式層 (Hill) | 99.14% |

生成カバレッジは全 7,453 件 (I20 の多成分対応で `Unsupported` が消えた)。
残る不一致は **31 件**。

## このタスクのゴール

残差 31 件のうち最大の塊である **ポリアザ縮環 12 件** を、実 InChI と同じ
機構 — `ichi_bns.c` の **t-group ハブ** — を実装して解消する。

### 対象の 12 分子

```
[H]Oc1cc2c[nH]nc2nn1      [H]Sc1cc2c[nH]nc2nn1
[H]Oc1cc2n[nH]cc2nn1      [H]Sc1cc2n[nH]cc2nn1
[H]Oc1cc2n[nH]nc2cn1      [H]Sc1cc2n[nH]nc2cn1
[H]Oc1cc2n[nH]nc2nn1      [H]Sc1cc2n[nH]nc2nn1
[H]Oc1ncc2n[nH]cc2n1      [H]Sc1ncc2n[nH]cc2n1
[H]Oc1nnc2n[nH]cc2n1      [H]Sc1nnc2n[nH]cc2n1
```

例 (`[H]Oc1cc2c[nH]nc2nn1`):

```
got  .../h1-2,10H,(H,6,8,9)          ← 環外 OH (atom 10) が固定されている
want .../h1-2H,(H,7,10)(H,6,8,9)     ← (H,7,10) の群ができるべき
```

## なぜ今の実装で落ちるのか

I22 で入れた偽陽性フィルタ `exact_reachability`
(`molrs/src/inchi/number.rs:517`) が、この群を落としてしまっている。

フィルタの理屈は「現在の Kekule 構造の二重結合をマッチング M とみなすと、
供与体 d から受容体 a へ H が移動できる条件は、d を根とする M-交互パスで
a に到達できることと同値」(対称差の議論)。これは **1 個の H が動く場合**は
厳密に正しい。

しかしこの 12 分子では、**2 個の可動 H が同時に動く**ことで初めて互変異性が
成立する。`[H]Oc1cc2c[nH]nc2nn1` について手で確認済み:

- 構造 A (入力): H は O10 と N6 の上。二重結合 `C4=C1, C3=C2, C5=N8, N9=N7`
- 構造 B (有効): H は N7 と N8 の上。二重結合 `C4=O10, C3=C1, C2=N6, C5=N9`

両者の対称差は **2 本の独立した交互パス**に分かれる:

```
path1: O10 -B- C4 -A- C1 -B- C3 -A- C2 -B- N6    (増加路: O10 と N6 を結ぶ)
path2: N8  -A- C5 -B- N9 -A- N7                  (減少路: N8 と N7 を結ぶ)
```

O10 と N7 は**同じ路の上に載らない**ので、単一交互パス判定では永久に繋がら
ない。正味の効果は「O10 と N6 の H が外れ、N7 と N8 に H が付く」で、
マッチングのサイズは保存される。

## 解決策: t-group ハブ

実 InChI (`ichi_bns.c`) は各 t-group に**仮想のハブ頂点**を置き、群の全端点を
ハブに繋いで容量 +1 を与える。こうすると上の path1 と path2 がハブを経由して
**1 本の交互路に繋がる**:

```
O10 → … → N6 → [hub] → N8 → … → N7
```

`molrs/src/inchi/number.rs:228` の `has_search_slack` の doc コメントに、
この「+1 容量」を t-group ハブなしで直接モデル化した旨が既に書いてある
(I16 の実機計装調査の産物)。今回はそのハブを実際に作る。

### 実装方針

1. **グラフ構築** — `exact_reachability` を拡張する。既に容量による頂点複製
   (スルホニル S = 二重結合 2 本) は実装済みなので、その仕組みに乗せる:
   - 群ごとにハブ頂点を `k` 個 (k = その群の可動 H 数) 作る
   - ハブの各複製を、その群の全端点の複製に辺で繋ぐ
   - 初期マッチング: H を持つ端点をハブの複製に対応付ける
     (これで**全頂点がマッチ済み**の完全マッチングになる)
2. **判定の読み替え** — 全頂点がマッチ済みになるので「露出頂点から探索」が
   使えない。端点 `a` が H を持てる条件は
   **「辺 (a, ハブ複製) がある完全マッチングに属する」**。標準的な判定は
   `G − a − hub_clone` に完全マッチングが存在するか。
3. **最大マッチングが要る** — `blossom.rs` は
   `alternating_reachable` (`blossom.rs:58`) しか公開していない。
   Edmonds の最大マッチング (増加路を繰り返し探す) を追加する必要がある。
   既存の `lca` / `mark_path` が再利用できる。

### 落とし穴

- **群の可動 H 数 `k`** は `mobile_groups` の出力にある。ハブ容量はこれ。
- **複数群が同時に動く**のが本質なので、ハブは**群ごとに別々**に作ること。
  全端点を 1 個の大域ハブに繋ぐと群をまたいだ H 移動を許してしまう。
- 現在の `exact_reachability` は「頂点分割なしの π 全体グラフ」だが、
  `mobile_groups` 本体のブリッジ探索は**別の**頂点分割グラフを使っている
  (縮環共有原子を環ごとに分裂させる)。混同しないこと。

## やってはいけないこと (実測で棄却済み)

`RUST_INCHI_PLAN.md` 末尾の「試行して棄却」節に詳細。要点だけ:

1. **大域的 Kekule 充足可能性で判定する** — 「その端点に H を置いた構造が
   分子全体として存在するか」を DFS で判定。狙った 12 件は直るが
   `[H]Xc1...c1-2` 型 (縮環結合が単結合で書かれる 6-5 縮環) の **16 件が
   退行**し、99.58% → **99.54%** と悪化した。両ファミリーは局所パターンが
   同一で大域的にもどちらも構造が存在するため、この基準では区別できない。
2. **純粋 blossom で群そのものを作る** — 端点認定を緩く取ると群を作りすぎ、
   **88.8%** まで悪化する。端点認定は `seed_groups`
   (`molrs/src/inchi/number.rs:339`) に任せ、BNS は**偽陽性フィルタ**として
   使うこと。
3. **結合距離のカットオフ (1,3/1,5 のみ許す)** — 同じ距離で正反対の正解を
   持つ分子対が実在するため、距離だけでは原理的に分離できない (実測確認済み)。
4. **c2rust で本家 InChI を移植** — `c2rust 0.22.1` が `ichitaut.c` /
   `ichi_bns.c` / `ichinorm.c` で軒並みパニックする (`iterators.rs:332`)。
   環境不備ではないことを自明な C ファイルの変換成功で確認済み。

## 検証サイクル (必ず全部通すこと)

```bash
cargo build --release
cargo test --release --lib inchi::
rtk proxy cargo test --release --test inchi_gate -- --nocapture --test-threads=1
cargo clippy --release --all-targets -- -D warnings   # 出力は字下げされる。
                                                       # grep '^error' では拾えない
cargo fmt -p molrs
cargo test --release -p molrs
```

- **ゲート閾値**は `molrs/tests/inchi_gate.rs` にあり、変更のたびに実測値の
  **わずかに下**へ引き上げる (実測値ちょうどだと浮動小数で落ちる)。
  変更理由は日本語のコメントで残す慣例。
- **残差の分類**には使い捨ての `molrs/tests/dbg_*.rs` を `#[ignore]` 付きで
  作り、**コミット前に必ず削除**する。
- 退行ゼロが原則。1 件でも落とすなら理由を明示すること。
- コミットメッセージは `I24: <英語要約> (99.58% -> X%)` + 日本語本文。

## 残り 31 件の内訳 (このタスク以外)

| 件数 | 種別 | メモ |
|---|---|---|
| 12 | ポリアザ縮環の可動 H 群 | **本タスク** |
| 9 | c 層の番号付け (縮環・部分飽和) | `C1=Cc2cnccc2CN1` 等 |
| 6 | 余分な可動 H 群 | ニトロソピロール `O=Nc1ccc[nH]1`、4-ヒドロキシクマリン等。実 InChI が 1,3/1,5 の短距離パターンしか認識しないことに起因する可能性 |
| 3 | 電荷・イソシアニドの番号付け | `NC#N` は got `h3H2` / want `h2H2`。H の多い方に小さい番号が付く — プロペンで実証した「H 昇順」則と逆に見える |
| 2 | 立体 + 互変異性 | `O=C1CCCC[C@@H]1O` |
| 1 | `/b` 層 | 無水物 `C/C=C/C(=O)OC(=O)/C=C\C` |

## 新セッションの開始手順

```
cd /home/tanaka/ghq/github.com/nobyt/molrs
claude        # または既存セッションで /clear
```

最初のプロンプト例:

> `RUST_INCHI_I24_PLAN.md` を読んで、I24 (BNS t-group ハブ) を実装してください。

> **注意**: このリポジトリには git remote が設定されていない。push は
> できないので、ローカルコミットのみで進めること。
